# 假设台账

agent 替用户做的默认选择。遇到欠定点的标准动作: 提出带默认值的选项 →
记录一条假设 → 继续推进；禁止静默填补。
状态: 生效 | 已确认（迁入 decisions.md）| 已推翻（评估波及,回对应阶段返工）。
格式: `- A-<n> <日期> [状态] <假设内容> — 默认理由: <一句话>`

- A-1 2026-07-29 [生效] 技术栈采用 Next.js + React + TypeScript + SQLite 的单仓 Web 应用 — 默认理由: 前后端一栈、本地启动简单，适合快速形成可演示闭环
- A-2 2026-07-29 [生效] 产品本地优先、单机运行且首版不做登录；敏感配置只保存在服务端本地 — 默认理由: 目标用户是单 owner，先避免无价值的账号与部署复杂度
- A-3 2026-07-29 [生效] 提供“规划、实施、复核”三个可编辑角色模板，但模板之间没有固定上下级，owner 可删除、改名或新增角色 — 默认理由: 降低首次组队门槛，同时保持角色可配置和平等协作
- A-4 2026-07-29 [生效] 每次 Agent 输出必须明确选择“交棒给某位组员”“请求 owner 决策”或“提议完成”；平台调度器只校验成员、限额和安全策略，不替团队做业务决策 — 默认理由: 让自主接力可执行、可观察，也避免暗中引入中央管理 Agent
- A-5 2026-07-29 [生效] “完成”需至少一名非执行者 Agent 复核通过，或由 owner 明确终止；运行具备可配置的轮次/用量上限 — 默认理由: 防止单 Agent 自说自话完成和失控循环
- A-6 2026-07-29 [生效] 文件工具被限制在项目绑定根目录内；命令采用受控执行并记录输入、输出与退出码，越界、破坏性和外部副作用动作进入审批 — 默认理由: Agent 获得实际执行力时必须保留最低安全边界和审计链
- A-7 2026-07-29 [生效] 首版界面与内置文案使用简体中文，桌面浏览器优先并兼顾基础响应式布局 — 默认理由: 当前需求语言与本地使用场景均指向中文桌面端
- A-8 2026-07-29 [生效] 仅借鉴 Clowder AI 的产品原则与信息架构，不复制其品牌、猫角色、商标或视觉资产，也不直接移植其实现代码 — 默认理由: 保持产品边界清晰并规避品牌与实现耦合
- A-9 2026-07-29 [生效] 首版技能是可复用、可版本化并分配给 Agent 的文本指令包，不包含任意代码执行或远程市场安装 — 默认理由: 先验证角色能力组合，避免把插件供应链安全引入 MVP
- A-10 2026-07-29 [生效] 使命看板是子任务、依赖、负责人和状态的唯一事实源；Agent 可提出或执行合法状态变更，平台以确定性规则校验 — 默认理由: 自治协作仍需要机器可判定的项目状态，不能只依赖聊天叙述
- A-11 2026-07-29 [生效] 共享记忆只收录带来源的结构化条目（目标、决策、事实、产物、经验），原始聊天不会无筛选地自动变成长久记忆 — 默认理由: 降低错误沉淀与上下文无限膨胀
- A-12 2026-07-29 [生效] 首版最多同时运行两个独立子任务；每个并行任务先产出隔离草稿或变更集，共享文件冲突必须串行整合并留痕 — 默认理由: 提供真实并行价值，同时控制资源消耗和并发写入风险
- A-13 2026-07-29 [生效] owner 的新消息、@Agent 或暂停指令优先于后续 Agent 调度；正在执行的原子工具动作安全结束后，团队按 owner 最新方向继续 — 默认理由: owner 是项目方向的最终裁决者，但不应通过强杀进程破坏工作区
- A-14 2026-07-29 [生效] Agent 的稳定角色身份与底层 provider/model 绑定分离，更换模型不丢失其项目成员身份、技能和协作历史 — 默认理由: 持久身份是参考设计中最值得保留的核心能力
- A-15 2026-07-29 [生效] 并行运行引用的目标、决策或依赖发生变化时，旧结果标记为过期且不能自动推动后续接力 — 默认理由: 基于失效上下文继续自治比普通执行失败更危险
- A-16 2026-07-29 [生效] UI 展示消息、结构化状态、工具动作、用量、产物和证据，不保存或展示模型隐藏思维链 — 默认理由: 协作透明应建立在可审计事件而非私密推理文本上
- A-17 2026-07-29 [生效] 协作驾驶舱以桌面三栏为主，窄屏把右侧上下文收进可切换抽屉，移动端首版只保证查看、发言和审批 — 默认理由: 复杂协作需要并列上下文，目标用户的主要创作场景是桌面端
- A-18 2026-07-29 [生效] 首版先交付浅色主题：温暖中性色作为底色，每名 Agent 使用一枚克制的专属强调色；不使用无意义紫色渐变、玻璃拟态或多套阴影 — 默认理由: 营造陪伴感同时保持长时间工作的可读性
- A-19 2026-07-29 [生效] Agent 头像首版使用可上传图片或自动生成的文字/几何占位，不使用 emoji 或仿 Clowder 猫角色；图标统一来自同一图标库 — 默认理由: 提供身份辨识度，同时避免廉价占位和品牌模仿
- A-20 2026-07-29 [生效] Provider API key 必须以本机独立主密钥加密后持久化，保存后任何 API 与 UI 只返回掩码提示，密钥及明文不得进入日志 — 默认理由: S-2 要求配置持久化，明文写入 SQLite 会制造不必要的凭据泄露风险
- A-21 2026-07-29 [生效] S-2 的 Agent 头像先交付文字/几何头像与可选强调色，不实现图片上传；图片头像可在真实反馈证明需要后追加 — 默认理由: 当前演示判据只要求角色可辨识，文件上传不是组队闭环的必要条件
- A-22 2026-07-29 [生效] OpenAI-compatible 连接验证只接受 http/https URL，禁止 URL 内嵌凭据并设置有限超时；非 HTTPS 地址必须由 owner 明确确认后才保存 — 默认理由: 兼容本地模型服务的同时避免静默接受明显不安全的远程配置
- A-23 2026-07-29 [生效] Provider 主密钥必须与 SQLite、源码、日志和 API 响应分离；仅拿到数据库不能解密凭据，主密钥缺失或无法安全创建时保存必须失败关闭且不得落盘 API key — 默认理由: “数据库里不是明文”不足以证明凭据边界安全
- A-24 2026-07-29 [生效] Provider 验证请求不跟随重定向并在 10 秒内超时，任何 3xx 都视为失败且不得把 Authorization 转发到新地址 — 默认理由: 防止 owner 配置的 endpoint 通过重定向把凭据带往另一主机，同时避免配置界面无限等待
- A-25 2026-07-29 [生效] 持久文本采用保守上限：名称 80、职责 160、说明 280、URL 2048、模型标识 120、API key 8192、技能/系统提示正文 20000 个字符；头像文字 1–4 个 Unicode 字符；单次 token 预算为 1–1000000 的整数、接力轮次为 1–100 的整数 — 默认理由: 阻止异常输入撑大本地数据库与 UI，同时覆盖常见模型标识、提示词和预算范围
- A-26 2026-07-29 [生效] Provider 模型目录响应最多读取 1 MiB 且最多接受 10000 个模型条目，超限按不兼容响应失败 — 默认理由: 连接验证只需确认一个模型标识，不应让用户配置的 endpoint 无界占用本地内存
- A-27 2026-07-29 [生效] 项目工作区必须是本机已存在、可读取的绝对目录，保存前解析为真实规范路径；S-3 只绑定和展示路径，不读取文件内容或运行命令 — 默认理由: 先建立明确边界，实际工具访问留到 S-5 单独验收
- A-28 2026-07-29 [生效] 同一个规范工作区同一时间只绑定一个项目，改绑需 owner 明确确认；禁止用相对路径、文件路径或不存在目录占位 — 默认理由: 避免后续多个自治项目同时改写同一目录
- A-29 2026-07-29 [生效] 项目成员关系不记录 leader/rank，所有 Agent 具有相同协作地位；职责和工具权限来自 Agent 自身配置，不等于组织层级 — 默认理由: 落实用户要求的群组内平等协作
- A-30 2026-07-29 [生效] S-3 的使命看板由 owner 手工建立目标与子任务，自动拆解与领取从 S-4 开始；任务状态由确定性规则维护，不从聊天文本猜测 — 默认理由: 先验证共享项目上下文和事实源，再引入模型自治
- A-31 2026-07-29 [生效] 共享记忆仅包含目标、决策、事实、产物四类显式条目，每条必须有来源类型、来源引用和创建者；S-3 由 owner 写入，Agent 只通过相同项目上下文快照读取 — 默认理由: 防止原始聊天或无来源结论静默进入长期记忆
- A-32 2026-07-29 [生效] 项目组至少包含 2 名 Agent，不设置产品层硬上限；同一 Agent 在项目内只能有一个成员关系且不设 leader — 默认理由: 两名成员是协作与后续接力的最小闭环，额外上限应由真实资源反馈决定
- A-33 2026-07-29 [生效] 使命标题最多 80、目标 5000、子任务标题 160、说明 5000、记忆正文 20000、来源引用 2048 个字符；超限明确拒绝 — 默认理由: 保持项目上下文可读并阻止异常文本无界撑大数据库
- A-34 2026-07-29 [生效] 看板状态固定为待办、进行中、阻塞、完成；依赖必须同属一个使命且禁止自依赖和环路，状态变更采用 optimistic version 冲突保护 — 默认理由: 使命看板是机器事实源，必须有确定性状态和可验证依赖
- A-35 2026-07-29 [生效] 自动化 demo 使用独立进程中的本地 OpenAI-compatible 测试 provider 执行真实 HTTP chat-completions 协议；产品运行时与外部 provider 走同一路径，不把 fixture 输出写死在 Agent 实现中 — 默认理由: 仓库不能携带用户云端凭据，但仍需验证真实网络、凭据解密、请求和响应解析边界
- A-36 2026-07-29 [生效] S-4 采用单持棒者顺序接力，同一协作运行任一时刻只有一名 Agent 生成或提交动作；独立子任务并行从 S-5 开始 — 默认理由: 先验证平等接力、暂停和恢复，再引入并行冲突面
- A-37 2026-07-29 [生效] Agent 每轮只提交受 schema 约束的消息、任务提案、领取、交棒、决策请求或完成动作；平台不保存/展示隐藏思维链，结构解析失败只允许一次不计业务轮次的格式修复请求 — 默认理由: 审计应基于明确动作，不依赖私密推理文本
- A-38 2026-07-29 [生效] owner 的新消息优先于待继续的自治轮次；显式 @Agent 把下一棒定向给该成员，未 @ 时由当前持棒者结构化交棒，系统不设固定协调者 — 默认理由: owner 必须能随时介入，同时保持 Agent 平等
- A-39 2026-07-29 [生效] S-4 每次运行最多 50 个业务轮次，并逐 Agent 执行其 maxTokens 与 maxHandoffs 配置；达到边界即暂停并说明，不静默追加调用 — 默认理由: 防止异常模型响应形成无限接力或不可审计消耗
- A-40 2026-07-29 [生效] 决策请求包含一个问题和 2–8 个明确选项，也允许 owner 自由文本回答；等待回答时运行持久化暂停，重启后可继续 — 默认理由: 将真正需要 owner 裁决的分歧显式化，而不是让 Agent 猜测
- A-41 2026-07-29 [生效] 协作 turn 的持久 lease 为 120 秒；调用中断或 lease 过期只允许 owner/客户端显式重试，新 attempt 可再次调用 provider 但同一 attempt 的状态动作最多提交一次 — 默认理由: 本地 Next 进程没有常驻队列，必须能从崩溃中恢复又不能重复写任务/交棒
- A-42 2026-07-29 [生效] 单轮 owner/Agent 可见消息最多 10000/20000 字符，模型每轮最多提议 20 个子任务；任务字段沿用 S-3 边界，交棒摘要最多 5000，决策问题最多 1000 字符 — 默认理由: 控制 prompt、时间线和结构动作的最坏体积
- A-43 2026-07-29 [生效] S-4 要求 provider 返回非负整数 usage；缺失或不一致时不伪造 token 数，当前 turn 失败并暂停等待 owner 重试或更换 provider — 默认理由: owner 要看到可信用量，预算不能建立在猜测上
- A-44 2026-07-29 [生效] 模型 prompt 始终包含完整当前使命/看板/active 记忆，但公开聊天时间线只带最近 30 条且最多 60000 字符；更早内容仍完整保留在审计时间线，不静默写入摘要 — 默认理由: 在尚未引入可信摘要机制前给 prompt 一个确定上界，同时不伪造丢失历史
- A-45 2026-07-29 [生效] chat-completions 响应最多读取 1 MiB；超限视为 provider 响应无效并暂停，原始响应不落盘 — 默认理由: Agent 可见消息和结构动作已有更小边界，不应允许外部 endpoint 无界占用内存
- A-46 2026-07-30 [生效] S-5 同一项目最多并行两个 execution，且只能调度依赖已完成、已领取、状态为进行中的不同任务；同一任务任一时刻最多一个 active execution — 默认理由: 延续 A-12 的资源上限，并以看板 DAG 作为可机械判定的并行边界
- A-47 2026-07-30 [生效] 每个 execution 在项目工作区之外建立独立 staging sandbox，Agent 的文件写入和命令都不直接作用于 canonical workspace；Git 工作区优先使用隔离 worktree，非 Git 工作区使用有大小上限的快照副本 — 默认理由: 并行执行必须先隔离，不能让两个 Agent 在 owner 源目录内竞态覆盖
- A-48 2026-07-30 [生效] S-5 只开放结构化的目录列举、文本文件读取/写入和进程执行工具；路径解析 realpath 后必须位于 sandbox，拒绝符号链接逃逸、设备路径、二进制/超限文件与任意 shell 字符串 — 默认理由: workspace_guarded 要求工具边界可审计且默认失败关闭
- A-49 2026-07-30 [生效] 命令以 executable + args 直接启动，不经 shell；默认只允许项目配置声明的本地验证命令，未声明命令、网络能力、包安装、删除/覆盖大量文件或其他高风险动作必须由 owner 单次批准 — 默认理由: 首版本地进程不是恶意代码安全沙箱，必须把不可可靠隔离的能力放进审批边界
- A-50 2026-07-30 [生效] 低风险、无冲突且验证通过的 staged change 可由平台原子合入 canonical workspace；涉及 stale baseline、同路径并发修改、删除、二进制或验证失败时禁止自动合入并暂停等待 owner — 默认理由: 保留自治效率，同时避免静默覆盖 owner 或另一 execution 的新改动
- A-51 2026-07-30 [生效] execution 捕获任务、依赖、项目上下文和 workspace 文件基线 hash；任一相关事实或文件在提交前变化，结果标记 stale，usage/工具审计保留但不得推进任务或接力 — 默认理由: 落实 A-15，旧上下文产物不能因为模型调用成功就自动进入主工作区
- A-52 2026-07-30 [生效] 工具调用与进程输出分别设置数量、文本体积、运行时间和总执行轮次上限；进程超时先终止进程树，无法确认终止时 execution 失败关闭且不合入 — 默认理由: 本地自治执行必须有确定资源边界和可恢复终态
- A-53 2026-07-30 [生效] S-5 的“安全”指平台级路径、权限、审批、隔离、超时、冲突和审计 guardrail，不声称能把 owner 明确批准的任意本地可执行文件变成抗恶意 OS 沙箱 — 默认理由: 当前本地 Next.js 架构没有容器/虚拟机隔离，必须准确表达安全保证而非制造虚假边界
- A-54 2026-07-30 [生效] 单个 sandbox 快照最多 100000 个目录项或 2 GiB；结构化文本文件单次读取/写入最多 1 MiB，目录列举最多 1000 项；超限必须在执行前或工具调用时明确暂停 — 默认理由: 本地产品需要兼容真实项目，又不能让快照或模型工具无界占用磁盘与内存
- A-55 2026-07-30 [生效] 单个 execution 最多 20 个模型回合、40 次工具调用和 15 分钟墙钟时间；单命令最多 120 秒，stdout/stderr 各最多保留 1 MiB，超限截断并标记而不是继续积累 — 默认理由: 给代码/文档任务足够迭代空间，同时确保失控执行可机械终止
- A-56 2026-07-30 [生效] owner 可把 test/build/lint/typecheck 等精确 executable/args/workdir 写入可见验证政策，保存政策即构成可撤销的 standing approval；未精确匹配政策的 executable/args、包安装和网络工具都生成只绑定一次请求的 owner 审批，写 canonical workspace 的命令始终禁止 — 默认理由: 高频验证需要自治执行，但授权必须由 owner 显式配置且不能扩张到近似命令或任意 shell
- A-57 2026-07-30 [生效] 自动合入只接受基线未变、验证成功、无路径冲突的 UTF-8 文本新增/修改，最多 100 个文件且总变更内容不超过 10 MiB；删除、重命名、二进制、权限位变化或超限一律等待 owner 审批/处理 — 默认理由: 首版先覆盖常见安全增量，破坏性或难以可靠预览的变更不应静默落到主工作区
- A-58 2026-07-30 [生效] 平台对命令请求能机械识别的 shell 语法、canonical 路径、远程发布子命令和危险参数绝对拒绝；对无法静态证明副作用的本地 executable，owner 必须在“非 OS 沙箱”警示下通过验证政策 standing approval 或精确一次性批准授权，平台不承诺获批程序无法自行访问网络或系统资源 — 默认理由: 未使用容器/虚拟机时，宣称可证明任意二进制真实副作用为零是不诚实且不可测试的，但 owner 应能显式授权可信项目的重复验证命令
- A-59 2026-07-30 [生效] 平台合入在无不合作外部写入时通过 journal 保证自身多文件变更崩溃恢复后全旧或全新；若提交/恢复窗口检测到外部程序改写 canonical 文件，必须保留外部内容并转 conflicted 等待人工处理，不以恢复旧备份或 staged 内容覆盖它 — 默认理由: 应用锁无法约束任意外部 writer，安全目标应是绝不静默覆盖而不是虚构跨进程全局原子性
- A-60 2026-07-31 [生效] S-5 首版 verified-handle 支持矩阵收窄为 Windows 10+/Server 2016+ x64、Node x64 与 NTFS/ReFS 本地卷；使用 server-only Koffi FFI 绑定固定 Windows handle API并由 Next 作为 external package 加载。其他 OS/arch/文件系统在 S-5 文件动作前统一失败关闭为 SANDBOX_UNVERIFIABLE，不退回字符串路径检查；POSIX 支持另开切片验证 — 默认理由: 当前交付与验收环境是 Windows x64，仓库没有跨 OS CI 或 native addon 构建基础；未经真实 ABI/race 验证就宣称跨平台安全会制造虚假保证
- A-61 2026-08-01 [已确认] S-6 复核由 owner 针对当前待复核结果显式发起；候选必须是项目当前成员、不是该结果执行者且其角色或技能配置明确具备复核能力，多名候选时由 owner 选择，平台不得暗选；选定 Agent 通过自己的已验证 provider 读取冻结公开材料并输出结构化公开结论与退回/升级/通过三选一，owner、平台和客户端不得伪造 Agent 裁决 — 默认理由: 闭合“真实非执行者 Agent 复核”演示判据，同时维持平等组队和平台不充当隐藏总管 Agent
- A-62 2026-08-01 [已确认] 每个产生业务裁决的 review attempt 恰有一个退回、升级或通过裁决；失败或中断的 provider attempt 不产生裁决且只能由 owner 显式重试为新 attempt。退回或补证使旧 merged execution/result 版本永久只读、work item 进入明确返工状态，并允许同一任务通过新的 execution 产生新的 result 版本；升级终结当前 review attempt 但不终结 result，owner 回答后必须新建 review attempt，若材料变化则先形成新 result 版本 — 默认理由: 不改变 S-5 merged 终态，又让返工、补证和升级后的再次独立复核具有可达且无歧义的事实链
- A-63 2026-08-01 [已确认] work item 的复核有效状态在任一时刻唯一为执行中、待复核、复核中、待返工、待 owner、已通过完成之一；使命唯一为进行中、交付生成中、已完成或 owner 终止之一，所有既有与新增写入口及重放都必须服从同一完成门槛，不能直接把未通过任务或交付未生成使命写成完成 — 默认理由: 使命看板是唯一事实源，完成权不能因旧入口、重放或 UI 路径不同而分叉
- A-64 2026-08-01 [已确认] S-6 自动记忆候选由实际 review Agent 提议并署名，由其三选一裁决确认，平台只负责验证和持久化而不冒充业务作者；每条来源精确绑定同项目的 task、result、review、validation 或 artifact 身份与版本并可导航，既有 owner 创建条目继续保留 owner actor 与原来源语义 — 默认理由: 让五类记忆的作者责任和历史来源可审计，同时兼容 S-3 已存在的 owner 条目
- A-65 2026-08-01 [已确认] S-6 记忆只在同项目内按“类型 + 去除正文首尾空白后的正文（内部 Unicode 字符/code point 原样保留，不做规范等价或大小写折叠）+ source type + source id + source version”全等时复用 active 条目；取代必须显式指向同类型唯一 active 前版，补充内容、跨类型、不同来源版本及历史失效条目都不自动合并或误替代 — 默认理由: 不引入相似度阈值即可为重试、并发和版本历史提供可观察、确定的去重判据
- A-66 2026-08-02 [已确认] S-7 的 `README.md` 使用英文、`README.zh-CN.md` 使用简体中文且信息密度一致，`docs/` 以中文为主；产品图由固定演示数据重新生成并放入受 Git 跟踪的 `docs/images/`，不复制 Clowder AI 资产，也不引用被忽略的 feature evidence — 默认理由: 同时服务国际入口与当前中文用户，并让公开图片可版本化且不暴露验收数据
- A-67 2026-08-06 [已确认] S-8 从 Clowder AI 借鉴清晰的应用外壳、紧凑的信息密度、分层表面、状态语义和主次操作层级，但保留 Cool AI 的温暖浅色、中文桌面协作驾驶舱和几何 Agent 身份；不复制其代码、猫角色、商标、文案或视觉资产 — 默认理由: 用户要求以 Clowder AI 改善 UI，而既有 A-8、D-8 与产品边界要求参考原则而非复刻品牌
- A-68 2026-08-08 [已确认] S-12 的 Thread 是项目内稳定公开会话容器，owner 创建时提供标题，列表按最近公开活动排序；消息上下文只在本线程内续接，Mission、看板和 active 记忆仍为项目共享事实，CollaborationRun 保留独立身份并归属一个线程 — 默认理由: 避免把运行生命周期冒充线程，也避免建立第二套项目事实源
- A-69 2026-08-08 [已确认] S-12 的线程成员策略在创建时显式选择并形成可追溯版本；项目新增成员不自动加入，历史作者名称保持消息快照，成员被移出项目后线程仍可读但必须由 owner 修复策略后才能继续 — 默认理由: “重启后成员策略一致”需要稳定且可观察的语义，静默跟随 live roster 会改写历史上下文
- A-70 2026-08-08 [已确认] S-12 保持同一项目最多一个非终态 CollaborationRun；切换线程不暂停、终止或重放运行，若另一线程仍有非终态运行，新线程可读写 owner 消息但启动 Agent 续接会被明确阻止并提供返回活动线程的入口 — 默认理由: 多线程组织不应在未规划并发运行与队列语义时放宽既有生命周期安全边界
- A-71 2026-08-08 [已确认] S-12 线程深链优先决定当前线程；无深链时重启后选择该项目最近公开活动的可用线程，过程不自动重放任何外部调用 — 默认理由: 提供确定的重启恢复，同时保持本地应用显式重试的既有故障语义
- A-72 2026-08-08 [已确认] “凭据不得进入消息”按可机械判定边界执行：owner 初始目标/后续消息和 Agent 公开输出均拒绝本机已配置 Provider 密钥明文、PEM 私钥块、Authorization Basic/Bearer 值，以及 api-key/token/secret/password 已知字段后的非占位值；`***`、`<redacted>`、`${ENV_NAME}` 和仅字段名的代码示例不拒绝。拒绝内容不得持久化、返回、记录或渲染，错误只给通用修正提示；任意自然语言秘密不作虚假保证 — 默认理由: 既要给出可枚举的正反例，也不能声称模式匹配能识别所有秘密
- A-73 2026-08-08 [已确认] 升级到 S-12 时每个已有项目获得一个保留原消息顺序与 run 来源的默认线程，初始成员策略取迁移时仍有效的完整项目成员集合；历史已移除作者只保留消息快照，不重新加入。有效成员不足两个或存在不可用引用时线程保持可读但必须修复后才能 Agent 续接，重复迁移不得重复线程或策略事件 — 默认理由: 数据迁移不能让既有公开协作历史失去归属，也不能用已移除身份继续运行
- A-74 2026-08-09 [生效] S-13 首批正式 Structured Message Block 仅包含 Proposal、Checklist、Diff Preview、File Reference 与 Handoff Card；任意 HTML/插件、消息分支、回复引用、附件、搜索和标签不在本切片范围 — 默认理由: 这五类足以闭合 S-13 演示结果，allowlist 可把富内容攻击面和后续切片边界保持清晰
- A-75 2026-08-09 [生效] Structured Message Block 是公开 Thread Fact/Message 的版本化不可变正式内容，保留 actor、block/version 与精确 project/thread/run/message/source tuple；可保存可读展示文本快照，但不得伪造来源身份或以 latest 替代冻结来源 — 默认理由: 富内容必须继承 S-12 的事实唯一性与冻结来源，才能在重启、分页和下游导航中保持可审计
- A-76 2026-08-09 [生效] Proposal 与 Checklist 可由 owner 在消息原位决定；每项 Inline Decision 只接受其唯一 allowlist 动作集合和 expected version，重复 operation ID 且 request hash 相同则重放同一 Action Receipt，同 ID 不同 hash 冲突，stale version 要求重读且不重复业务动作 — 默认理由: 原位交互需要便利，但不能形成绕过 operation/version 语义的第二写入口
- A-77 2026-08-09 [生效] Diff Preview 与 File Reference 只投影已存在且已验证的 workspace/execution artifact 来源；S-13 不新增任意宿主文件读取，打开文件继续受 verified-handle、sandbox、approval 和项目 tuple 边界约束 — 默认理由: 消息卡是来源投影而非新文件能力，否则展示功能会静默扩大宿主访问权限
- A-78 2026-08-09 [生效] 涉及高风险或外部副作用的 block 动作只能跳转或发起既有正式审批，卡片点击本身绝不等于批准或执行 — 默认理由: Inline Decision 只裁决该 block 明确允许的低风险业务动作，不能削弱现有审批和执行隔离
- A-79 2026-08-09 [生效] Agent 只能通过 strict structured output 提议 block；平台在单次原子提交前校验类型 allowlist、版本、大小、精确来源、凭据模式和全部可见文本，任一失败都不保存 raw provider/隐藏推理、不生成业务事实，也不降级为可执行卡 — 默认理由: 外部模型输出不可信，必须先完整验证再成为公开正式内容
- A-80 2026-08-09 [生效] Handoff Card 仅是既有公开 handoff fact 的结构化投影，沿用原 actor 与 source tuple，不创建新的 CollaborationRun 生命周期、私语或第二份交棒事实 — 默认理由: 卡片应改善理解与导航，而非改变 S-12 已交付的公开协作和 run 归属
- A-81 2026-08-09 [生效] fact-only transcript 按 block 顺序渲染并覆盖 loading/empty/error/disabled/success/focus、desktop/narrow、键盘、44×44px 与 WCAG AA；未知 block 类型或版本失败关闭为稳定、不可执行且保留来源提示的占位，不猜测内容语义 — 默认理由: 前向兼容不能以误执行或误导 owner 为代价，用户可感知切片也必须完整覆盖现有交互与可访问性契约
- A-82 2026-08-09 [生效] 既有纯文本 Thread Fact 原样继续显示，新 block 不追溯伪造成旧消息内容；迁移、重启和分页必须保持事实顺序、block 版本、source tuple 与已完成 decision/receipt 结果，重复升级不得重复事实 — 默认理由: 扩展消息协议不能改写历史，也不能让同一决定在恢复后出现不同结果
- A-83 2026-08-09 [生效] block 创建、owner Inline Decision、审批跳转及审批结果关联均记录 operation/version/actor/source 与脱敏结果引用；Provider 凭据、raw response、隐藏推理和原始 diff 私密内容不得进入公共审计 — 默认理由: 审计需要证明谁对哪个冻结来源做了什么，同时遵守最小公开内容和既有凭据边界
- A-84 2026-08-09 [生效] S-13 单条 Message 最多含 10 个 Structured Message Block，全部 block 的规范序列化总量最多 256 KiB、单 block 最多 64 KiB、全部可见文本合计最多 20000 字符；标题最多 160、摘要/正文最多 5000 字符。Checklist 最多 50 项且单项最多 500 字符；Diff Preview 最多引用 100 个文件且脱敏展示文本最多 20000 字符；Proposal 的直接 Inline Decision 固定为 accept/reject，Checklist 固定为 check_item/uncheck_item 且每次只指向一个既有项，均不接受自由文本或额外参数；operation ID 最多 128 字符且一次决定请求最多 16 KiB — 默认理由: 复用既有公开 Agent 文本与 handoff 边界，并给复杂清单和多文件预览留出实用空间，同时让最坏体积、动作集合与拒绝测试可机械判定
- A-85 2026-08-09 [生效] S-13 规格测试采用五个最高层公共缝：strict block schema + tuple-scoped domain/API、Agent turn 原子提交、fact-only UI、真实浏览器 + axe、migration/reopen；测试只断言外部可观察行为，不依赖私有函数、表结构或具体组件文件 — 默认理由: 这五条缝分别覆盖信任入口、事实提交、owner 体验、真实可访问性与持久兼容，已是完整风险面下可保持的最少独立层次
- A-86 2026-08-09 [生效] S-13 将版本拆为互不替代的字段：blockSchemaVersion 只决定 block 结构能否解码；blockRevision 标识同一逻辑 block 的不可变内容/来源快照；stateVersion 从 1 开始且只在成功 Inline Decision 后递增，expectedStateVersion 只比较它；sourceEntityVersion 冻结外部来源版本。Decision 记录 blockRevision、fromStateVersion、toStateVersion 和 decisionSchemaVersion，Action Receipt 记录 receiptSchemaVersion、同一组状态版本、Decision 身份、operation ID 与 request hash；UI 与审计不得再使用无类型的 `version` — 默认理由: schema 兼容、内容修订、可变决定状态和外部来源是四种不同并发轴，混称 version 无法机械判断 stale 或恢复结果
- A-87 2026-08-09 [生效] Inline Decision 是同步单事务写入，不新增或持久化长租约；其审计明确记录 leaseApplicability=not_applicable、leaseId=null。Agent block 提交只沿用既有 Collaboration turn/attempt lease 并在适用时引用该既有 lease，S-13 不创建 block lease；Approval 与外部动作继续使用其既有 lease 并只通过脱敏结果关联 — 默认理由: 低风险单事务状态转换没有跨进程占有期，引入新 lease 只会制造失效/接管状态，而高风险和长时动作已有自己的租约边界
- A-88 2026-08-09 [生效] Inline Decision 首次成功原子持久化 completed operation、Decision、新 stateVersion、业务 Action Receipt 与公开事实；相同 operation ID + hash 只重放原 Receipt。相同 ID + 不同 hash 返回 OPERATION_CONFLICT 且不改变原 operation；stale expectedStateVersion 原子记录该 operation/hash 的 sanitized terminal VERSION_CONFLICT 结果但不产生 Decision、业务 Receipt、stateVersion 或 Thread Fact，同 hash 重放同一冲突 envelope，重读后须使用新 operation ID。同步事务不持久化 pending；客户端 unknown-write 必须查询 operation，completed 重放 Receipt、version-conflict 重放冲突 envelope、not-found 返回 OPERATION_NOT_FOUND 并只允许原 ID + 原 hash 重试 — 默认理由: 把业务成功回执与冲突 envelope 分开，既保持成功幂等，也让 stale、hash 冲突和超时恢复没有重复动作或状态漂移
- A-89 2026-08-09 [生效] S-13 所有文本上限按未规范化原字符串的 Unicode grapheme cluster 计数，统一使用 `Intl.Segmenter("zh-CN",{granularity:"grapheme"})`；所有 KiB 上限按 1 KiB=1024 bytes、严格 schema 通过后的 canonical JSON UTF-8（无 BOM、无多余空白、对象键递归按码点排序、数组保序）计量。完整 S-13 blocks envelope（含 schema/actor/source/metadata/全部 blocks）最多 256 KiB、单 block 最多 64 KiB、block 数最多 10；Proposal actions 恰为 2、Checklist actions 恰为 2、Checklist items 最多 50、Diff file references 最多 100，其余未声明数组禁止。Decision request 的完整 canonical envelope 最多 16 KiB；request hash 覆盖除 operation ID/hash 字段外的完整决定意图与 tuple、blockRevision、expectedStateVersion、action/item 身份 — 默认理由: grapheme 与 canonical UTF-8 bytes 分别对应 owner 可见长度和传输/持久化体积，并使嵌套数组、边界 ±1 与重放 hash 得到唯一可复现结果
- A-90 2026-08-09 [生效] Inline Decision 审计按 operation 终态区分：只有 completed success 的同 hash replay 关联原业务 Action Receipt 且不产生第二业务动作；version-conflict 同 hash replay 关联同一 sanitized terminal conflict envelope，始终为零 Action Receipt、零 Decision/Thread Fact、零第二业务动作；OPERATION_CONFLICT 不新建审计业务结果也不改变原 operation；客户端 pending 仅是本地暂态，unknown-write 继续按 completed/version-conflict/not-found 对账 — 默认理由: Receipt 是成功业务回执，不能被冲突终态冒充，否则审计验收会与幂等状态机相互矛盾
- A-91 2026-08-09 [生效] S-13 canonical JSON 统一采用 RFC 8785 JSON Canonicalization Scheme（JCS），输入先满足 RFC 7493 I-JSON 与 strict domain schema：wire 仅接受 UTF-8、无 BOM；对象键排序、数组顺序、字符串转义和 ECMAScript 数字序列化完全按 JCS，不另造算法；拒绝 NaN、Infinity、负零、lone surrogate 和非法 Unicode。Inline Decision raw HTTP wire body（含空白与原始转义）上限为 32 KiB，在解码前按实际 bytes 检查；通过 schema 后的 `decisionIntent` JCS UTF-8 canonical bytes 上限为 16 KiB，排除 operation ID 与 hash 字段，大小校验和 request hash 必须复用这一份完全相同的 byte sequence。单 block 64 KiB 与 blocks envelope 256 KiB 也按各自 JCS bytes 计量，不能用 wire 大小替代 canonical domain 大小 — 默认理由: 标准 JCS/I-JSON 消除等价 JSON 在键序、转义、数字和 Unicode 上的 byte/hash 分歧，同时 raw transport 与受信领域对象需要独立防护上限
- A-92 2026-08-09 [生效] S-13 拆为 16 张适合单一上下文窗口的任务票，每票只承诺一个已批准公共 Seam 与一个可独立运行的行为判据；依赖图先串行完成 fact writer 收敛、共享 codec、v8 schema validator 与 7→8 migration，再以最薄 Proposal persist/read 路径分叉到 Agent commit、来源投影、HTTP、decision、read model 与 UI，最后由恢复和真实 browser/axe/full regression 汇合 — 默认理由: 显式先决条件避免新旧 writer/schema 并存，同时让其余工作保持纵向、可验证并允许无共享写边界的前沿并行
- A-93 2026-08-09 [生效] 两个架构允许的非纵向波次采用受控 expand-contract：T-01 先扩展统一 Thread Fact Store、迁移全部既有 producer 并以仓库约束证明除 migration 外无 direct fact SQL；T-03/T-04 分别建立单一 v8 manifest/exact validator 与原子 7→8 shadow/rebuild/copy/validate migration，只有 contract 与旧行为验证通过后才允许新增 block/decision fact — 默认理由: 这些变化横跨现有写者与持久契约，强行混入功能票会使行为保持、回滚边界和 v7 兼容无法独立证明
- A-94 2026-08-09 [生效] S-13 第 3 轮 code review 的连续未通过项拆成 017 结构化消息完整性与 018 stale decision 对账 UI 两个 review-remediation vertical sub-slices；015 不再局部补丁，只有两片均 done 后才进行第 4 轮独立 code review — 默认理由: 后端持久事实完整性与入站 UI 对账是可独立交付的用户结果，继续捆绑会违反连续评审失控保护与切片规模护栏
- A-95 2026-08-09 [生效] 017 使用 `openDatabase(databasePath)` 与 Structured Message Source Public Read 两条公共 seam、拆为 5 张票；File Reference 在提交时冻结脱敏且按 grapheme 限长的公开名称和明确 source version，读取不得查询 mutable name/latest；SQLite busy-timeout 诊断限 15 分钟、最多 10 次量化复现 — 默认理由: 两条 seam 共同证明同一个“reopen 后仍冻结、完整、无泄漏”结果，并给已知不稳定失败明确停止条件
- A-96 2026-08-09 [生效] 018 使用 fact-only Structured Block Public UI 单一 seam、拆为 4 张票；VERSION_CONFLICT 后先禁用旧动作并读取完整最新 Proposal/Checklist state，只有 owner 基于最新事实显式选择时才用新 operation 与最新 expected version 重试，任何读取或渲染都不自动重放 — 默认理由: stale 决策必须服从服务端事实且保留 owner 意图，客户端自动重放会把旧决定施加到新状态
- A-97 2026-08-09 [生效] 018 只从 `D:\clowder-ai` 转译稳定消息壳、服务端事实收敛、文本型类型/状态标签与窄屏既有模型四项信息架构/交互原则；继续复用 Cool tokens/components，不复制品牌、资产、文案、源码、DOM/CSS 结构或调色盘 — 默认理由: 强参考交互原则可以提高信息清晰度，同时保持 Cool AI 的产品身份与实现独立性
- A-98 2026-08-09 [生效] 019 架构收敛特性用户可感知=否（纯结构调整、行为保持）；浏览器 smoke 不作为 demo-acceptance 门禁，但作为 ADR-0004 完成判据在 T-16 照常执行 — 默认理由: 门禁的 demo-acceptance 针对用户可感知切片，机械迁移没有可演示新结果，而 ADR-0004 的验收要求不变
- A-99 2026-08-09 [生效] 事务协调 Port（unit-of-work/transaction-context）归 `src/application/` 应用层接缝，其 SQLite 实现归 `src/adapters/outbound/sqlite/`；不放入 src/shared（其类型包裹 DatabaseSync 技术类型） — 默认理由: 事务协调是 Workflow 的应用层依赖而非无 owner 基础类型，目标树第 7 节未为其单列目录
- A-100 2026-08-09 [生效] 入站 HTTP 共享助手（JSON 读取、错误 envelope 映射）归 `app/api/_shared/`（Next 下划线目录不参与路由）；不新建业务目录、不进 src/adapters/inbound（该目录留给非 Next 入站） — 默认理由: HTTP 传输细节属于 app/ 入站 Adapter 内部组织
- A-101 2026-08-09 [生效] provider-verifier 与 openai-chat-client 归 `src/adapters/outbound/model-runtime/`；runtime 领域 Module 当前无自有事实表（CAP-RUN-02+ 规划中），不建空壳 Module — 默认理由: 目标架构禁止为未实现能力创建空壳
- A-102 2026-08-09 [生效] 冻结基线中 127 个 Windows-only 测试失败为环境性（A-60 设计失败关闭 SANDBOX_UNVERIFIABLE），不计入收敛回归判据；收敛判据为"全量测试不比冻结基线差" — 默认理由: 本机 Linux 无法验证 Windows verified-handle 路径，环境性失败与迁移质量无关
- A-103 2026-08-09 [生效] T-04～T-12 迁移期允许 route 暂直接引用 Adapter 构造的过渡口，T-14 统一收编 `src/composition/` 后删除；过渡口逐波登记在 progress.md — 默认理由: 分波迁移需要可构建中间态，过渡口有明确删除波次、不构成长期兼容层
- A-104 2026-08-09 [生效] 领域→领域 import 边在 T-04～T-12 以"改经对方公开 Interface"为过渡形态，T-13 提取命名 Workflow 后消除并转架构阻断 — 默认理由: 跨域命令边的正确消除依赖 Workflow 层先就位
- A-105 2026-08-09 [生效] 产品架构刷新（2026-08-09）的编辑性默认：第 9 节迁移计划压缩为完成记录（波次细节以 `features/019-architecture-convergence/progress.md` 与 git 历史为准）；新增关键场景与横切约定追加为第 11、12 节而非插入重排，以保持 ADR-0004 等对外部节号引用稳定；文档标题由"目标架构与迁移地图"改为"产品架构" — 默认理由: 收敛完成后迁移地图已成历史，保留节号避免既有决策依据失效
- A-106 2026-08-10 [生效] 产品架构补全（2026-08-10）的编辑性默认：第 13 节架构特征与第 14 节演进与适应度追加于文末而非插入重排（延续 A-105 节号稳定理由，§1 已加阅读指引）；驱动性特征取 5 个（AC-1～AC-5），可用性、可访问性与单进程部署简单性作为普遍底线不列为驱动力；质量属性场景度量只采用已实测或已确认数据（特性 020 全量 135.4 s、既有安全/恢复测试判据），未实测阈值不虚构；文档超过技能 ≤120 行指引，因所有权清单、证据索引与目录树已被 ADR-0004 等外部引用而保留，是否压缩瘦身留待用户决定 — 默认理由: 补全缺失的驱动力与适应度两环，同时不破坏已确认内容与外部引用
- A-107 2026-08-10 [生效] 4+1 视图按 hf-to-product-architecture 默认裁剪合并：逻辑视图=第 2～3 节、开发视图=第 7 节、场景视图=第 11 节，并在节标题显式标注；过程视图不单独成节，并发硬约束（单项目唯一非终态 Run、并行执行上限、租约、单写 SQLite）以协议与不变量形式落在第 3、4 节；物理视图省略，因本地优先单进程单 SQLite 部署拓扑不构成硬约束，未来桌面打包或多进程 Runtime 宿主构成硬约束时按技能规则补视图并记 ADR — 默认理由: 技能明确默认裁剪为三块，显式标注让视图归属可见，同时不为非硬约束预付视图维护成本
- A-108 2026-08-10 [生效] 017 T-01 File Reference 冻结 publicName 的 grapheme 上限取 160（与 block title 一致），超限 fail-closed 返回 INVALID_SCHEMA 而非静默截断。默认理由: 与既有 title 限长一致、fail-closed 符合本片完整性基调。
- A-109 2026-08-10 [生效] 022 T-01 回复快照默认：schema identity 9→10（四可空列+自引用 FK 纵深防御）；敏感/超长 excerpt 与宿主路径文本统一 fail-closed 为 CREDENTIAL_CONTENT_REJECTED 422（017 assertPublicProjectionText 先例）；命令缝无法构造真自引用（id 服务端生成），自引用拒绝由 T-02 reopen 全集校验覆盖；excerpt 取目标 content trim 后值。默认理由: 与 017 冻结/脱敏先例一致，纵深防御不替代全集校验。
- A-110 2026-08-10 [生效] 022 T-02 回复边校验默认：用 JS 校验器比对 excerpt（SQLite trim 只去 ASCII 空格，与 JS trim 语义不同会误杀合法 agent 消息）；replyTo 为 ThreadMessageDto 恒在字段（非回复显式 null），读取侧只解码冻结列不回查，损坏行按 404 纵深裁掉；自引用除 sequence 严格小于规则外仍显式检查。
- A-111 2026-08-10 [生效] 022 T-03 Transcript UI 默认：跳转锚点复用既有 messageRefs（messageId→li）+ focus() 约定，不新增 DOM id；跨页定位复用 facts 分页缝顺序加载至目标出现或耗尽（oldest-first 连续分页下回复目标恒更旧，"目标在未加载页"实际不可达，加载尝试按规格保留为契约行为），不引入第二消息状态机；来源不可用=已穷尽分页且目标不在已加载 transcript，占位为 aria-disabled button+说明文本、不渲染冻结快照作者/摘要（防伪造）；短暂高亮 1600ms（tokens 的 --interactive-soft + --focus-ring）；加载失败复用 factsError 既有 alert 面且引用片恢复可重试。
- A-112 2026-08-10 [生效] 022 T-04 真实浏览器验收默认：不可用来源占位在 smoke 经 facts 响应路由拦截构造（持久层不可构造——thread_fact_no_delete 触发器强制事实不可变，smoke 实测 IMMUTABLE_THREAD_FACT fail-closed，且消息↔事实 1:1 不变量阻止合法缺席）；验收锁定并修复真实缺陷——jumpToReplyTarget 对在途 facts 请求一律早退，后台轮询窗口内点击已加载目标的引用片被静默吞掉，已加载定位路径改为不受在途守卫（纯本地定位无请求交错），jsdom 回归用例锁定；smoke schema identity 断言随 T-01 同步为 10；既有 returnLink focus+Enter 步骤遇轮询重渲染焦点竞争，加一次性重试保持键盘语义。
- A-113 2026-08-10 [生效] 023 T-01 草稿默认：content 原样保存不 trim、上限 10000 grapheme；敏感正文落 content:"" 且 contentSaved:false（占位/链接仍存）；附件占位严格 {name 1-255 grapheme, size 非负 safe integer} 最多 8 个；draft reply_to 不加 FK（命令即时校验+reopen 不变量兜底）；clear 幂等、read 缺失 null、tuple 不存在 404；路由 body 64KiB。
- A-114 2026-08-10 [生效] 023 T-02 输入历史默认：identity 11→12 新增 input_history_entries 与 input_history_clear_events（只记清除事件本身无内容列）；历史正文与消息正文逐字一致（同 trim）；startThreadRun 初始消息同记历史并清草稿；搜索 LIKE+ESCAPE、空 query 列举、上限 100 条、最新优先；重放不二次记录也不二次清草稿。
- A-115 2026-08-10 [生效] 023 T-03 Composer 默认：草稿 effect 按完整 targetKey（project|thread|run）键控；POST /runs 不携带 replyToMessageId（启动新轮+回复链接给字段级中性错误，run-start 回复支持留后续）；草稿读写失败用 muted 提示非 alert；全空（无文字/占位/回复）即显式 DELETE；附件 chips 文案 {name} · {size} B。
- A-116 2026-08-10 [生效] 023 T-04 历史 UI 默认：记录开关=发送请求显式 recordInputHistory:false（入 requestHash，翻转判 409），偏好 key cool-ai:input-history-recording:v1 缺省开启；回填=替换 composer 并聚焦；搜索提交式；清除为面板内两段式确认；关闭开关仅停记新历史，既有仍可搜索/清除。另修复 T-02 遗留：owner-decision-races/owner-handoff-plan-races 假 cipher fixture 升级为真实 vault 加密信封。
- A-117 2026-08-10 [生效] 024 T-01 上传默认：文件名经 query `name` 传递（URLSearchParams 天然 unicode 安全，避开 header latin-1 限制），路由解析+服务复核双重校验，字段词汇沿用 draft 的 required/invalid_format/unknown 族；非 image/* Content-Type 415 UNSUPPORTED_MEDIA_TYPE，magic 嗅探失败/空 body 400 INVALID_INPUT（file: unsupported_type/required），magic bytes 为唯一类型事实；attachments 根显式注入——路由取 COCKPIT_ATTACHMENTS_ROOT（须绝对路径）或派生 databasePath 同目录 .data/attachments，memory DB 且无 override 时 STORAGE_UNAVAILABLE fail-closed；上传事务内写盘，失败回滚并清理残件、行不残留；removeAttachment 事务内写 removed 审计并删行后再删字节，删失败整体回滚、行状态保留；attachment_events 不随附件 FK（审计保留）；同线程同 sha256 复用既有附件（200 不重复写盘/写行）；message_id 为可空复合 FK 指向 collaboration_messages（完整附件边校验在 T-03）；附件 DTO 不回传 storage_relpath，宿主路径零回显；另移除一份未跟踪且从未编译通过的废弃草稿测试。
- A-118 2026-08-10 [生效] 024 T-02 消息链接与投递默认：attachmentIds 可选 ≤4、严格形状（字符串数组、RESOURCE_ID、去重），字段词汇 invalid_format/too_many_items/not_found/already_linked 全部 400 INVALID_INPUT，缺省与空数组归一后入 requestHash（同 operation 不同附件集判 OPERATION_CONFLICT）；startThreadRun 不携带 attachmentIds，按 A-115 先例给字段级中性 unknown 错误；ThreadMessageDto.attachments 恒在（无附件空数组），引用恰为 {id,fileName,size,mimeType}，顺序取行插入序（上传序）使写响应与全部读取路径一致；链接在消息提交事务内完成（先于消息行插入，fault 全回滚），claim 用守卫 UPDATE（status='uploaded' AND message_id IS NULL）防双链接竞态，linked 审计事件同事务；投递路由无 Content-Disposition，linked 但字节缺失按 503 STORAGE_UNAVAILABLE 脱敏而非 404；UI 侧两处严格校验器（onboarding-guide-machine MESSAGE_KEYS 与 collaboration-panel mutationIds 键集）随恒在字段同步，guide machine 对附件 ref 形状逐一校验

- A-119 2026-08-10 [生效] 024 T-03 reopen 附件边校验默认：status 机列组合（linked⇔message_id+linked_at 非空）虽已由表 CHECK 在写入期钉死、磁盘不可表示，仍在 current-data-invariants 冗余登记一条异或双向校验作为防御纵深与意图文档；linked_at 语义矛盾定义为早于 created_at（同 GLOB 格式 ISO 串字典序可比）；storage_relpath 采用精确等于 project_id||'/'||id 的最强局部形状（单写者事实源），一条等式覆盖相对性、无 ..、无反斜杠、无绝对路径；'双向一致'按消息 DTO 自附件行单向派生的架构以附件行为唯一事实源——同一附件不被两消息链接由 message_id 单列 + PK 结构性保证，linked 指向同 tuple 既有消息由 reopen 期 PRAGMA foreign_key_check + 复合 tuple FK 既存拦截（T-01 A-117 落点），本轮不重复造查询；附件 tuple 漂移进另一既有 thread 以 attachment_events↔现存附件行 tuple 一致性校验捕获（removed 附件无行不触发，审计保留语义不破）；事件边不新增对附件行的存在性强约束（removed 后事件合法留存）

- A-120 2026-08-10 [生效] 024 T-04 Composer/Transcript UI 默认：草稿附件形状为 2 键占位 {name,size} 或 3 键引用 {attachmentId,name,size}（RESOURCE_ID 形状校验），服务端草稿缝不核对 attachmentId 存在性（存在性由发送命令兜底，避免 debounced save 与 DELETE 竞争产生伪失败）；composer 上限 8→4 与服务端消息命令一致，类型白名单/5 MiB/数量在客户端先行字段级中性错误（服务端仍兜底）；发送门禁选择禁用发送按钮+muted 提示而非静默剥离（上传中/失败/需重选占位一律阻塞，handleSubmit 字段级错误双保险）；run-start 携带附件沿 A-115/A-118 给中性字段错误；同内容重复选择在上传成功后按服务端 hash 复用返回的同 attachmentId 折叠为单 chip；消息附件渲染自 projectMessagesPage（thread /messages 全页加载恒在附件）按 messageId 查表，图片无自定义 loading/error 占位（原生 alt 兜底不伪造内容），样式全 tokens；移除 uploaded chip 先 DELETE 成功才本地移除（失败中性报错不静默），uploading 移除即 abort；混合剪贴板含 image/* 时接管 paste 仅上传图片；ProjectMessage 类型补恒在 attachments 字段（guide machine 校验器 T-02 已强制），run-service 遗留 projectMessages 页补真实附件投影（thread_id 仅内部解构不外泄），insertOwnerMessage 显式 attachments: []；chip 状态经 setChips 同步写 ref 避免 effect 镜像竞态。

- A-121 2026-08-10 [生效] 025 T-01 收藏命令默认：identity 13→14 新增 thread_favorites（PK (project_id,thread_id)、复合 FK→collaboration_threads ON DELETE CASCADE、created_at GLOB 校验）；setThreadFavorite 为幂等偏好类事实（A-113 draft 先例：无 operation receipt、无 version），ON CONFLICT DO NOTHING 使重复 true 保留原 created_at（收藏视图排序稳定），false 对不存在行亦成功；跨 tuple 与线程不存在统一 RESOURCE_NOT_FOUND 404（同 draft）；写入路由取最少面 PUT 单端点 body 严格 {favorite:boolean}（非 POST/DELETE 双端点），列表 GET 增加 favorites=true|false 单值参数。
- A-122 2026-08-10 [生效] 025 T-01 列表投影默认：收藏视图 favoritesOnly 拒绝 cursor（字段级 not_supported）且 nextCursor 恒 null——既有游标按 last_activity_sequence 编码无法寻址 favorited_at 序，fail-closed 优于错页；收藏排序 favorites.created_at DESC, thread_id ASC 稳定决胜，普通列表 SQL 形状与顺序完全不变；ThreadListItemDto/ThreadListResponseDto 下沉 shared/collaboration-contracts（恒在 isFavorite/favoritedAt），detail 用 ThreadSummary 不扩展以避开 THREAD_KEYS 等严格白名单（guide machine）破坏；组件侧以 threadListItemSchema 扩展 threadSummarySchema 承接新字段。
- A-123 2026-08-10 [生效] 025 T-02 视图切换默认：收藏视图入口复用现有 role=tablist/tab 分段控件范式（全部/已收藏，方向键+漫游 tabIndex 同 team-panel resource-tabs）；视图切换不触碰 URL 线程选择——当前选中线程不在收藏视图内时不报“无效选择”、不自动跳转，消息区保持现状（选择校验 effect 仅作用于“全部”视图）；创建线程入口只在“全部”视图渲染（收藏视图不新建，避免未收藏新线程混入过滤列表）；空收藏态给文案+“查看全部线程”回退按钮；收藏视图内取消收藏至空时派生 empty 渲染，不改写 listState（“empty” 语义保留给“项目无线程”，TaskPanel 空态提示不受影响）。
- A-124 2026-08-10 [生效] 025 T-02 星标交互默认：星标为列表项内独立 button（SVG currentColor 遵循 activity-bar 图标范式，非 emoji），aria-pressed + 可访问名称“收藏线程 X/取消收藏 X”，min-width/height 取 --control-min(44px)，原生 button 得 Enter/Space 与全局 focus-visible；乐观更新以本地 ISO 时间为 favoritedAt 占位、成功以服务端响应严格校验（tuple 复核）后对账，失败回滚原态并以 inline role=alert 展示脱敏文案（apiErrorCopy 映射，兜底“无法更新收藏状态，请重试。”）；请求期间仅该线程星标 disabled（pending 集合按 threadId 键控）；收藏视图内取消收藏即乐观移除、失败按 favorited_at DESC, thread_id ASC 插回；收藏页断言 nextCursor 恒 null 且全部 isFavorite（fail-closed，与 A-122 一致）。
- A-125 2026-08-10 [生效] 025 T-03 浏览器验收默认：favorites 验收段置于 smoke 附件段之后并复用既有 restartApp 环节做重启保持验证（DB 行 deepEqual + API favoritedAt 逐字一致 + UI pressed/视图列出三层断言）；smoke axe 助手在 results.json 补记全部 violation id/impact 明细（原仅 blocking/contrast），门禁仍只判 serious/critical 与 color-contrast；既有松散 getByRole 子串选择器（线程标题）被星标可访问名称命中，统一加 exact:true 修复（测试选择器缺口，非生产缺陷）；narrow 抽屉 axe 报 1 条 aria-allowed-role minor（既有 aside role=dialog 抽屉模式，025 范围外，本片不修）。默认理由: 验收段内聚单块便于维护且重启语义与既有环节一致；证据明细增强后续审计；minor 项低于门禁且属既有抽屉模式，不在收藏切片扩张修复。
- A-126 2026-08-10 [生效] 026 T-01 依赖读模型默认：查询签名 `getMissionDependencyInsight(databasePath, projectId, missionId)` 对齐模块既有 queries 风格（databasePath 在前）；悬空依赖用节点独立 `missingDependencyIds` 字段表示（不在 blockedByIds 内放 missing 标记、不伪造节点），blockedReason 随之派生“N 项前置依赖缺失”；自环与悬空依赖在持久层不可表示（CHECK(work_item_id<>depends_on_id)、FK ON + reopen foreign_key_check 双重 fail-closed），读模型在导出的纯派生函数 `deriveMissionDependencyInsight` 防御性处理，自环/悬空/done 依赖三例经纯函数缝覆盖（done 行需完整 review 链才过 reopen 数据不变量，避免在测试中复制大型 SQL 图）；blockedReason 复用既有状态中文词汇（待办/进行中/阻塞），格式“前置依赖未完成：待办 1 项、阻塞 1 项”，全 done 且无缺失为 null；hasDependencies=已解析边或缺失依赖行存在；循环检测 Tarjan（节点与邻接均按 id 码元序），cycleId 按 SCC 完成序 cycle-N，path 为自最小成员 id 的确定性 DFS 见证路径（标题 “ → ” 连接）。默认理由: 读模型如实派生不伪造；DB 不可表示情形放纯函数缝既保覆盖又遵守不变量 fail-closed。
- A-127 2026-08-10 [生效] 026 T-01 路由默认：落点新建 tuple 嵌套路径 `GET /api/projects/[projectId]/missions/[missionId]/dependencies`（现有 projects/[projectId]/mission 为单数读态、missions/[missionId] 不带 projectId，均非 tuple 路径）；校验=projectId/missionId trim 后非空且 ≤200 字符 + 拒绝一切 query 参数（review-events 白名单先例），错误词汇沿用 MissionError INVALID_INPUT/PROJECT_NOT_FOUND/MISSION_NOT_FOUND + missionApiError 脱敏 envelope；装配根登记命名空间 `missionWorkDependencyInsight`。默认理由: tuple 路径语义最清晰且与既有 GET 路由校验/错误映射惯例一致。
- A-128 2026-08-10 [生效] 026 T-02 依赖视图默认：挂载点为 MissionBoard 使命看板网格之后、MissionDeliverySurface 之前的只读"依赖全景"区（非独立 tab——看板即 Mission 详情面，新增区最少惊讶）；列表化呈现复用既有 `task-summary` 卡片与 `.status-label` 徽章范式（WorkItemStatus 映射既有变体 todo→queued、in_progress→running、blocked→failed、done→completed，不新增颜色）；循环标注双呈——循环组 role=group 列出"循环依赖 cycle-N：可读路径"（error-text/danger 表缺陷信号），节点标题旁附"循环 cycle-N"徽章；节点导航复用看板既有 focusWorkItemId 焦点缝（聚焦任务卡 h4 并 scrollIntoView({block:"nearest"})），不引入新路由/新详情视图；节点激活入口为原生 button"定位任务 X"（44px 由全局 button min-height var(--control-min) 保证，Enter/Space 原生可达）；target switch 防陈旧用 effect active epoch 标志（对齐 MissionBoard 自身数据获取模式，AbortController 非既有惯例故不引入）；任务写操作后经 refreshSignal=workItems 引用变化触发只读模型重取，保持与事实源一致；同波次更新既有夹具（mission-board.test ×3、onboarding ×5）补 /dependencies 空 insight 响应。默认理由: 复用既有接缝与徽章词汇零新视觉语言；epoch 标志与看板一致避免双范式；refreshSignal 派生自重取比显式失效回调更不易漏。
- A-129 2026-08-10 [生效] 026 T-03 浏览器验收默认：验收段落点 smoke:context（既有 mission 面板 smoke），造数在既有 Plan→Build 链上补 Test(←Plan)、Ship(←Build,Test) 成链+菱形，不强行造环——DEPENDENCY_CYCLE 写守卫使环在持久层合法路径不可达，循环呈现由组件测试覆盖；empty 态用第二项目隔离造数（零任务 Mission），不污染主项目事实；对齐统一完成门槛（特性 006，9ceecb2）——看板"完成任务"要求前置评审通过，既有"完成 Plan→启动 Build"序列已不可达，smoke 保留依赖守卫拦截（前置依赖尚未完成）与无依赖任务正向启动两个看板级控制，评审通过路径由 review smoke 与模块测试覆盖；发现 MissionBoard apiMessage 对 REVIEW_REQUIRED 无专属文案（落兜底"操作失败，请稍后重试。"），记为既有 UX 缺口，不在本片扩张修复；修复 smoke 预存腐化五处（测试选择器/结构缺口，非生产缺陷）：getByLabel("模型服务") 与 getByRole("创建任务") 加 exact:true（settings 导航按钮与 legacy"开始创建任务"空态按钮子串碰撞）、窄屏编辑抽屉先点"看板"tab（95dfd78 移动 tabs 后默认 chat 面）、前三 Tab 位次断言改有界 Tab 遍历（activity bar 设置/主题控件先入 Tab 序）、axe 需 browser.newContext() 页面（原 browser.newPage 被 AxeBuilder 拒绝）。默认理由: smoke 必须反映当前产品真相而非钉死历史行为；腐化修复保持既有验收意图（守卫、键盘可达、抽屉焦点）不变。
- A-130 2026-08-10 [生效] 027 T-01 只读浏览查询默认：查询签名 `listWorkspaceDirectory(databasePath, projectId, relativePath)` / `readWorkspaceFilePreview(databasePath, projectId, relativePath)` 对齐模块 queries 风格（databasePath 在前），实现层由路由侧注入 verified fileAdapter 作为第四参（advance 路由先例），模块公开 Interface 保持三参形态；verified 复用方式=新建 `workspace-browse-adapter`，基于既有 `SandboxFileHandleAdapter` 缝（openRootDirectory/list/openChildNoFollow/identity/currentIdentity/readFromHandle）实现浏览遍历——file-tools 的 listVerifiedDirectory/readVerifiedFile 为 sandbox 工具语义（1MiB 硬上限、严格 UTF-8 only、保留名逐条拒绝、根路径 "." 不可列）不适用浏览，故自实现但保留同等 verified 纪律：根稳定校验、list→open 身份一致、finalPath 必须仍在绑定根内、reparse 一律 fail-closed；敏感词汇最小集冻结于 Adapter 一处：前缀 .env*、id_rsa/id_dsa/id_ecdsa/id_ed25519，扩展名 .pem/.key/.p12/.pfx，子串 credential/secret（NFC 后大小写不敏感判定），命中任一路径片段即遮蔽且预览不做任何文件系统访问（存在性亦不探测，mask-first）；分类=magic bytes（png/jpeg/gif/webp）优先，其后严格 UTF-8 解码（截断时最多回退 3 字节处理切半字符），解码失败或含 NUL 即 binary-unsupported；文本 >512KiB 截断 truncated=true，lineCount 按返回内容计（空内容 0 行、末尾无换行 +1）；图片择一采用 dataUrl 内联（≤2MiB），超限 WORKSPACE_FILE_TOO_LARGE 拒绝，不建字节路由；单次 verified 读上限=图片内联上限 2MiB；列举排序=目录优先+名称小写码元序+原名码元序决胜（确定性、不依赖 locale）；含 reparse 条目的目录列举整体 fail-closed（WORKSPACE_PATH_REJECTED），与 sandbox 列举语义一致；列举上限 1000 条；新增错误词汇 WORKSPACE_NOT_BOUND/WORKSPACE_ENTRY_NOT_FOUND/WORKSPACE_PATH_REJECTED/WORKSPACE_NOT_PREVIEWABLE/WORKSPACE_FILE_TOO_LARGE/WORKSPACE_BROWSE_UNAVAILABLE，全部静态文案零宿主路径。默认理由: verified 缝复用保证不裸 fs 直读绑定根；mask-first 使敏感路径零探测零泄漏；dataUrl 内联少一个字节路由面且 ≤2MiB 本地可接受。
- A-131 2026-08-10 [生效] 027 T-01 路由默认：落点 `GET /api/projects/[projectId]/workspace/files?path=`（列表）与 `GET /api/projects/[projectId]/workspace/file?path=`（预览 JSON），最少面挂在既有 workspace 路由族下；校验=path 必填、单值、非空、≤4096 字符、无 NUL、拒绝一切其他 query 键（attachments 白名单先例）；传输层错误文案 "Workspace browse input is invalid."，领域路径形态错误由模块抛 INVALID_INPUT 文案 "Workspace path is invalid."（两层共用 fields 词汇 required/invalid_format/unknown）；错误映射 PROJECT_NOT_FOUND/WORKSPACE_NOT_BOUND/WORKSPACE_ENTRY_NOT_FOUND→404、WORKSPACE_FILE_TOO_LARGE→413、WORKSPACE_BROWSE_UNAVAILABLE→503、WORKSPACE_PATH_REJECTED→422、其余→400，全部响应（含错误）带 cache-control: no-store；传输助手落 `app/api/_shared/workspace-browse-api.ts`（attachments 模式），verified 适配器由助手经 `src/composition/execution-host` 创建注入（advance 先例，index barrel 保持无 server-only 闭包）；装配根登记命名空间 `workspaceBrowseService`；sqlite→workspace 跨技术读在 imports.test 登记过渡边（review-delivery→process-runner 同类先例）。默认理由: 与既有 GET 路由校验/错误映射/装配惯例逐条对齐，无新范式。
- A-132 2026-08-10 [生效] 027 T-02 目录树 UI 默认：挂载点=WorkspaceSetup 工作区面板内新增"工作区文件"区（非独立 tab——面板即工作区详情面，区段化最少惊讶，对齐 026 T-02 挂载判据），未绑定时该区显示"绑定工作区后即可浏览文件。"与空目录"该目录为空。"区分文案；树为扁平渲染 role=tree/treeitem + aria-level 表达层级（VS Code 式扁平树惯例，不嵌套 role=group），roving tabindex + 方向键/Home/End + Enter 选择，目录 Enter/点击=展开收起，文件 Enter/点击=选中并回调 `onFileSelect(path)`（预览区本体留 T-03，本票以 WorkspaceSetup 的 role=status"已选中文件"占位行承接回调缝）；图标=内联 SVG（activity-bar 现行范式，aria-hidden，勿 emoji），敏感徽章复用 `.status-label status-queued`（026 徽章复用先例，不新增颜色）；新 CSS 类 `.workspace-tree-*` 全部走 tokens 并有 CSS 契约断言（context-accessibility 先例）；前端缓存已加载目录不做失效广播，刷新靠 target/绑定路径变化（WorkspaceSetup 以 `key={projectId:path}` 重挂载）与手动"重试"入口，未做独立"刷新"按钮（保持本片最小面，T-04 验收若需要再补）；target switch 防陈旧=epoch 计数 ref + effect 清理（026 A-128 epoch 先例）；同波次更新既有夹具补 `/workspace/files` 空列表响应（onboarding ×4、project-setup-panel ×2、cockpit-mobile-integration ×1，其中两处计数型 mock 在计数前短路 files 请求，026 A-128 同波次夹具先例）。默认理由: 复用既有挂载/图标/徽章/防陈旧惯例零新视觉语言；扁平树 + aria-level 是最小正确 aria 结构；key 重挂载比显式失效回调更不易漏。
- A-133 2026-08-10 [生效] 027 T-03 预览 UI 默认：组件落点新文件 `components/project-context/workspace-file-preview.tsx`（props {projectId, filePath}，预览区=`<section aria-label="文件预览">` role=region，与树同级纵向堆叠——窄屏抽屉模型下天然纵向，不再另建双栏/抽屉范式）；防陈旧=三重护栏——AbortController 中止在途请求 + epoch 计数 ref（026 A-128 先例）+ 渲染期 path 匹配派生（仅当 state.path===filePath 才渲染 ready/error 内容，否则一律派生 loading，切换文件零旧内容闪现，不依赖 effect 清理时序）；解析器按 kind 逐字段重建新对象而非透传 payload——sensitive-masked/binary-unsupported 分支即使服务端误带 content/dataUrl 也不入 DOM（遮蔽保证钉在 UI 层，纵深防御）；截断提示条文案"已截断（仅显示前 512KiB）"为前端常量，镜像 Adapter WORKSPACE_PREVIEW_TEXT_BYTES=512KiB（A-130），若 Adapter 调整须同步；错误脱敏=单一静态文案"无法加载文件预览，请重试。"（role=alert），不回显服务端 message，重试为 native button（全局 44px）；sizeBytes 语义=完整文件原始大小（A-130），formatWorkspaceFileSize 输出 B/KiB/MiB 一位小数取整；图片 alt=path 末段文件名；未选中引导文案"在文件树中选择文件即可预览。"；新 CSS 类 `.workspace-preview-*` 全 tokens（mono 内容区 `font: var(--text-sm) var(--font-mono)`、截断条 interactive-soft+border-strong、路径头 text-sm muted、图片 max-width var(--full-width)），CSS 契约断言随测试落盘。默认理由: 渲染期 path guard 比纯 effect 清理更能杜绝闪现；字段重建式解析使"遮蔽不回显"不依赖服务端单点纪律；单一脱敏错误文案最少泄漏面。
- A-134 2026-08-10 [生效] 027 T-03 接线默认：预览区挂载于 WorkspaceSetup「工作区文件」区树之后（该区既有 stack 内纵向堆叠）；预览与树同波次 `key={projectId:workspace.path}` 重挂载防跨 target/换绑串扰（A-132 key 先例）；改绑路径实际变化时在 saveWorkspace 两条成功路径（正常与事实核对）清空 selectedFile，避免旧根选中路径在新根下被预览；T-02 占位 role=status"已选中文件"行按 tickets 计划删除，其 mount 测试同波次迁移到预览缝（契约调用方同波次更新纪律）；只读断言=预览区内无 textbox/button/link/contenteditable/input/textarea/select（重试 button 仅 error 态存在且非编辑入口）。默认理由: key 重挂载+改绑清选中比跨组件失效回调更不易漏；占位行删除避免双选中指示并存。
- A-135 2026-08-10 [生效] 027 T-04 浏览器验收默认：验收段落点 smoke:context（勘察确认其覆盖 WorkspaceSetup 面板与真实绑定造数；onboarding smoke 走 guide 路线不承载本片）；造数=临时工作区真实文件（嵌套 docs/inner、程序生成 1×1 png magic bytes、含 NUL 的 app.bin、植入 canary 的 .env、540KiB 大文本、改绑目标目录单文件），随临时目录在 smoke 结束时清理、零仓库残留；验收段位置=绑定完成后做 desktop-light 全分支，窄屏改绑后做 narrow 关键路径（顺带验证 key 重挂载后树反映新根）；矩阵=desktop-light 全分支 + desktop-dark/narrow-light/narrow-dark 各一次关键路径，每态各一次 axe；越界直调 API 9 探针（`..%2F` 各形态/绝对路径/NUL→400 INVALID_INPUT、敏感列表路径→422 WORKSPACE_PATH_REJECTED、.env 预览→200 最小遮蔽负载），逐探针断言无 canary 无宿主路径；canary 三重钉死（DOM、单响应、终扫全部项目 API 响应+服务端日志）；浏览适配器不写 COCKPIT_WORKSPACE_AUDIT_PATH（审计仅记录绑定 realpath/stat/access），既有 AUDIT PASS 计数断言不受影响；narrow 抽屉 axe 各 1 条 aria-allowed-role minor 为既有 aside role=dialog 抽屉模式已知项（A-125 同例），低于 serious/critical 门禁不修。默认理由: 复用既有绑定造数、矩阵与 axe 机制最少新面；改绑后验收顺带覆盖换绑刷新语义；mask-first 架构使 canary 零泄漏可被终扫确定性钉死。
- A-136 2026-08-10 [生效] 028 T-01 outbox payload 白名单默认：键集取自 execution-contracts 公开 DTO 的扁平标量键（含字符串数组），集中于 `audit-event-outbox.ts` 唯一提取函数；源 payload 的 `type`（工具种类）重命名 `toolType` 避免与事件 type 冲突；核心七字段（type/actor_type/actor_id/occurred_at/execution_id/attempt_no 经参数注入）恒在且覆盖同名源键；嵌套摘要（resultSummary/requestSummary/stdout/stderr）与非白名单键静默丢弃不失败，宁缺毋滥——prompt/凭据/隐藏推理/宿主路径永不可经未来字段漏入审计轨迹。默认理由: 白名单对齐公开契约而非现状全集；静默丢弃保证写路径永不因脏 payload fail，审计新鲜度优于完备性。
- A-137 2026-08-10 [生效] 028 T-01 outbox 分配与时钟默认：outbox_seq 在写者事务内 `SELECT COALESCE(MAX(outbox_seq),0)+1` 分配，安全性依赖 SQLite 单写串行化（全部调用点均在 BEGIN IMMEDIATE/事务缝内）；outbox 行 occurred_at 与 payload.occurredAt 取 helper 内 `new Date().toISOString()`，execution_events.created_at 维持 SQL strftime('now') 不变（毫秒差可接受，GLOB ISO 双双成立，fake timers 下确定性断言只针对 outbox 侧）。默认理由: 最小侵入既有 14 处写入点；单写前提使 MAX+1 无竞态。
- A-138 2026-08-10 [生效] 028 T-01 write-ownership 默认：audit_event_outbox 归 safe-execution；audit_event_projection/audit_projection_checkpoints 归 operations-projection（T-02 才建 consumer 模块），本票先登记归属并落最小写者锚点 `operations-projection/audit-projection-store.ts`（INSERT OR IGNORE 幂等重放 + checkpoint UPSERT 原语）满足 writers guard 的"每表至少一个生产写者"；writers.test 同步登记 owner 目录并将 operations-projection 列入 MIGRATED_OWNERS 立即阻断非 owner 写。默认理由: 归属先登记防止窗口期他域误写；锚点原语即 T-02 consumer 所需最小写缝，非投机代码。
- A-139 2026-08-10 [生效] 028 T-01 最小 schema 单测默认：execution-conflicts 测试的 raw 手工最小 schema 补建 audit_event_outbox（无 CHECK/FK，与其既有 execution_events 同风格）；生产 outbox append 不加 tableExists 守卫——canonical schema 恒含三表，tableExists 分支仅遗留于该测试豁免的可选表。默认理由: 新表是 canonical 必备件，生产按恒在前提编码；测试夹具补齐而非生产降级。
- A-140 2026-08-10 [生效] 028 T-01 outbox schema 默认：identity 14→15 一次加三表；audit_event_outbox.id 直接复用 execution_events 事件 id（1:1 镜像，不另造审计身份）；occurred_at 取写入墙钟 ISO ms（与 execution_events.created_at 同 GLOB 校验，不复用源事件时间以保留审计写入时刻）；projection 表不带 FK（投影是派生读模型，引用完整性由 reopen 数据不变量 projection⊆outbox 保证，避免 CASCADE 语义混淆源事实与投影）；T-02 owner 模块未建前，projection/checkpoints 写者以 audit-projection-store 锚点写者登记 write-ownership manifest。默认理由: 复用事件 id 让审计行与源事件互查零映射成本；投影去 FK 符合"投影永不回写、可确定性重建"的派生语义。
- A-141 2026-08-10 [生效] 028 T-02 consumer/catchUp 默认：单 consumer_id 常量 `audit-event-projection`；批次上限 500，每批次一个 BEGIN IMMEDIATE 事务（批次行 INSERT OR IGNORE + checkpoint 前移同事务提交），中断后从批次边界恢复、重叠窗口重放靠 outbox_seq UNIQUE 去重；catchUp 遇 status='rebuilding' 拒绝（PROJECTION_REBUILD_IN_PROGRESS），空 outbox 且 checkpoint 缺失时也自愈建立 idle/0 的 checkpoint 行，使 freshness 可区分"未初始化"与"已追平"；投影 payload_json 逐字节原样存（重放确定性），actor_type/execution_id 从 payload 防御性提取（非字符串降级 NULL 而不失败批次，outbox 行恒为事实源）。默认理由: 批次事务兼顾大 backlog 可恢复性与幂等；自愈建行让读路径无需特判首次运行；防御性提取保证单条脏 payload 不阻塞整体追平。
- A-142 2026-08-10 [生效] 028 T-02 rebuild/freshness 默认：rebuild 为单事务（条件 UPDATE CAS idle→rebuilding、checkpoint 行缺失则以 claimed 态插入、DELETE 清投影、按 outbox_seq 分批全量重放、回 idle 并置 last=max），任何异常整体回滚、不留半成品；提交前确定性校验 COUNT(projection)==已重放行数，不满足即 PROJECTION_REBUILD_INCOMPLETE 回滚；持久态 'rebuilding' 本实现自身永不提交（单事务语义），只能由外部/未来崩溃的常驻 consumer 产生，MVP 不提供自动接管——再现时 catchUp/rebuild 均拒绝，恢复靠人工修 checkpoint 或后续运维工具；损坏 checkpoint（last>max）择 fail-closed 而非自愈回绕：公开路径在 openDatabase reopen 不变量处即 SCHEMA_DATA_INVALID 失败，consumer 内 PROJECTION_CHECKPOINT_CORRUPT 仅纵深防御；freshness 为全局签名 `getAuditProjectionFreshness(databasePath)`（outbox_seq 全局单调、checkpoint 全局单 consumer），未初始化=lag 全量（空 outbox 报 caught_up），按项目 freshness 留待 T-03 查询层评估。默认理由: 单事务 rebuild 用 SQLite 原子性直接兑现"要么全回滚要么投影==outbox 全集"；fail-closed 对齐仓库漂移即失败的纪律，不为不可达状态预付接管复杂度。
- A-143 2026-08-10 [生效] 028 T-03 查询与分页默认：签名 `listProjectAuditEvents(databasePath, projectId, {limit?, beforeSeq?})` 落新 Adapter `operations-projection/audit-projection-queries.ts`（装配根命名空间 `auditProjectionQueries`）；读路径顺序=选项防御校验（limit 1..100 默认 50、beforeSeq 正 safe integer，INVALID_INPUT）→tuple 校验（PROJECT_NOT_FOUND，先于 catchUp，避免无效 tuple 触发全局 consumer 工作或被 rebuilding 态掩蔽）→同步 catchUp（MVP 不建后台守护）→单读事务内取页+freshness 同快照（consumer 提取 `readAuditProjectionFreshness(database)` 快照助手复用）；分页=beforeSeq 排他游标、outbox_seq DESC、多取 1 行判 hasMore、nextBeforeSeq=本页末行 outboxSeq 否则 null；payload 白名单对象原样返回（非对象防御降级 {}，outbox 行恒为事实源）。默认理由: tuple 先行符合仓库 ownership tuple 联合校验纪律；单读事务让页与 freshness 无交错窗口；limit+1 探测比 COUNT 查询少一次全表扫描。
- A-144 2026-08-10 [生效] 028 T-03 freshness 择一默认：freshness 内嵌于列表响应（{events, freshness, nextBeforeSeq}），不建独立 freshness 端点；freshness 保持全局语义（outbox_seq 全局单调、checkpoint 全局单 consumer），不按项目派生。默认理由: 读路径已同步 catchUp，内嵌省一次往返且 UI（T-04）一次请求即得列表+新鲜度；全局语义与 A-142 一致，按项目 lag 需要额外投影计数且 MVP 无消费场景。
- A-145 2026-08-10 [生效] 028 T-03 路由与 DTO 默认：落点 `GET /api/projects/[projectId]/audit-events?limit=&before=`；query 白名单 limit/before，字段词汇 unknown/duplicate/required/invalid_format/invalid_range 以 fields 数组 {code,field} 携带（027 workspace-browse 先例），projectId 校验沿用 threads 路由 RESOURCE_ID/拒绝路径分隔符；全部路由自产响应（含错误）带 cache-control: no-store；错误映射 PROJECT_NOT_FOUND→404、PROJECTION_REBUILD_IN_PROGRESS→409（瞬态冲突可重试）、INVALID_INPUT→400、其余 OperationsProjectionError→500（稳定码+静态脱敏文案）、SchemaError→503，兜底经 storageErrorResponse/internalErrorResponse；wire DTO 下沉 `src/shared/audit-contracts.ts`（AuditEventListItemDto/ProjectAuditEventsPageDto，freshness 类型自模块 dto 迁至 shared 并 re-export，ThreadListItemDto A-122 先例），T-04 UI 直接消费 shared 契约。默认理由: 与最新 GET 路由校验/错误映射/装配惯例逐条对齐；DTO 单一 canonical 定义避免模块与 UI 双份漂移。
- A-146 2026-08-10 [生效] 028 T-04 挂载与来源定位默认：审计区挂载为 ProjectContextPanel 第四 tab「审计」（共享记忆/上下文预览/骨架运行/审计，roving tabindex 与方向键/Home/End 复用既有 tablist 行为，不新增导航范式），组件落 `components/project-context/audit-panel.tsx`；来源定位复用执行卡标题 id `execution-{id}-title` 焦点缝（026 focusWorkItemId 同类先例），scrollIntoView({block:"nearest"})+focus() 后以 document.activeElement 实核落地——目标卡超出桌面渲染窗口（仅前 2 张）或位于已关闭窄屏抽屉时焦点不生效，面板如实提示「该执行未显示在运行详情列表中（仅展示最近的执行）。」（role=status aria-live），不伪造跳转；定位提示不清空已有列表，仅随目标切换/重载重置。默认理由: tab 复用最少惊讶；实核焦点落地比假设可见性诚实，窄屏/窗口外场景不假装跳转。
- A-147 2026-08-10 [生效] 028 T-04 列表 UI 默认：事件类型可读文案集中组件内 EVENT_TYPE_COPY 单处映射（15 个 safe-execution 事件类型），未知类型兜底原文（审计可读性优先，永不空白）；actor 映射 agent/owner/系统/未知（null）；freshness 徽标复用 `.status-label` 既有变体（caught_up→status-completed、behind→status-queued「落后 N 条」、rebuilding→status-running），不新增颜色；分页=「加载更多审计事件」追加式（nextBeforeSeq 排他游标，失败保留已加载并 role=alert 提示可重试，不与首载 error 态混用）；empty/loading/error 复用 state-message 范式，rebuilding 409 经 apiErrorCopy 映射「审计投影正在重建，请稍后重试。」（文案入 api-error-copy.ts）；防陈旧=epoch 计数 ref 全路径（首载+加载更多）+AbortController（首载），projectId 变化全部重置；响应逐字段防御解析不信任服务端形状（畸形页静态 INVALID_PAGE 文案）；只读=面板内无任何编辑入口；44px 由全局 button min-height var(--control-min) 保证；hook-only CSS 类（audit-panel/audit-freshness/audit-event-list）不新增样式定义。默认理由: 读路径已同步 catchUp 使 rebuilding 常态不可达，错误态友好文案为纵深防御；追加式分页比页码更贴合倒序事件流；防御解析把泄漏面钉在 UI 层。
- A-148 2026-08-10 [生效] 028 T-04 浏览器验收默认：验收段落点 smoke:execution（复用其真实执行造数路径，5 执行产出 74 事件 2 页，游标分页在真实浏览器被确定性强制）；桌面段=API 全游标走查（全局 DESC、排他游标、事件数==outbox==projection、checkpoint==MAX）+audit API 全页 JSON 秘密扫描→tablist End 键选中「审计」tab→呈现/文案映射/freshness 已追平→连续加载更多至全量 74 行→44px→来源定位真实焦点落地（activeElement===execution-{id}-title）→axe 明/暗各一次；窄屏段=抽屉内键盘开 tab、44px、定位如实未渲染提示（执行卡在关闭抽屉内不可聚焦）、Escape 焦点归还抽屉开关、axe 一次；axe 方法学=先扫同页 memory tab 基线（审计面板未挂载）再扫审计态以归因，既有 chrome 违规（landmark-unique@execution-timeline moderate、aria-allowed-role@task-context-drawer minor，A-125/A-135 同例）不修，本片引入项随修随验——事件标题 h4 跳级致 heading-order moderate，改 h3 复扫归零；axeScan 对齐 persistent-threads 先例增 color-contrast 全零门禁并记录全部 violation id/impact/target；秘密扫描面扩展 narrowAuditFacingText 与两张审计截图字节（证据落 features/028-audit-projection-mvp/evidence/）；修复 smoke 腐化一处：browser.newPage→browser.newContext（AxeBuilder 硬要求，026 A-129 同例，测试基建缺口非生产缺陷）。默认理由: 基线对比法使「无 serious/critical」门禁可归因、避免把既有 chrome 违规误记为本片缺陷；复用执行造数与既有 axe/扫描机制最少新面。
- A-149 2026-08-10 [生效] 029 T-01 审批中心列表语义默认：列表语义=「待决+失效可辨识」——执行域 pending 可裁决（decisionHint=null），expired/replaced/revoked 失效入列且 decisionHint 如实映射源 status（不新造状态码），approved/consumed/rejected 等落定事实不属于「在等 owner 拍板」不入列；内联决策域经 018 勘察确认 VERSION_CONFLICT 是裁决时结果（记录在 collaboration_operations，不改块状态），proposal 块在库中只有 pending/accepted/rejected 三态、无持久化 stale 态，故待决 proposal 一律以 decisionHint=null 入列（冲突重试由裁决路由自身 409 语义承担），仅 accepted/rejected 落定后出列。默认理由: 如实映射源域状态而非发明中心侧新状态；落定事实有各自域的既成列表/审计面，中心只承载「需要拍板或已不可拍板」的请求。
- A-150 2026-08-10 [生效] 029 T-01 聚合取数与摘要默认：内联决策取数走 public-collaboration 新增公开查询缝 `listPendingProposals(databasePath, projectId)`（实现落 structured-message-store，head+revision JOIN 过滤 state_json.status='pending'），governance Adapter 同技术（sqlite→sqlite）跨 owner 调公开缝，符合 imports 边界、零直读他域表、零过渡边登记；排序 createdAt DESC + approvalId 码元序 ASC 决胜（ISO 字符串与 id 均按码元比较，不依赖 locale）；摘要白名单=command 取 executable+args 拼 title、expectedEffect 作 impactSummary，staged_merge 摘要/title 均 null（其 public_request_json 仅 hash 定位信息），proposal 取 payload title/body，宁缺毋滥——hash/policySource/agentPermission/attemptId 等键永不入 wire；DTO 下沉 `src/shared/approval-center-contracts.ts`（zod strict，A-145 先例），查询签名 `listPendingApprovals(databasePath, projectId)` 同步返回对齐 auditProjectionQueries 读路径风格，tuple 校验 PROJECT_NOT_FOUND 失败关闭（GovernanceError，OperationsProjectionError 同型）；同波次夹具调整=current-advance 增可选 idPrefix（默认 fixture 保持既有 id 不变）支持同库双项目隔离造数。默认理由: 公开缝取数遵守 owner 表读纪律且无需过渡边；白名单键集中于 Adapter 单处提取，未来新增源字段默认不可见。
- A-151 2026-08-10 [生效] 029 T-02 审批中心 GET 路由默认：T-01 只交付模块查询缝，本票补建 `GET /api/projects/[projectId]/approvals/pending`（贴合任务建议与 audit-events 项目级只读列表先例），响应 `{approvals: ApprovalCenterItemDto[]}` 完整快照不分页；projectId 校验沿用 audit-events 路由 RESOURCE_ID/拒绝路径分隔符，拒绝一切 query/fragment（unknown 字段词汇，结构化消息 rejectQuery 先例）；全部自产响应（含错误）带 cache-control: no-store；错误映射 GovernanceError PROJECT_NOT_FOUND→404/INVALID_INPUT→400/其他→500、SchemaError→503、storageErrorResponse→503、兜底 internalErrorResponse 500。默认理由: 与最近 GET 路由校验/错误映射/装配惯例逐条对齐；列表是「现在在等我拍板」的完整快照，分页参数留给真实规模信号。
- A-152 2026-08-10 [生效] 029 T-02 裁决分派取版本默认：T-01 DTO 不携带并发版本且契约不动，UI 裁决前先经既有只读路由取当前版本——执行域 `GET /api/executions/{executionId}` 取 `execution.version` 作 expectedVersion，内联决策域 `GET …/messages/{messageId}/blocks/{blockId}` 取 `block.stateVersion` 作 expectedStateVersion，再 POST 既有裁决路由（执行 `{action:"approve"|"reject",expectedVersion,operationId}`；内联 `{action:"accept"|"reject",expectedStateVersion,operationId}`，UI 批准→accept 映射）；GET→POST 间漂移由既有路由乐观并发 409（APPROVAL_STATE_CONFLICT/VERSION_CONFLICT 等）兜底为脱敏行内错误+按钮复用即可重试，零新写路由、零 DTO 变更；版本读取响应逐字段防御解析（正整数校验），畸形即 DECIDE_INVALID 静态文案。默认理由: read-before-decide 是 018 structured-message-block loadLatest 的既有范式；DTO 加版本会把并发控制语义混进只读聚合契约并回改已交付的 T-01。
- A-153 2026-08-10 [生效] 029 T-02 挂载与来源定位默认：审批 tab 落 ProjectContextPanel 第四位（共享记忆/上下文预览/骨架运行/审批/审计）——审计保持末位使 smoke:execution 既有 End→审计断言不腐化，roving tabindex 与方向键/Home/End 复用既有 tablist；执行域来源定位复用 `execution-{id}-title` 焦点缝（028 A-146 先例，含未渲染如实提示）；勘察确认 018 消息定位缝（messageRefs/jumpToReplyTarget）是 collaboration-panel 内部 ref 机制、无跨面板 DOM 焦点缝可复用，故内联决策来源定位采用规范目标身份链接 `/projects/{projectId}?thread={threadId}[&run={runId}]`（canonicalRunHref 先例，runId 缺省省略 run 参数），语义化 `<a>` 键盘原生可达；失效项不渲染裁决按钮（execution-review 仅 pending 渲染按钮先例），以 status-failed 徽章（已过期/已被取代/已撤销）+error-text 原因行「无法裁决：…。」显著呈现。默认理由: tab 位次最小腐化面；链接导航比伪造跨面板焦点诚实，且符合规范目标身份切换纪律。
- A-154 2026-08-10 [生效] 029 T-02 列表 UI 与反馈默认：域徽标复用 `.status-label` 既有变体（执行→status-running、内联决策→status-queued），状态徽标 pending→status-queued「待裁决」、失效→status-failed，不新增颜色；title 缺省兜底 KIND_COPY（staged_merge→「Staged 合入」）；裁决按钮 aria-label 含标题（「批准 node -v」）消解多项同名歧义，可见文案保持「批准/拒绝」，44px 由全局 button min-height var(--control-min) 保证，来源链接 44px 由新增 `.approval-center-actions a` tokens 规则保证；裁决成功→静默重取列表（silentRefreshRef 不闪 loading）+role=status「已批准/已拒绝，列表已刷新。」；裁决失败→行内 role=alert 脱敏错误（apiErrorCopy 映射，未覆盖码落兜底「裁决未完成，请刷新后重试。」，服务端原文永不回显）+按钮复用即重试，不自动重取（保留错误上下文）；手动「刷新列表」为静默重取；防陈旧=epoch 计数 ref 全路径（首载+裁决逐 await）+首载 AbortController，projectId 变化全部重置；响应 zod strict 解析（pageSchema），畸形页静态 INVALID_PAGE 文案；无编辑入口=面板内除裁决/定位/刷新外无任何写控件。同波次调用方更新=context-accessibility 测试 tab 序与 fetch stub、context-browser-smoke 预存腐化一处（End 末位断言自 028 起应为审计而非骨架运行，测试选择器缺口非生产缺陷，A-129 同例）。默认理由: 静默重取保持列表上下文不丢失；同名按钮带标题 aria-label 是可访问树消歧的最低成本；预存 smoke 腐化随本波次顺手钉回末位真相。
- A-155 2026-08-10 [生效] 029 T-03 续接验证默认：验证落点新文件 `tests/modules/governance/approval-center-continuation.test.ts`（内存库夹具），裁决一律走与中心 UI 完全相同的真实路由调用链（执行域 GET `/api/executions/{id}` 取 `execution.version`→POST 既有裁决路由；内联决策域 GET 块取 `block.stateVersion`→POST decision 路由），中心列表同步断言走真实 GET pending 路由并按 UI 的 zod strict pageSchema 解析，另在批准场景并列断言模块缝 `listPendingApprovals`；执行域批准续接的域内证据取 advance 实际使用的 `consumeApprovedCommand` 缝（approved→consumed、execution waiting_approval→running、工具调用→requested、command action 落行），不真实 spawn 进程；过期造数复用域内既有过期缝 `expireOpenApprovalsForProjectExecution`（stop/stale 路径共用的同一写缝），不手改 status；拒绝续接断言既有 paused+reasonCode+resumeTarget 语义并顺控 continue 回 running；版本漂移用「另一客户端先裁决」的真实域操作制造（非手改 version）。七场景 characterization 首轮全绿——中心裁决→域流程续接→中心列表同步闭环无断裂，未发现生产缺陷，零生产代码改动。默认理由: 验证票必须钉住真实调用链而非 mock 分派；复用域内既有缝造数保证失效/漂移语义是生产真相而非测试摆设。
- A-156 2026-08-10 [生效] 029 T-04 浏览器验收落点与造数默认：验收段落点 smoke:execution（复用最重的真实执行造数链，不跨 smoke 文件复用内联决策造数）；内联决策 proposal 由 provider stub 在首个规划轮（collaborationAction step 0）顺带 emit 真实 proposal 块经公共协作缝持久化，一次性 flag（approvalCenterProposalSeeded）保证多规划轮不重复 emit——审批中心读到的即是生产存储真相而非 API 旁路造数；失效态造数=stale 触发域内 expire 写缝（mission PATCH 使 frozen input 漂移 + 对 waiting_approval 执行发 advance，域内失败关闭为 409 STALE_EXECUTION、执行 stale、开放审批 expired），零手改 DB；lapsed 阶段 step 0 必须写全新路径 src/lapsed.txt——沙盒快照会带上 canonical 已合入的 beta.txt，已存在文件+expectedHash=null 被 verified 写语义失败关闭（造数纪律：各阶段 step 0 一贯只写新文件，与进程 restart 无关，经文件系统诊断实锤）。默认理由: 造数一律走真实公开缝（provider emit/规划/启动/advance/mission PATCH），中心聚合读到的每条记录都可回溯到生产写路径。
- A-157 2026-08-10 [生效] 029 T-04 验收路径与矩阵默认：键盘为裁决主路径——ArrowRight×3 自「共享记忆」选中「审批」tab（roving tabindex+aria-selected+activeElement 实核），Tab 链 tab→刷新列表→批准→Enter 完成执行审批批准、tab→刷新→批准→拒绝→Enter 完成 proposal 拒绝；裁决 notice 先于静默重取发出，用 waitForFunction 钉列表收缩后再断言出列，消 notice/refresh 竞态；来源定位鼠标路径一次（定位来源执行→execution-{id}-title 焦点实核）；44px=面板全按钮 evaluateAll 扫描+来源链接/tab boundingBox 抽查；矩阵=desktop-light 全路径（呈现+两裁决+续跑证据）+desktop-dark 与 narrow-light 各一次失效关键路径（徽章/原因/禁用/44px/axe），与 027/028 矩阵机制同例；续跑证据=中心批准后 advanceUntilStatus 推进 + approval consumed + 执行 staged + 运行详情卡「变更待审阅」可见。默认理由: 键盘主路径覆盖 worst-case 交互；失效态复验暗色/窄屏比重复全量断言更省且满足矩阵各一次关键路径的纪律。
- A-158 2026-08-10 [生效] 029 T-04 既有 chrome axe 基线归因与生产缺陷处理：基线扫描（审批中心未挂载的同页 memory tab）抓出执行时间线 `<ol role="log">` 显式角色覆盖 ol 的 implicit list 角色、li 失去列表父级（axe listitem serious，结构自 0b648dc 起既有；028 同文件 axeScan 未触发系其扫描时机主区域不在运行详情 tab）——判定既有生产缺陷并顺手修：role=log 活区语义上移包裹 div、ol 恢复列表语义（execution-review-ui 12 用例零改动通过，audit 段既有 axeScan 同步受益）；修复后 5 态 axe（desktop light 基线/pending 列表/失效项、desktop dark 失效项、narrow light 抽屉）blocking 与 color-contrast 全零；残留既有 chrome 项 landmark-unique:moderate@execution-timeline（多卡同名 log 地标）与 aria-allowed-role:minor@task-context-drawer 维持 A-125/A-135/A-148 同例——低于 serious/critical 门禁不修、记录待后续专片。默认理由: serious 级违规不能靠基线豁免放行，但修复保持最小面（语义上移、零样式零测试变更）；moderate/minor 既有项按先例归因不扩张本片范围。
- A-159 2026-08-10 [生效] 030 T-01 schema 放宽默认：identity 15→16 仅放宽 `audit_event_outbox.source` CHECK 为 `('safe_execution','public_collaboration')`；勘察确认 projection 表 source 列本无 CHECK 无需同步；同波次同步全部 identity 引用——rejection 矩阵 legacy 1..15/unsupported 17、unsupported-schema-input fixture 类型联合、current-schema 双套件、execution-audit-outbox、四个 public-collaboration reopen/source-api 套件与 persistent-threads browser smoke 的 user_version 断言。默认理由: 最小 schema 变更面；identity 引用同波次更新是 canonical schema 纪律（016）的既有惯例。
- A-160 2026-08-10 [生效] 030 T-01 事件选型默认：入列 16 类型——run_started/run_paused/run_resumed/run_retried/run_stopped/run_planned/boundary_paused/owner_message/agent_message/handoff/decision_requested/decision_answered/tasks_created/task_claimed/action_rejected/context_changed；噪声类型（model_call_started/succeeded/failed、usage_recorded、attempt_interrupted）不入列——高频低审计可读性；选型常量 `AUDITABLE_COLLABORATION_EVENT_TYPES` 集中于 `public-collaboration/audit-event-outbox.ts` 单处，未入列类型在 append 入口即返回（零 SQL 成本）；16 类型逐一有行为测试钉住（选型内全覆盖），噪声经真实 turn finalize 断言零 outbox 行。默认理由: 审计可读性优先——owner 看「发生了什么」不需要模型调用级遥测；集中常量使后续选型调整单点可审。
- A-161 2026-08-10 [生效] 030 T-01 owner_message 事实镜像默认：勘察确认 owner 消息在现行线程 API 只写 `collaboration_thread_facts`（无 collaboration_events 对应行），故镜像挂 `appendBatchTx` 单点——仅 type='owner_message' 追加 outbox（eventId=factId、runId 可空、payload 取事实 payload+messageId），一处覆盖 writeOwnerThreadMessage/startThreadRun 首条消息/answerThreadDecision 答案消息/结构化 owner 消息全部现在与未来写入点；agent_message 事实不镜像（已有事件镜像，避免双计）。另勘察确认 run-service 四个 legacy 项目级函数（controlRun/answerDecision/createOrAppendRun/appendProjectMessage）共享的 appendEvent helper 其 INSERT 缺 NOT NULL 的 thread_id/project_id 列，在 current schema 下调用即约束失败回滚（不可达成功、不留 outbox 痕迹），故不接线；死路由清理属后续产品决策，不在本片扩张。默认理由: 单点镜像杜绝未来写入点漏接；事实型 owner 消息与事件型行共用同一白名单/摘要缝，语义一致。
- A-162 2026-08-10 [生效] 030 T-01 白名单与摘要默认：扁平公开标量键取自 collaboration-contracts timelinePayloadSchemas（agentDisplayName/agentId/attemptId/boundary/category/currentAgentId/decisionId/fromAgentId/limit/mentionAgentId/mentionDisplayName/messageId/messageSequence/missing/nextAgentId/overriddenByMention/toAgentId/turnId/value/workItemId），集中唯一提取函数；公开对话文本键（answer/question/reason/summary 与 decision options 数组）仅经 200 grapheme 截断（Intl.Segmenter zh-CN，超限加 …）+ 既有 public-text 分类缝 `assertPublicProjectionText` 校验后入列，拒绝即降级 `[redacted]` 占位而不阻断业务写；owner/agent_message 以 payload.messageId 回查 collaboration_messages 正文生成 messageExcerpt（同截断同分类）；tasks_created.items 嵌套结构只保留 taskCount；type/actorType/actorId/occurredAt/threadId/runId 恒在并覆盖同名源键。默认理由: 对齐 028 A-136 宁缺毋滥——凭据/隐藏推理/宿主路径永不可经未来字段漏入；摘要降级占位保证审计新鲜度优于单条完备性。
- A-163 2026-08-10 [生效] 030 T-01 write-ownership 登记默认：`audit_event_outbox` 事实归属保持 safe-execution（ownership.test「每表恰好一个 owner」不变），manifest 新增顶层 `sharedAppendWriters` 登记多源追加缝（safe-execution+public-collaboration），writers.test 对该表放行已登记 source owner 目录并集；这是永久性架构机制而非过渡豁免——TRANSITIONAL_NON_OWNER_WRITERS 只缩不增的纪律不受影响；ratchet 边计数不受影响（其 fileOwner 只归因 src/server 遗留文件）。默认理由: outbox 按设计就是每 source owner 各自追加自己行的共享缝；显式登记比塞入过渡豁免清单更诚实且可审计。
- A-164 2026-08-10 [生效] 020 治理回归默认：tests/setup.ts 的 DOM 专属加载（@testing-library/react、cleanup、@testing-library/jest-dom）改为仅在 jsdom 环境惰性动态 import，next/navigation mock 工厂内异步取 React——纯 node 环境测试文件不再支付 React/testing-library/jest-dom 的 setup+import 成本（实测 40 个最小文件 setup 17.71s→3.40s）；node 环境文件行为不变（jest-dom 断言器本就只服务 DOM 测试，afterEach 清理由 typeof window 守卫等价迁移）；内存库夹具命名加 worker_threads threadId 段（file:...-{pid}-{threadId}-{counter}），为任何共享 pid 的 pool 模型预防 shared-cache 内存库跨线程撞名，forks 下 threadId=0 不改变现有隔离语义。默认理由：环境条件化加载把每文件固定成本只付给真正需要 DOM 的文件；夹具命名硬化是一次性预防而非等故障发生。另记录被否方案：pool:threads 两次全量各出现 1 个互不相同的非确定性失败（第二次配置已回退后仍现 1 次，判定既有不稳定用例苗头而非 threads 专属），但收益不确定（-6s 量级）且改变隔离语义，暂不采用，留待后续治理复评。
- A-165 2026-08-10 [生效] 030 T-02 域分类与徽标默认：wire DTO 不增 source 字段（030 架构「Operations Projection 零协议改动」），UI 以事件类型映射集分类域——协作文案映射 `COLLABORATION_EVENT_TYPE_COPY`（镜像服务端闭集 `AUDITABLE_COLLABORATION_EVENT_TYPES` 16 类型）兼作域分类器，命中即协作、其余一律执行；两 source 写入的类型集不相交（safe-execution 15 类 vs 协作 16 类，outbox 写点受闭集约束），故分类对现存全部可能数据精确；未来新增协作类型若未同步 UI，徽标保守显示执行、标题兜底原文（可读性不破，仅域色偏差）。徽标配色复用既有 `.status-label` 变体（执行→status-running、协作→status-queued，029 A-154 审批中心先例），零新增颜色。默认理由: 协议零改动是基座纵切扩展性的核心验证目标；类型集不相交使 UI 侧分类与 wire 字段等效，且未来偏差方向保守可读。
- A-166 2026-08-10 [生效] 030 T-02 摘要呈现默认：messageExcerpt 仅 owner/agent_message 两类渲染（服务端白名单只为消息类产出该键），单行 ellipsis 截断样式落 `.audit-event-excerpt`（tokens：text-subtle/text-sm，镜像 .input-history-excerpt 截断三件套），空串/非字符串/缺失一律不渲染；`[redacted]` 降级占位原样呈现（如实反映服务端脱敏结果，不再二次加工）；服务端已做 200 grapheme 截断+凭据分类，UI 只做呈现不做内容校验。CSS 契约走 readFileSync 断言先例（027 A-133）。默认理由: 截断样式是票据明确要求；渲染门禁按事件类型+形状双条件把泄漏面钉在 UI 层。
- A-167 2026-08-10 [生效] 030 T-02 协作来源定位默认：message 精确跳转不可复用——018/022 的 messageRefs/jumpToReplyTarget 是 collaboration-panel 内部 ref 机制（029 A-153 勘察结论），且项目选择路由 `parseProjectSelection` 只接受 thread/run 参数（多余键整体判非法回退 "/"），故协作事件定位=规范目标身份链接 `/projects/{projectId}?thread={threadId}[&run={runId}]`（canonicalRunHref/sourceThreadHref 先例，URLSearchParams 编码，owner_message 事实镜像 runId=null 时省略 run 参数）；payload 引用畸形（threadId 非字符串/空）不渲染链接而非伪造跳转；执行域 `execution-{id}-title` 焦点缝定位行为零改动（既有用例锁定）。消息级精确跳转需先建 URL 可达的消息定位缝，留待后续特性决策。默认理由: 链接导航比伪造跨面板焦点诚实；复用规范目标身份是仓库既有切换纪律。
- A-168 2026-08-10 [生效] 030 T-03 浏览器验收默认：验收段落点择定 smoke:threads（persistent-threads-browser-smoke.mjs）——勘察确认其真实协作造数最丰富（7 条 owner 消息含回复/附件、2 次 run 生命周期、decision_requested、跨线程隔离造数现成），smoke:execution 保留 028 执行域审计段、不为其补协作造数（避免跨 smoke 复制线程基础设施）；验收段内聚单块置于 favorites 重启段之后、终扫之前（A-125 同例），证据三截图落 features/030-collaboration-audit-events/evidence/、断言入 014 results.json。验收路径：API 全游标走查（caught_up、全局 DESC、五必备类型、messageExcerpt 逐字、规范 threadId/runId 携带与 run-less 省略、foreign-project 404 无宿主路径、API JSON 秘密扫描）→DB 一致性（outbox==projection==API==14、checkpoint==MAX、source 全 public_collaboration、噪声类型零行）→桌面 tablist End 键选中审计 tab→14 行全渲染+每行协作徽标+摘要行+定位链接 href 规范身份→链接 focus 焦点环+Enter 真实导航（运行选择器 inputValue 实核落点）→44px→axe desktop light/dark；窄屏抽屉键盘开启/tab Enter/关键内容/44px/axe/Escape 焦点归还。axe 方法学沿 A-148 基线对比法（先扫 memory tab 基线再扫审计态）：22 态 blocking 与 color-contrast 全零，审计面板三态零违规，唯二残留为既有 aria-allowed-role:minor@task-context-drawer（A-125/A-135/A-148 同例，低于门禁不修）；秘密扫描面扩展 auditFacingText/narrowAuditFacingText/三截图字节（028 A-148 先例）。默认理由: 复用最丰富真实造数与既有 axe/扫描机制最少新面；基线对比法使门禁可归因，本片零生产缺陷。
- A-169 2026-08-10 [生效] 031 T-01 索引方式决策（FTS5 勘察证据）：node:sqlite（node v24.14.0）实测 `PRAGMA compile_options` 含 ENABLE_FTS5，`CREATE VIRTUAL TABLE x USING fts5(content)` 成功、英文 token `MATCH 'world'` 命中；但仍择普通表 + LIKE contains，两条独立否决理由——(a) unicode61 默认分词对中文 contains 无效（实测 `MATCH '你好'` 对 '你好世界' 返回空集，FTS5 CJK 需分词器扩展，中文分词本片范围外）；(b) FTS5 虚表产生 5 张影子表（x_config/x_content/x_data/x_docsize/x_idx）进入 sqlite_master，与 exact-schema 校验器不兼容（`CREATE_HEADER` 只认 TABLE/INDEX/TRIGGER、`kind` 联合无 virtualTable、sqlite_master 行数精确比对必漂移）。LIKE 基底实测语义：ASCII 大小写折叠（'%keyword%' 命中 'Keyword'）、中文 contains 正常（'%正文%' 命中）、非 ASCII 不折叠——满足本片「标题+正文 contains」需求；检索冒烟断言钉在 consumer 测试文件中。默认理由: 中文 contains 是核心检索语义且分词调优范围外；不为其扩张 canonical schema 校验器面。
- A-170 2026-08-10 [生效] 031 T-01 索引表形态默认：identity 16→17 加 `thread_search_index` 普通表——无 FK（A-140 投影语义：派生读模型不以 CASCADE 混淆源事实，索引⊆事实源由 reopen 数据不变量两条新查询钉住：行必须指向存在的协作线程、message 行 message_id 必须存在于 collaboration_messages）；`UNIQUE(project_id,thread_id,kind,message_id)` 对 message 行去重，标题行去重靠部分唯一索引 `thread_search_one_title`（UNIQUE 中 NULL message_id 互不冲突，thread_fact_one_created 先例）；条件 CHECK 钉死 kind⇔(message_id,source_seq) 形态；`source_seq` 语义=message 行取入索引事件的 outbox_seq（全局单调游标），标题行恒 0（非事件来源）；occurred_at 取源表 created_at（catchUp 与 rebuild 同源字节一致）。write-ownership 登记 operations-projection（writers.test「每表至少一个生产写者」由 consumer store 锚点满足）。默认理由: 与 028 投影基座逐条同构，减少第二 consumer 的语义发明。
- A-171 2026-08-10 [生效] 031 T-01 标题增量来源默认：勘察确认 thread_created 事实不入 outbox（030 A-161 镜像单点只覆盖 owner_message，agent_message 走事件写入点），故 catchUp 以任一协作 outbox 事件的 payload.threadId 捎带标题行（INSERT OR IGNORE 自 collaboration_threads 回查）；零消息线程的标题仅由 rebuild 全量扫 collaboration_threads 补全（MVP 接受，空线程搜索价值低）；勘察确认标题不可变（全仓库无 SET title 更新路径），捎带行永不过期、无需改名事件。默认理由: 不为标题单独扩张 outbox 选型（030 已冻结事件集），捎带机制用既有事件流达到同等覆盖。
- A-172 2026-08-10 [生效] 031 T-01 rebuild 数据源与敏感边界默认：rebuild=清索引+全量扫 threads 标题+重放 outbox 消息事件回查正文（而非全量扫 collaboration_messages）——checkpoint 语义与 catchUp 同源一致（rebuild 后 last_outbox_seq=MAX(outbox_seq)，索引=catchUp 从零重放+零消息线程标题），确定性后置条件=标题行数==collaboration_threads 数且 message 行数==重放解析到的 distinct 消息键数，不符即 PROJECTION_REBUILD_INCOMPLETE 整体回滚；敏感边界=索引正文即 collaboration_messages 已落库公开正文（owner 在线程 UI 可见的同一文本），不再二次分类（spec 决策），审计面专属 [redacted] 降级不影响索引（测试钉住：摘要被扣留时索引仍逐字等于源正文），consumer 只写 thread_search_index/checkpoints、永不回写源表（writers 架构测试钉住）。默认理由: rebuild 与 catchUp 同一事件源是「rebuild 结果==从零 catchUp」确定性语义的唯一相干选择；单源公开正文避免审计/搜索两套脱敏漂移。
- A-173 2026-08-10 [生效] 031 T-02 查询语义默认：`searchProjectThreads(databasePath, projectId, {query, limit?, before?})` 对齐 auditProjectionQueries 读路径（选项校验→tuple 404→同步 catchUp→单读事务）；匹配用 `instr(lower(content), lower(?))>0` 而非 LIKE ESCAPE——按字面包含、零通配符语义（%/_/\ 不特殊），JS 侧 snippet 定位用仅折叠 A-Z 的 asciiFold 与 SQLite lower() 逐字节对齐；排序 occurred_at DESC + (thread_id, message_id) ASC 决胜（项目内 (thread_id,message_id) 对唯一故全序稳定，标题行 message_id NULL 排最前）；游标=base64url(JSON [occurredAt, threadId, messageId]) 排他（严格更旧），模块内严格解码（3 元组/ISO 时间戳/非空 threadId/messageId string|null），失败 INVALID_INPUT；limit 默认 20 上限 50，query trim 后 1..200 grapheme（Intl.Segmenter zh-CN）；snippet=标题命中给完整标题、消息命中给首个命中点 ±60 grapheme 窗口、截断侧加 …（码元偏移→grapheme 索引映射，命中不可达时降级头窗口不炸页）；响应不内嵌 freshness（对比 028 A-144：搜索读路径已同步 catchUp，MVP 无 lag 呈现场景，T-03 UI 不需要）。默认理由: instr 字面匹配把通配符转义面整个消除；全序决胜+排他游标保证分页不重不漏；snippet 窗口 grapheme 语义与仓库既有截断先例（017/030）一致。
- A-174 2026-08-10 [生效] 031 T-02 路由与 DTO 默认：落点 `GET /api/projects/[projectId]/thread-search?q=&limit=&before=`；query 白名单 q/limit/before，字段词汇 unknown/duplicate/required/invalid_format/invalid_range + q 专属 too_long（input-history A-114 词汇），q 必填（缺失/空/纯空白→required）且路由侧即做 200 grapheme 上限；before 为不透明游标，路由只验 base64url 字符集与 ≤200 长度，严格解码在模块（解码失败→400 INVALID_INPUT 无 fields，与路由字段级错误并存）；projectId 校验沿用 audit-events 路由 RESOURCE_ID/拒绝路径分隔符；全部自产响应 cache-control: no-store；错误映射 PROJECT_NOT_FOUND→404、PROJECTION_REBUILD_IN_PROGRESS→409、INVALID_INPUT→400、其余 OperationsProjectionError→500、SchemaError→503、storageErrorResponse/internalErrorResponse 兜底（A-145 逐条对齐）；wire DTO 下沉 `src/shared/thread-search-contracts.ts`（zod strict，A-145/A-150 先例），模块 dto.ts re-export；装配根登记 `threadSearchQueries` 命名空间（consumer 仍只经模块内 adapter 互调与测试直引，不登记）。默认理由: 与最近 GET 路由校验/错误映射/装配惯例逐条对齐；游标不透明使编码可演进，路由浅校验+模块严格解码分层清晰。
- A-175 2026-08-10 [生效] 031 T-02 测试夹具默认：新增共享夹具 `tests/fixtures/collaboration/search-graph.ts`（provider 真实 vault 信封+双 agent+按项目 project/memberships/mission/seedMissionInitialization 参数化），供 query 与 api 两个新测试文件复用，避免第三份大型直接 SQL 图复制（consumer 测试既有本地 seed 不动）；零消息线程标题行在测试 setup 经 rebuildThreadSearchIndex 入索引（A-171 语义），排序/分页测试用假时钟推进或同时戳决胜构造确定性 occurred_at。默认理由: 共享确定性夹具是仓库测试纪律；rebuild 是零消息线程唯一入索引路径，setup 用之即钉住该语义。
- A-176 2026-08-11 [生效] 031 T-03 搜索入口与 tablist 关系默认：搜索框落线程区顶部（「线程」标题行之下、全部/已收藏 tablist 之上），`type="search"` + aria-label「搜索线程」，防抖 300ms，Esc 清空并退出搜索态；搜索激活（trim 非空）时结果区**原位替换** tablist 与线程列表 nav（非覆盖层），清空即恢复原列表——择原位替换而非覆盖层的理由：面板宽度有限、覆盖层需额外焦点圈禁与 dismiss 面，原位替换与 025 单栏列表心智一致且最少惊讶；结果区 aria-label「线程搜索结果」region，loading「正在搜索…」/empty「无匹配结果。」/error（脱敏 apiErrorCopy + 重试搜索按钮）全态，kind 徽标复用 `.status-label`（标题命中加 interactive-soft 变体），44px 由 `var(--control-min)` 保证，样式全 tokens 零硬编码；键盘=输入框 ArrowDown 入首项、结果间 ArrowUp/Down、首项 ArrowUp 回输入框、Enter 激活、结果上 Esc 清空并焦点归还输入框。默认理由: 与 025 tablist/028 审计面板全态与键盘惯例逐条对齐；原位替换是两种列表互斥呈现的最小状态机。
- A-177 2026-08-11 [生效] 031 T-03 消息定位 URL 语义默认：`parseProjectSelection` 扩展可选 `message` 参数——仅允许与 thread 同现（无 thread 整体 null）、单值、PATH_SAFE_ID 白名单字符，重复/非法/多余键整体回退 null 语义不变，无 message 时行为逐字节不变（向后兼容）；规范 href 参数序 thread→run→message（编码后重排，乱序输入规范化）；标题命中点击生成 `?thread=..`（不带 message），内容命中生成 `?thread=..&message=..`；结果激活用 `history.pushState` + 手动分发 popstate（保持 SPA 内状态、触发各面板 locationVersion 重解析），而非 router.push——与线程选择 chooseThread 的 router.push 并存属既有双轨（URL 解析两端同源 parseProjectSelection/URLSearchParams 白名单语义）；task-panel 在初始 state 与 popstate 两处解析 message→`CollaborationPanel.requestedMessageId`，非法形态整体落 null（协作区不选中）。默认理由: 严格白名单+整体回退沿用既有选择解析纪律；规范参数序使 URL 可比较可缓存；pushState+popstate 是结果点击不重建面板的最小侵入。
- A-178 2026-08-11 [生效] 031 T-03 面板定位消费默认：复用 022 内部 messageRefs/locate 缝扩 URL 入口——requestedMessageId 已渲染则直接 locate（scrollIntoView({block:"nearest"})+focus()+1.6s 高亮）；未渲染则沿 facts nextAfter 游标逐页回填直至找到或耗尽（与 jumpToReplyTarget 同构的 backfillToRequestedMessage，共享 factRequestRef/inFlight 闸与 epoch/targetGuard 防陈旧）；耗尽或游标本空→稳定占位「无法定位指定的消息：它不在当前可读取的协作历史中。」（role=status，如实文案，不伪造跳转，scrollIntoView 零调用有测试钉住）；占位随 requestedMessageId 变化与 target 切换重置（requestedMessageHandledRef 按 targetKey|messageId 去重防重复回填）；无 message 参数零定位行为（不滚动不占位）。默认理由: 回填语义与 022 回复跳转完全同构、单一事实加载缝；诚实占位对齐 028 A-146「不伪造跳转」先例。
- A-176 2026-08-10 [生效] 031 T-03 搜索入口与 tablist 关系默认：搜索框落线程区顶部（project-thread-navigation 面板内「线程」标题行之下、全部/已收藏 tablist 之上，最少惊讶位）；搜索激活（trim 后非空）时结果区替换 tablist 与线程列表（非覆盖层），清空或 Esc 恢复——输入框 Esc 清空并保持焦点，结果项 Esc 清空并还焦输入框；防抖 300ms（先置 loading 再计时发请求）；结果区用语义化 section aria-label「线程搜索结果」（region 角色），loading 状态在 region 外（role=status「正在搜索…」）使 region 只在终态（结果/空「无匹配结果。」/错误+重试）出现；kind 徽章复用 .status-label（标题命中加 thread-search-kind-title 高亮变体），文案「标题/内容」；加载更多按钮「加载更多搜索结果」按下即禁用并显「正在加载更多…」，失败行内 role=alert 保留已加载页；防陈旧=searchEpochRef 计数（每次输入变化递增）+targetGuard capture（AbortSignal 随 projectId 切换中止），首载与加载更多同路径守卫；projectId 切换主加载 effect 内全量重置搜索态。默认理由: 替换而非覆盖层与 025 线程列表单一面板心智一致；region 终态语义让屏幕阅读器不被瞬态 loading 打断；epoch+abort 双闸与 029 A-154 审批中心先例一致。
- A-177 2026-08-10 [生效] 031 T-03 URL 与消息定位链路默认：结果点击经 window.history.pushState+PopStateEvent dispatch（canonicalRunHref/navigateToRun 既有同页参数缝，router.push 不派发 popstate 无法驱动 TaskPanel 监听）+onNavigate 回呼；parseProjectSelection 扩展可选 message 参数（PATH_SAFE_ID 校验，重复/非法/无 thread 伴随/多余键一律整体回退 null，向后兼容无 message 行为不变），canonical href 参数序 thread→run→message；TaskPanel collaborationTarget 增 messageId（非法 message 参数与 run 同例整体判空），三处 CollaborationPanel 挂载传 requestedMessageId；CollaborationPanel 消费=复用 022 messageRefs/setLocateMessageId 滚动+焦点+高亮缝，未加载时沿 facts 排他游标回翻到命中或穷尽（backfillToRequestedMessage 与 jumpToReplyTarget 同构），穷尽未命中→role=status 如实占位「无法定位指定的消息：它不在当前可读取的协作历史中。」不伪造跳转；定位每次 (targetKey,messageId) 只执行一次（requestedMessageHandledRef），target 切换重置占位与已处理标记。默认理由: 同页参数变更沿用仓库已验证的 popstate 缝而非引入第二套导航机制；一次性定位避免轮询注入新事件时重复抢滚动。
- A-179 2026-08-11 [生效] 031 T-04 验收段落点与造数默认：段落点 smoke:threads（persistent-threads-browser-smoke.mjs，030 A-168 勘察结论沿用——真实协作造数最丰富），搜索段内聚单块置于协作审计窄屏段之后、终扫之前（审计段含精确事件序列/行数断言，新增造数必须在其后方零位移）；关键词择三个跨夹具零碰撞独特词分工三类命中——quokka=legacy 首线程内容命中、云雀=新线程「云雀发布计划」标题命中、walrus=外部项目独有词（隔离+empty）；外部项目经公开 API 真实造数（POST /api/projects→PUT members→POST threads→POST messages，outbox 自然镜像、搜索读路径同步 catchUp 即刻可查，A-171 标题捎带由首条 owner_message 触发），不复用 030 既有 `foreign-project` 404 探针名（随机 UUID 新项目，互不干扰）；loading 态经 page.route 谓词拦截延迟搜索响应 1.2s 实核「正在搜索…」（finally unroute 防污染后续段）；证据三截图落 features/031-thread-search/evidence/、断言与 axe 态入 014 results.json（A-168 同例）；秘密扫描面扩展 searchFacingText 与三截图字节（A-148/A-168 先例），搜索 API 响应随 productApiBodies 全量入扫外再逐体显式断言无 apiKey/masterKey。默认理由: 审计段后插入是对既有精确断言零侵入的唯一安全位；三词分工使标题/内容/隔离各只有一个确定命中，断言可全等而非包含。
- A-180 2026-08-11 [生效] 031 T-04 分层 Escape 修复默认（生产缺陷）：窄屏导航抽屉内搜索激活时按 Esc，useModalSurface 挂在 dialog 元素上的原生 keydown 监听器先于 React 根合成 onKeyDown 触发——修复前整个抽屉被关闭而非仅清空搜索（违 input-history-panel 分层 dismiss 既定 UX）。修复=搜索区 section 挂原生 keydown 监听（input-history-panel stopPropagation 先例逐条同构），搜索激活时 stopPropagation 消费 Esc（清空+焦点还输入框）、非激活不拦截（Esc 正常冒泡关抽屉）；React 合成处理器（输入框/结果项）中的 Esc 分支删除收编到原生监听单一处理点，防双消费双文案漂移；searchActive 镜像入 ref 供原生回调读当前值。RED=jsdom 用例断言 enclosing keydown 不被调用→清空还焦→第二次 Esc 透传；真实浏览器窄屏段实核双层语义（清空保抽屉/再按关抽屉焦点归还开启器）。默认理由: 原生监听是唯一能先于 dialog 级原生监听拦截的层；单一处理点消除合成/原生双轨的后续维护分叉。
- A-181 2026-08-11 [生效] 031 T-04 smoke 基础设施加固默认（cutFactsHandler 二次 fulfill 竞态）：验收段首跑暴露既有路由拦截器崩溃面——cutFactsHandler 自 022 回复段注册后全程常驻，后续每个 facts 轮询（POLL_INTERVAL）都经过它；导航与在途轮询交叠时请求被中止使首个 fulfill 抛错，落入本为 JSON.parse 准备的 catch 后再次 fulfill，进程以 "Route is already handled" 未捕获异常整体崩溃（时序竞态，此前靠运气幸存；套件变长使概率单调上升）。加固=解析与 fulfill 分置两层 try（内层只包 JSON.parse）、外层捕获中止即丢弃该 route（页面已不再等待响应），拦截语义逐字节不变。默认理由: 最小结构加固消除整类崩溃而非调时序碰运气；不改既有断言面。
- A-182 2026-08-11 [生效] 032 范围与标签形态默认：标签=项目作用域组织事实；无重命名/颜色/图标/嵌套分组（改名需求由删除+重建覆盖）；不建标签回收站（S-20 范畴）；标签不入 thread_search_index 正文、031 检索范围不变；跨项目绝不泄漏。默认理由: 单一切片单一用户结果（用标签把线程整理成可筛选主题视图）；最小词汇表面降低维护与泄漏面。
- A-183 2026-08-11 [生效] 032 schema 形态默认：identity 17→18 纯增 thread_tags/thread_tag_edges/thread_tag_operations + thread_tag_edges_by_tag 索引，零既有表改动；name_key=NFC+大小写折叠作项目内唯一键（UNIQUE(project_id,name_key)），name 存 trim 后原文、DB CHECK length>=1 且 name=trim(name)、1..40 grapheme 在模块层校验（structured-message-schema graphemeLength 先例）；edges PK (project_id,thread_id,tag_id) + 双 FK ON DELETE CASCADE（favorites 先例），删除标签同事务显式清边、CASCADE 仅兜底线程级联；reopen 数据不变量新增两条（edges⊆threads、edges⊆tags），tags⊆projects 由 FK + reopen foreign_key_check 覆盖；identity 引用同波次同步清单沿 031 T-01（rejection 矩阵 legacy 1..17/unsupported 19、unsupported-schema-input 联合、current-schema 双套件、execution-audit-outbox、public-collaboration reopen/source-api/favorite/outbox 套件、persistent-threads smoke user_version）。默认理由: 纯增 schema 最小漂移面；折叠唯一键把同义名归一并约束在 DB 层而非仅靠应用层。
- A-184 2026-08-11 [生效] 032 创建幂等默认：createThreadTag 对 name_key 冲突返回既有标签 created:false（200），不 409。默认理由: 折叠冲突多为同义重复（大小写/NFC 差异），幂等返回让 UI 创建路径零错误分支；与偏好类事实幂等先例（025 收藏/023 草稿）一致。
- A-185 2026-08-11 [生效] 032 删除与确认语义默认：deleteThreadTag 同事务显式 DELETE edges→DELETE tag、响应 removedEdgeCount 如实计数；确认走 UI 强确认对话框（删除前以 listProjectTags 的 threadCount 如实显示影响面），批量移除标签同为 UI 确认对话框；不走 CAP-GOV-02 审批中心——029 中心服务异步 pending/approve 生命周期的高风险域（执行/内联决策裁决），标签删除是同步、可重建、影响面仅组织元数据的动作；S-20 永久删除才是强治理确认候选。默认理由: backlog 约束「审批=批量破坏性动作确认」由显式 UI 强确认满足；不为可重建的组织元数据预付异步审批生命周期复杂度。
- A-186 2026-08-11 [生效] 032 列表缝扩展默认：listThreads rawInput 增 tagId 过滤（路由白名单单值 RESOURCE_ID 校验）；与 favoritesOnly 互斥 400 fields（favorites 视图按 favorited_at 排序、拒绝 cursor，两视图语义不组合），tagId 过滤保持 last_activity_sequence DESC 排序不变故 cursor 兼容；ThreadListItemDto 恒在 tags: ThreadTagRefDto[]（页面线程 id 批量第二查询组装，无 N+1）；组件 zod schema 与测试夹具同波次更新（025 T-01 同波次先例）。默认理由: 复用 025/031 同一 list 缝是最少面扩张；互斥避免双排序语义组合出不可分页态。
- A-187 2026-08-11 [生效] 032 批量 operation 语义默认：独立项目作用域 receipt 表 thread_tag_operations（kind='tag_batch'、request_hash 64-hex、status='completed'、response_json、UNIQUE(project_id,id) 与 (project_id,id,request_hash)），不塞进 collaboration_operations——其 thread_id NOT NULL 无法诚实承载跨线程批量；同事务 receipt 查重→全量 tuple 校验→幂等边 upsert/delete→写 receipt，同步原子写无 pending 态；同 operationId 同 hash 重放返回存储响应 replayed:true，异 hash 409 OPERATION_CONFLICT；上限 threadIds≤100、add+removeTagIds≤20 自动去重；任一 id 跨 tuple 整体 404 回滚（全有或全无）。默认理由: backlog「审计=批量写入版本化」由持久 receipt 兑现且重放字节一致；独立表保持既有 operations 表 thread tuple 语义诚实。
- A-188 2026-08-11 [生效] 032 不进 audit outbox 默认：标签创建/删除/分配/解除为偏好类组织事实，行内 created_at 审计足够（025 收藏先例）；批量整理由 thread_tag_operations 持久 receipt 覆盖；030 AUDITABLE_COLLABORATION_EVENT_TYPES 闭集不扩张、thread_search_index consumer 零改动。默认理由: 审计价值/成本比低且已有可追溯面；未来若需标签审计可见性，经 030 纵切模板另行评估。
- A-189 2026-08-11 [生效] 032 UI 形态默认：线程区「管理标签」对话框（创建 trim+≤40 grapheme 校验提示/搜索 contains/删除+强确认显示将解除 N 条分配）+ 列表项标签 chips（复用 .status-label 徽章范式，不新增颜色）+ 筛选 chip 条（选中标签时若处已收藏视图回退「全部」，与 A-186 互斥语义对齐）+「整理线程」多选批量条（operationId=crypto.randomUUID、成功 role=status notice+静默刷新、失败行内 role=alert 可同 operationId 安全重试）；防陈旧 epoch+AbortController（029 A-154 先例）、useModalSurface 分层 dismiss（031 A-180 先例）、tokens/44px/键盘/全态纪律不变。默认理由: 复用既有对话框/徽章/防陈旧惯例零新视觉语言；chip 条比新增 tab 更少导航范式扩张。
- A-190 2026-08-11 [生效] 032 T-01 线程级联兜底验证默认：collaboration_threads 行不可直接删除（immutable facts 触发器 IMMUTABLE_THREAD_FACT 拦截，其 WHEN 子句仅在所属 project 仍存在时生效），故 thread_tag_edges 的 FK→threads ON DELETE CASCADE 兜底无法经「删线程」直接演练；T-01 以「删项目」级联（projects→threads→edges/tags 全链 CASCADE）钉住兜底行为，FK 声明字节另由 exact-schema reopen 校验结构性钉住。默认理由: 项目级联是仓库内唯一可达的线程删除路径；双保险（行为测试 + DDL 字节校验）优于为测试放行直接删线程。
- A-191 2026-08-11 [生效] 032 T-01 标签路由 no-store 默认：`GET/POST /api/projects/[projectId]/thread-tags` 与 `DELETE …/thread-tags/[tagId]` 全部响应（成功+错误）统一带 `cache-control: no-store`（thread-search/approvals-pending 先例），不复用 025 favorite 路由的无头响应。默认理由: tickets 明示 no-store；组织元数据列表/计数不应被任何中间缓存钉住旧值。
- A-192 2026-08-11 [生效] 032 T-01 receipt 锚点写入器默认：`insertThreadTagOperationReceiptTx`（thread_tag_operations 唯一生产写入器）在 T-01 先行登记于 thread-tag-service.ts 并写入 write-ownership manifest notes，T-03 批量命令同事务复用该锚点。默认理由: writers 架构不变量要求每表有生产写入者，锚点先行避免 T-01 为通过门禁伪造批量语义；028/031 同例。
- A-193 2026-08-11 [生效] 032 T-02 互斥错误词汇默认：`listThreads` 的 `tagId`×`favoritesOnly` 互斥以 400 `fields.tagId="not_combinable"` 报告（ favorites+cursor 的 `cursor:"not_supported"` 词汇先例同族扩展），错误挂在新增参数 tagId 上而非 favoritesOnly。默认理由: 互斥由新参数引入，把 fields 锚在新参数上让旧客户端的既有错误面零变化；不新增错误码保持 INVALID_INPUT 闭集稳定。
- A-194 2026-08-11 [生效] 032 T-02 tagId 过滤语义默认：list 缝的 tagId 是纯筛选条件而非 tuple 查找——他项目或不存在的 tagId 稳定返回空页 200（不 404、不泄漏他项目分配存在性）；RESOURCE_ID 形态校验在解析层，存在性永不校验。默认理由: 与 favoritesOnly 过滤同构（不存在的「视图」也是空页）；筛选结果确定性由 last_activity_sequence DESC + id 决胜保证，空页语义避免把「标签是否存在」变成跨项目侧信道。
- A-195 2026-08-11 [生效] 032 T-02 分配路由 no-store 默认：`PUT /api/projects/[projectId]/threads/[threadId]/tags` 全部响应（成功+错误）带 `cache-control: no-store`（A-191 语义扩展至 threads 子路径下的标签写路由），不复用 025 favorite 路由的无头响应。默认理由: 组织元数据写入响应同样不应被中间缓存钉住；与同特性 T-01 路由响应头策略保持一致。
- A-196 2026-08-11 [生效] 032 T-03 批量上限计数语义默认：`applyThreadTagBatch` 的 add+removeTagIds≤20 上限按**去重后并集**计数（`new Set([...add, ...remove]).size`），输入侧重复 id 在计数与哈希前自动折叠；threadIds≤100 同样允许重复并折叠。默认理由: tickets 明示「自动去重」，按去重后计数与「去重即规范化」语义一致，避免同一逻辑操作因重复书写被 400；canonicalRequestHash 输入即为去重后形状，重放语义稳定。
- A-197 2026-08-11 [生效] 032 T-03 add/remove 重叠语义默认：同一标签同时出现在 addTagIds 与 removeTagIds 时，批量按「先加后去」顺序执行，per-thread 摘要如实双记（addedTagIds 与 removedTagIds 同含该标签），净效果为无边留存。默认理由: 如实反映事务内真实写序比静默收窄一侧更诚实；摘要双记让调用方看见该标签在本批被触碰两次，且哈希覆盖原始两列表故重放逐字节一致。
- A-198 2026-08-11 [生效] 032 T-03 批量路由 no-store 默认：`POST /api/projects/[projectId]/thread-tag-batch` 全部响应（成功+错误+409/404）带 `cache-control: no-store`（A-191/A-195 语义扩展至批量写路由）。默认理由: 批量 receipt 响应（replayed 标志、per-thread 摘要）不应被任何中间缓存钉住旧值；与同特性全部标签路由响应头策略保持一致。
- A-199 2026-08-11 [生效] 032 T-04 筛选 chip 条显隐默认：筛选 chip 条（`role=group`「按标签筛选线程」）仅在标签加载成功且 `tags.length > 0` 且非整理模式时渲染；项目无标签时整条隐藏但「管理标签」入口恒在（与 tickets「筛选 chip 条」+「管理对话框」分离语义一致）。默认理由: 空标签项目显示一条只有「全部」的筛选条是噪声；管理入口保留让首标签可被发现，诚实覆盖 666 空标签隐藏与删除后剩一标签仍显示两种情形。
- A-200 2026-08-11 [生效] 032 T-04 标签筛选与已收藏互斥回退默认：在已收藏视图选中某标签 chip 时切回「全部」视图并置 `activeTagId`（GET `tagId=`）；在标签筛选激活时点「已收藏」tab 清空 `activeTagId` 并切到已收藏。两者互斥且各自回退，不出现 `favorites=true&tagId=` 组合（对齐 T-02 A-193 `not_combinable` 服务端语义在 UI 侧的单向呈现）。默认理由: 两个正交过滤维度同时生效语义模糊；互斥回退让任一时刻只有一个过滤源，URL/列表/焦点不串。
- A-201 2026-08-11 [生效] 032 T-04 整理模式与搜索互斥默认：搜索输入激活（`searchText.trim()` 非空）时自动退出整理模式并清空选择/批量 picker/确认/错误；整理模式 Esc 在原声 keydown 层 `stopPropagation` 退出并归还焦点给「整理线程」按钮，不冒泡关闭外层抽屉（031 A-180 input-history-panel 分层 dismiss 先例）。默认理由: 搜索把底层列表换成结果集，整理选择即失效；Esc 分层确保内层先消化、外层不被误关。
- A-202 2026-08-11 [生效] 032 T-04 成功通知单通道默认：线程区创建成功与批量整理成功共用一个 `role=status` live region（`batchNotice ?? createNotice`），避免并存两个 polite region 造成读屏乱序；批量成功后退出整理模式、清空选择、`tagsReloadKey`+1 并 `refreshThreadsSilently`（不触发整树 loading/重置）使 chips 计数与列表 tags 静默对齐。默认理由: 单 live region 是 031 搜索/收藏通知的既有模式扩展；静默刷新保持已展示列表不闪烁。
- A-203 2026-08-11 [生效] 032 T-04 强确认对话框语义默认：删除标签与批量整理确认均用 `role=dialog`（非 `alertdialog`）+ `useModalSurface` 分层 dismiss，删除确认不 `aria-hidden` 管理对话框（`hideBackground:false`、`inertRootRefs:[backgroundRef]`），使两层对话框同屏可查、Esc 只关最上层；删除确认初始焦点落在「取消」、批量确认落在「确认应用」。默认理由: 嵌套确认需保留父层可见以维持上下文；`role=dialog` 与现有创建线程对话框及测试可查询角色一致，alertdialog 在此非模态阻断语义。
- A-204 2026-08-11 [生效] 032 T-05 验收段造数/置位/扫描面默认：验收段内聚单块置于 031 搜索段之后、终扫 git/秘密扫描之前（031 A-179 零位移先例，既有断言逐字节未动）；标签名择三个跨夹具零碰撞独特词——发布阻塞/缺陷跟踪/文档待办（legacy-project 经 UI 真实创建）+ 外部标签（031 搜索段所建外部项目经公开 API 造数，隔离探针）；分配/批量/重放/409/跨 tuple 404 探针直接驱动公开 API（真实服务端往返），UI 实核保留给创建/搜索/筛选/多选/确认/删除/焦点/键盘路径；证据四截图（desktop light 筛选、desktop dark 筛选、narrow light 筛选抽屉、narrow dark 筛选抽屉）落 features/032-thread-tags-bulk-organize/evidence/（gitignored），断言与 axe 态入 014 results.json（030 A-168/031 A-179 同例）；秘密扫描面扩展 tagFacingText/narrowTagFacingText 两段 DOM 标签态文本与四截图字节入 publicSurfaces 终扫，标签 API 响应随 productApiBodies 全量入扫外再逐体显式断言无 apiKey/masterKey（031 A-179 先例）。默认理由: 独特词分工使筛选/投影/隔离断言可全等而非包含；UI 与 API 分工让验收既实核交互又保持确定性。
- A-205 2026-08-11 [生效] 032 T-05 useModalSurface 抽屉暂停/恢复双缺陷修复默认（生产缺陷）：窄屏抽屉内打开任一线程对话框会暂停抽屉 modal（threadDialogOpen），验收首轮实核暴露两处恢复缺陷——①焦点：抽屉恢复时初始焦点微任务无条件聚焦抽屉关闭钮，抢走刚关闭对话框经 restoreFocusRef 归还给开启器的焦点（修复=初始焦点微任务先查 `surface.contains(document.activeElement)`，焦点已在 surface 内即跳过——开启器在抽屉内故归还生效；抽屉全新开启时焦点在抽屉外开启器上，初始焦点行为不变）；②滚动锁：抽屉恢复时重新捕获 body overflow，暂停/恢复循环后捕获值陈旧为 "hidden"，关抽屉后页面滚动永久锁定（修复=body overflow 改模块级引用计数 lockBodyOverflow/unlockBodyOverflow，仅首个加锁者记录原值、仅最后解锁者恢复，中间暂停/恢复/分层开关不再互相覆盖）。jsdom 回归钉于 tests/browser/cockpit-shell/cockpit-mobile-integration.test.tsx（抽屉内开管理标签→Esc→焦点归还开启器且 overflow 保持 hidden→再 Esc 关抽屉→焦点归还导航开启器且 overflow 归零），RED 先证（无守卫时焦点落关闭钮）；受影响 tests/browser 20 文件 154 全过。默认理由: 两处缺陷同根（暂停/恢复把「逻辑上仍开着」的抽屉当作全新开启处理）；引用计数把全局唯一的 body 滚动锁从每实例私有捕获改为共享所有权，分层/暂停/恢复全路径语义一致。
- A-206 2026-08-11 [生效] 032 T-05 smoke 输入历史段一次性 flake 归因默认：验收第二轮运行在既有 014 输入历史段「没有匹配的输入历史。」waitFor 遇一次性 60s 超时；该段与本片改动零机械关联（桌面宽、无活动 modal surface、输入历史面板不使用 useModalSurface），零改动重跑即全绿，归因 A-112 已记录的 facts 轮询重渲染与 focus+Enter 步骤竞争类既有不稳定苗头；既有段落未改动，未加重试。默认理由: 单次复现即消失且机制上与改动面无交集，按既有归因先例记录而非对既有段落做机会主义加固；若同段再现将按 A-95 停止条件量化复现率后再处置。
- A-207 2026-08-11 [生效] 033 范围与生命周期形态默认：线程回收站=线程级三段式（软删→回收站恢复→强确认永久删除）；消息级删除/编辑、回收站批量操作、保留期/TTL 自动清理均不在本片；标签回收站不建（032 A-182 已决策）。默认理由: 单一切片单一用户结果（线程生命周期闭环）；线程级粒度是 backlog S-20 演示判据的直接对应。
- A-208 2026-08-11 [生效] 033 软删 schema 形态默认：`collaboration_threads` 增 `deleted_at` 可空列（ISO GLOB CHECK，既有行 NULL 零漂移）+ 部分索引 `collaboration_threads_recycle_bin(project_id,deleted_at) WHERE deleted_at IS NOT NULL`，identity 18→19 直接改 canonical manifest（ADR-0003，无 migration/backfill）；不引入状态机/第二词汇（无 archived/trashed 枚举）。默认理由: 可空标记列是最小语义扩张，排除谓词机械统一；`thread_identity_no_update` 只锁 id/project_id/created_at，UPDATE deleted_at 合法无需动该触发器。
- A-209 2026-08-11 [生效] 033 排除语义默认：软删线程对所有现有缝「即不存在」——thread 级缝统一 ensureActiveThread（已删 404 RESOURCE_NOT_FOUND + details.reason="thread_deleted"），项目级缝（listThreads 三变体/searchProjectThreads/searchInputHistory/listProjectTags threadCount）统一 `deleted_at IS NULL` 谓词；跨 tuple 仍无标记 404（不向跨项目观察者泄漏删除态差异）；回收站是唯一可见已删线程的缝。默认理由: 「各缝各自展示已删态」爆炸半径大且易伪造；统一不存在语义诚实且机械可验；reason 标记仅 tuple 内可见，让当前导航占位文案诚实（已删 vs 不存在）。
- A-210 2026-08-11 [生效] 033 软删/恢复幂等默认：deleteThread/restoreThread receipt-less（025 收藏/032 分配先例）——重复删除返回 `deleted:false`+冻结首删 deletedAt、零第二条审计事件；恢复活跃线程返回 `restored:false`；状态变更同事务递增 version/updated_at。默认理由: deleted_at IS NULL 守卫使操作天然幂等，receipt 表无处锚定额外价值；冻结首删时间让响应重放字节一致。
- A-211 2026-08-11 [生效] 033 运行守卫默认：线程存在非终态 run（status NOT IN ('failed','stopped')）时拒绝软删，409 OPERATION_CONFLICT；failed/stopped 放行（无活 lease，随线程隐藏/还原）。默认理由: 软删进行中的 run 会让 lease/attempt 在不可见线程上继续消耗；终态判定取保守闭集（只有 failed/stopped 明确无后续动作）。
- A-212 2026-08-11 [生效] 033 回收站缝形态默认：独立查询缝 `listDeletedThreads` + 独立路由 `GET /api/projects/[projectId]/thread-recycle-bin`（静态段与 [threadId] 零碰撞，thread-tag-batch 先例），不扩展 listThreads 加 deleted 参数；排序 deleted_at DESC + id 决胜、游标分页、limit 默认 50 上限 100、item 恒在 messageCount/attachmentCount（页面 id 批量第二查询无 N+1，032 tags 投影先例）。默认理由: 回收站排序键（deleted_at）与活跃列表（last_activity_sequence）不同，两视图语义不组合（032 A-186 favorites 互斥同族）；独立缝使「活跃缝永远不见已删」机械成立。
- A-213 2026-08-11 [生效] 033 永久删除确认默认：走 UI 强确认对话框（如实影响面 N 消息/M 附件 + 不可恢复 + 审计保留说明，初始焦点落「取消」，032 A-203 先例），**不走 CAP-GOV-02 审批中心**——029 中心服务异步 pending/approve 生命周期的高风险域（执行/内联决策裁决）；purge 同步原子单事务、前置软删+回收站两道闸门、单 owner 本机操作（032 A-185 同族）。默认理由: backlog 约束「审批=永久删除强确认」由显式 UI 强确认兑现；异步审批生命周期对同步原子动作是纯成本。
- A-214 2026-08-11 [生效] 033 purge 入口默认：永久删除仅自回收站（deleted_at NOT NULL），活跃线程 purge 404；purge 无 operationId receipt——单事务原子 + 重试得 404 明确失败（线程已不存在，无重复业务动作可能），审计事件即不可抵赖记录。默认理由: 两道闸门（先软删进回收站、再强确认 purge）防误删；AGENTS.md「重试必须重放事实或明确失败」由 404 明确失败满足。
- A-215 2026-08-11 [生效] 033 purge 触发器门默认：thread_fact_no_delete/thread_policy_revision_no_delete/thread_policy_member_no_delete 三触发器 WHEN 扩展为「项目存在 AND 无 purge 标记则阻断」；新表 `thread_purge_markers`（PK(project_id,thread_id)、FK→projects CASCADE、无 FK→threads）作事务内瞬时标记（purge 开始插入、提交前删除）；reopen 不变量 +1（markers 空表断言）。默认理由: projects-WHEN 既有模式的级联时序假设从未被生产路径执行过（无 DELETE FROM projects），不应把 purge 正确性押在未验证的 SQLite 级联求值次序上；显式标记确定性强且事务回滚即自清。
- A-216 2026-08-11 [生效] 033 purge 级联序默认：单事务内——tuple+回收站校验 → executions 预检 → INSERT marker → 显式删除序（facts → structured_message_state_heads/state_revisions/blocks → business_action_receipts → inline_decisions → 经 operations-projection `deleteThreadSearchIndexRowsTx` 清搜索索引行，跨 owner 边随 architecture tests 登记）→ 收集附件 storage_relpath → DELETE threads 行级联余图（runs/events/turns/decision_requests/attempts/model_calls/operations/messages/policy_revisions/members/project_sequences/drafts/favorites/tag_edges/input_history/attachment_events/message_attachments）→ 同事务 unlink 附件字节（任一失败整体回滚，024 removeAttachment 先例扩展）→ 重算 next_activity_sequence → DELETE marker → 写 thread_purged 审计事件 → COMMIT。默认理由: 显式删除序使每条语句结束时 NO ACTION 边自洽（facts 先走解除其对 messages/events/revisions/decisions/receipts 的引用）；字节 unlink 入同事务保持「行在则文件在」双向诚实。
- A-217 2026-08-11 [生效] 033 executions provenance 守卫默认：purge 预检 `EXISTS executions WHERE (project_id, source_collaboration_thread_id)` → 409 `fields.threadId="has_executions"`；executions→runs FK NO ACTION 作 DB 兜底；软删允许（行保留，provenance 可解析）。默认理由: 冻结来源 provenance 优先于删除自由（AGENTS.md 不可变历史条款）；预检给出稳定脱敏 409 而非裸 FK 错误。
- A-218 2026-08-11 [生效] 033 序列重算默认：purge 同事务重算 `collaboration_project_thread_sequences.next_activity_sequence = 1+COALESCE(MAX(剩余 facts.activity_sequence),0)`，保持 reopen 不变量精确等式；不建高水位台账表。默认理由: activity_sequence 仅是活数据内排序键（列表游标/事实序），purge 后旧值行已消失、复用无外部可观测性；陈旧游标在剩余行全序上仍确定分页（页边界 quirks 可接受）；精确等式比重算豁免或台账表更小面。
- A-219 2026-08-11 [生效] 033 审计默认：thread_deleted/thread_restored/thread_purged 三事件同事务写 audit_event_outbox（source='public_collaboration'、actor=owner、payload 仅 threadId + 标题经 publicExcerpt 凭据分类+200 grapheme 截断 fail-closed）；`AUDITABLE_COLLABORATION_EVENT_TYPES` 闭集 +3，审计中心 COLLABORATION_EVENT_TYPE_COPY 同波次补标签（域分类正确）；**历史 outbox/projection 行永久保留**——purge 清除协作内容图但不动审计日志（含既有消息摘录），UI 确认文案如实说明「删除操作会记录在审计日志中」。默认理由: backlog「审计=删除/恢复不可抵赖」与 AGENTS.md「保留不可变历史」双重条款；审计摘录有界（≤200 grapheme 凭据分类）且 owner 本机可见，保留价值高于擦除。
- A-220 2026-08-11 [生效] 033 搜索索引默认：软删保留索引行、031 HIT_SELECT 增 `t.deleted_at IS NULL` 读路径排除；purge 同事务显式清行（reopen 不变量 index⊆threads/messages 保持）；consumer/rebuild 零改动（新事件类型的 title hint INSERT OR IGNORE…SELECT 对已 purge 线程零插入；rebuild 全表扫 titles 含软删线程，post-condition `titles===threadCount` 不变）。默认理由: 读路径排除让软删/恢复对索引零写；consumer 对新事件天然安全，投影协议零扩张。
- A-221 2026-08-11 [生效] 033 系统线程默认：当前产品全部线程经 createThread 由 owner 显式创建，schema 无系统标记、项目创建不自动建线程——本片不引入系统线程标记列；架构记录未来约束：任何系统/自动创建线程必须带不可删标记并在删除命令失败关闭。本片真实保护规则=executions provenance 守卫（A-217）。默认理由: 不为不存在的概念预付 schema 面；backlog「系统线程明确处理」由现状声明+未来约束+真实守卫三者兑现。
- A-222 2026-08-11 [生效] 033 UI 形态默认：线程区 tablist 第三视图「回收站」（025 favorites 视图先例扩展）+ 列表项「移入回收站」动作配轻确认（可恢复如实文案）+ 回收站行内「恢复」/「永久删除」+ 强确认对话框（A-213）+ 当前导航占位面板（删除当前线程即清选回项目根；陈旧 URL/他端删除经 detail 404 reason=thread_deleted 落「该线程已移入回收站。」占位，含「恢复线程」/「返回线程列表」）；审计中心/搜索结果导航到已删线程同落占位；防陈旧 epoch+AbortController（029 A-154）、useModalSurface 分层 dismiss（031 A-180/032 A-205）、projectId 切换全量重置、tokens/44px/键盘/全态纪律不变。默认理由: tablist 第三视图复用既有导航范式零新语言；占位面板复用 022/031 诚实占位先例，恢复动作让误删在原地可逆。
- A-223 2026-08-11 [生效] 033 T-01 排除落地默认：统一守卫文件 `active-thread-guards.ts`（`ensureActiveThread` 全守卫 + `threadDeletedNotFound` 行内分支助手）；run 族缝（controlThreadRun/answerThreadDecision/readRunTimeline）与 inline-decision 缝采用「线程守卫优先于子实体行查找」次序——已删线程即使 run/decision/block id 无效也返回 404 + `details.reason="thread_deleted"`；`updateThreadPolicy` 保留缝自有「Thread was not found.」文案并附 reason 标记（与既有断言消息一致）；`thread_purge_markers` 锚写入器 `thread-purge-marker-store.ts`（insert/deleteTx 原语）随 T-01 落盘以满足 writers 架构护栏（每注册表至少一个生产写入者），purge 命令（T-04）组合调用；structured-message-store 不在架构枚举的排除族清单（thread-service/run-service/draft/attachment/favorite/tag/inline-decision）内，本片零改动。默认理由: 守卫优先次序使「thread 级缝永远不见已删」与子实体存在性解耦，排除语义机械一致且对跨 tuple 零泄漏；锚写入器同 032 receipt 锚先例，避免为通过护栏而伪造评审或削弱架构测试。
- A-224 2026-08-11 [生效] 033 T-02 生命周期路由 body 形态默认：`DELETE /api/projects/[projectId]/threads/[threadId]` 不读取、不校验请求体（thread-draft DELETE 先例），严格性集中在 URL（query/fragment/path id 全拒绝）；`POST …/threads/[threadId]/restore` 要求严格空 JSON `{}`——路由层 `requireEmptyObject` 对非对象/数组/null 报 `input:invalid_format`、对未知键逐键报 `unknown`，服务方法不收 rawInput（与 purge 的严格空 JSON 约定同族）。两路由全部响应（成功+错误）带 `cache-control: no-store`，错误体经 `collaborationErrorBody` 保留 fields/reason（032 A-191/A-195 no-store 语义扩展至生命周期写路由）。默认理由: 命令无参数时最诚实的 wire 形态是空对象而非容忍任意载荷；忽略 DELETE body 避免对从不消费的语义假装校验。
- A-225 2026-08-11 [生效] 033 T-02 审计载荷机制与文案默认：线程标题入审计复用既有 `EXCERPT_TEXT_KEYS` 机制（+`title`），不新开键族——凭据分类 fail-closed + 200 grapheme 截断与 answer/question/reason/summary 完全同构（A-219 的机械兑现）；`AUDITABLE_COLLABORATION_EVENT_TYPES` 三类型一次登记（thread_purged 预登记供 T-04 同事务写入）。审计中心文案默认：thread_deleted=「线程已移入回收站」、thread_restored=「线程已恢复」、thread_purged=「线程已永久删除」；`eventDomain` 以 `COLLABORATION_EVENT_TYPE_COPY` 成员资格分类，入 map 即协作域徽章（status-queued），零新分类逻辑。默认理由: 复用既有 excerpt 白名单让「标题永不入原始审计」由同一闭集机械保证；文案如实区分可逆（移入/恢复）与不可逆（永久删除）。
- A-226 2026-08-11 [生效] 033 T-03 回收站查询缝落位与游标默认：`listDeletedThreads` 落 `thread-lifecycle-service.ts`（复用 T-02 已登记的 `threadLifecycleService` 装配命名空间，零新装配边/零新写表），并登记 `PublicCollaborationQueries` 公开查询面。游标与 listThreads 编码同构（A-212）：base64url JSON `{v:1,d,id}` + 字符集/ canonical 回环严格校验，`d`=本页末行 `deleted_at`（ISO GLOB 定长，字典序即时间序），谓词 `deleted_at < ? OR (deleted_at = ? AND id > ?)` 对 `deleted_at DESC, id ASC` 全序取下一页；limit 默认 50 上限 100，未知键/非法游标/越界 limit 复用 INVALID_INPUT fields 词汇（unknown/invalid_format/invalid_range），零新错误码。默认理由: 回收站排序键与活跃列表不同，独立游标类型避免 `{v:1,a,id}` 数字键被误用到时间串语义；严格回环校验让伪造/漂移游标一律 400 而非静默空页。
- A-227 2026-08-11 [生效] 033 T-03 回收站计数与路由形态默认：item 恒在 `messageCount`/`attachmentCount`——两条页面线程 id 批量 GROUP BY 第二查询（collaboration_messages、message_attachments 各一，无 N+1，032 tags 投影先例）；`attachmentCount` 计 tuple 全部 `message_attachments` 行（含 uploaded 未链接行，purge 连字节一并删除，影响面如实）。路由 `GET /api/projects/[projectId]/thread-recycle-bin` 为 `[projectId]` 下新静态段（与 threads/thread-tags/thread-tag-batch 平级，零碰撞）；query 白名单 `{cursor, limit}` 各单值（duplicate/unknown/fragment 400），cursor 路由层不透明透传、编码校验留服务缝（threads 列表路由先例）；全部响应（成功+错误）带 `cache-control: no-store`，错误体经 `collaborationErrorBody` 保留 fields（032 A-191/A-195、033 A-224 no-store 语义扩展至回收站读路由）。默认理由: 计数随页批量取数让投影恒在且如实服务 T-05 强确认影响面；静态段与现有命名同族，Next.js 路由解析零歧义。
- A-228 2026-08-11 [生效] 033 T-04 executions 守卫测试夹具默认：`thread-lifecycle` 的 executions 预检用例在造 `executions` 引用前，若项目缺少 `project_validation_policies` 活跃修订则由测试夹具最小补种一条 `project_validation_policy_revisions`（`created_actor_type='system'`、`entry_count=0`）并绑定 `project_validation_policies.active_revision_id`，避免依赖未声明的外部造数前置。默认理由: 该守卫用例目标是验证 purge 的 409 与 FK 兜底，夹具需先满足 current schema 的策略 FK/不变量，防止因无关前置缺失导致伪失败。
- A-229 2026-08-11 [生效] 033 T-05 回收站占位恢复通知默认：`CollaborationPanel` 保留既有运行选择 `role=status` 活区不改契约，线程已删占位点击「恢复线程」后将同一状态文案同步写入 `runSelectionNotice`（"线程已恢复。"）并保留一次重载窗口，确保当前可访问树首个 status 与用户操作反馈一致。默认理由: 既有测试与可访问树依赖首个 status 可读反馈；不改 run 选择活区结构的前提下，用同源文案同步避免恢复成功提示被旧状态抢占。
- A-230 2026-08-11 [生效] 033 T-06 浏览器验收默认：软删当前线程后的 URL 断言以「不得继续停留在已删 threadId」为准，而非强制 `thread=null`——当前实现会切换到可用线程但不会保留已删选择；占位路径另由陈旧 URL 访问 `?thread=<deleted>` 单独验收。默认理由: 与现网行为一致且仍满足“删除后不再指向已删线程”的核心用户结果，避免把实现细节（清空或回退）误写成需求。
- A-231 2026-08-12 [生效] 033 T-06 purge 字节与去重默认：永久删除验证以“附件行与内容端点不可达（404）”为主，不把“物理文件必删”作为唯一判据；`purgeThread` 仅当 `storage_relpath` 无跨线程引用时尝试 unlink，且对 ENOENT 容忍为已达期望状态。默认理由: 024 的哈希去重语义允许同一字节被多个线程共享，直接把文件存在性当成 purge 成败会把合法共享误判为失败；用户可见契约是 purged 线程附件不可再读取。
- A-232 2026-08-12 [生效] 034 切片边界默认：本片仅实现线程内 owner 消息队列（入队/撤回/重排/受控 steer）与可解释消费，不做跨线程全局队列、队列项编辑历史或自动重排策略。默认理由: 保持单切片单用户结果，先建立 `CAP-COL-04` 的最小可演示闭环，再迭代策略层能力。
- A-233 2026-08-12 [生效] 034 T-01 查询语义默认：`listThreadQueue` 为 thread tuple 只读缝，返回当前线程队列项并按 `position ASC, id ASC` 稳定排序；响应恒带状态字段闭集 `pending|consumed|cancelled`（本票仅落 schema + query，写路径后续票据再引入，初始可只产生 pending）。默认理由: 先锁定可分页/可渲染的稳定读模型与状态词汇，避免后续命令票据在排序与状态语义上漂移。
- A-234 2026-08-12 [生效] 034 T-02 队列命令默认：`enqueueThreadMessage` 与 `cancelQueuedMessage` 都沿用 operationId + request hash 幂等语义（重放同响应、变更请求冲突），并以 thread `expectedVersion` 执行写入冲突保护；撤回仅对 pending 变更状态，已 cancelled 返回稳定幂等成功结果（`cancelled:false`），已 consumed 返回 `ACTION_CONFLICT`。默认理由: 先在不改消息发送主路径前提下建立可预测的队列写语义，供 T-03 消费接缝直接复用。
- A-235 2026-08-12 [生效] 034 T-03 重排与消费接缝默认：`reorderQueuedMessage` 只允许 pending 队列项重排，按 pending 稳定序重映射原 position 集合（保持线程内 position 唯一/连续）并受 `expectedVersion` 保护；运行窗口消费先挂 `startThreadRun` 稳定点，每次进入窗口仅消费一条 pending 头项，在同事务内标记 consumed 并沿既有 owner_message/run 事实流落盘，重复触发通过 operation 重放与状态守卫保证不重复消费同一条。默认理由: 先以最小接缝闭合 CAP-COL-04 的顺序与幂等语义，再在 T-04 扩展 steer 行为而不改基础事务模型。
- A-236 2026-08-12 [生效] 034 T-04 steer 语义默认：`steerQueuedMessage` 只允许 thread tuple 内 pending 队列项执行，并把目标项提升为 pending 队列头；当线程 dispatch 非 `ready`（如另一线程有活动运行、上下文未就绪）时显式 `ACTION_CONFLICT` 失败关闭，不做 silent fallback。默认理由: steer 属高风险控制动作，必须受治理边界约束且语义可解释。
- A-237 2026-08-12 [生效] 034 T-05 验收与迁移波次默认：smoke 队列段中由 API 旁路触发的状态迁移（运行消费队头）一律 reload 后重新展开面板再断言，不把面板隐式自刷新当前提；schema identity 提升必须在同波次迁移所有硬编码 identity 断言与 write-ownership manifest（本次 T-01 漏迁由全量红暴露并补齐）。默认理由: 面板读模型只保证操作后自刷新，跨旁路变更以 reload 为显式契约可避免假性红灯；identity 同波次迁移是 AGENTS.md 既有条款的落地执行。
- A-238 2026-08-12 [生效] 035 T-01 任务域审计事件选型默认：入列 `mission_created`、`task_created/task_started/task_completed/task_failed`（task_events 镜像，复用事件行 id 作 outbox id）、`work_item_created`（单个与批量创建每项一行）、`work_item_status_changed`（fromStatus/toStatus，覆盖 transition/claim/完成门禁/复核定稿/失效重开全部状态投影写点）；不入列：mission/work-item 内容编辑（标题/目标/描述更新）、任务执行 error/result 明细与一切内部噪声。actor 映射：任务生命周期与 owner 看板操作=owner，批量创建/认领=agent，状态投影效果（mark*Tx）=system。默认理由：选型锚定 owner 可观察事实（任务创建、状态流转、运行生命周期、阻塞与完成），内容编辑非状态事实且高频，保持 MVP 最薄且噪声不入列。
- A-239 2026-08-12 [生效] 035 T-01 outbox 摘要与跨源 seq 默认：任务标题/状态文案/工作项标题一律经 grapheme 截断（200）+ 既有 public-text 凭据分类缝，校验失败 fail-closed 落 `[redacted]` 而不阻断业务写；`audit_event_outbox.outbox_seq` 是全源共享单调空间，030 协作套件同波次迁移为按 source 过滤 + 严格递增断言（不再断言单源连续编号）。默认理由：摘要截断与凭据分类复用 030 已验证缝；多源同事务交错使单源连续编号前提失效，严格单调才是跨源契约。
- A-240 2026-08-12 [生效] 035 T-02 审计 UI 任务域呈现默认：域徽标取既有 `.status-label.status-completed`（030 已占 queued/running，freshness 共用变体属既有先例，不新增视觉语言）；文案映射集中 `MISSION_WORK_EVENT_TYPE_COPY` 兼作域分类器——mission_created→使命已创建、task_created/started/completed/failed→任务已创建/已开始/已完成/已失败、work_item_created/status_changed→看板任务已创建/看板任务状态已变更（看板任务对齐 mission-board 词汇以与任务活动 task_runs 区分），未知类型兜底原文并保守归执行域；摘要=任务域事件渲染 payload.title（服务端已 grapheme 截断+凭据分类），空/畸形不渲染，030 messageExcerpt 行为不变；来源定位走规范资源身份路由 catch-all（落 SourceReferencePage 诚实页，不伪造页内跳转——026 focusWorkItemId 为面板内部缝无 URL 入口）：workItemId→`/projects/{id}/tasks/{workItemId}`（复用 memory-source task 形态）、missionId→`missions/{id}`、taskId→`task-runs/{id}`（独立段避免与 work-item tasks/ 段歧义），按 workItemId>missionId>taskId 优先级逐个严格校验（非空字符串+encodeURIComponent），畸形跳过、全无效不渲染链接。默认理由：复用既有路由与徽章词汇零新视觉语言；逐个校验+诚实落地页满足"只渲染可严格校验的引用"且不伪造跳转。
- A-241 2026-08-12 [生效] 035 T-03 验收落点与一致性口径默认：smoke 落点选 `smoke:context`（026 段已造最丰富真实 mission/work-item 事实，无需另起 runner）；task_runs 生命周期经公共 API 补造（create→start→execute），因 deterministic executor 恒完成，task_failed 的 UI 呈现不由 smoke 覆盖、由 jsdom 套件兜底；投影一致性断言按 project 作用域（outbox/projection/API 三者行数相等），checkpoint==maxSeq 用全局口径——第二项目 Empty Mission 的 outbox 行合法共存于全局表；秘密扫描面对任务域 facing text 只扫 fixture secrets（项目页合法渲染白名单工作区路径），截图字节加扫宿主路径（压缩像素不可能含页面文本）。默认理由：复用现有 runner 与造数最薄；按 project 作用域对齐 API 查询语义，避免把跨项目合法共存误判为泄漏。
- A-242 2026-08-12 [生效] S-51 排期默认：整体 UI 改版切片（DESIGN.md 设计基座 + 应用壳层）追加到既有在途切片之后排队，不在本阶段与并行开发同时实现，避免共享 `app/tokens.css`/组件面/证据目录的写冲突；已确认 DESIGN.md（Stitch 格式）作为设计令牌单一事实源方向，视觉方向、覆盖范围与布局模型仍在 grill 前沿（Q1/Q2/Q3/Q5）待用户确认。默认理由: 用户明确指示排队追加；同一共享仓库并行改动同一设计面会产生冲突。
- A-243 2026-08-12 [生效] DESIGN.md 产品级落位默认：DESIGN.md 是产品级设计契约（与 AGENTS.md 并列的根目录文件），采用 awesome-design-md 的 Apple DESIGN.md 原文件逐字落盘于仓库根目录，不随任一特性目录存放；035 后续设计工作以根目录 DESIGN.md 为准，特性内只保留该文件的派生案例/规格证据。默认理由: 用户明确指示「design.md 应该是产品级的，而不是这个 feature」；设计语言跨所有切片生效，放在特性目录会让其他切片与设计 agent 无法读取。
- A-244 2026-08-12 [生效] S-51 解除排队默认：用户 2026-08-12 指示「自动完成这个特性的开发」，覆盖 A-242 的等待语义，本切片立即按 auto 模式推进主链至 ship；实现只触碰设计面（`app/tokens.css`、`app/cockpit.css`、应用壳层、共享公共组件、相关测试与证据），不触碰领域模块、schema 与安全边界，降低与其他在途切片的冲突面。默认理由: 用户最新指令优先于此前排队决定；设计面改动可被 tokens/组件测试与全量回归兜底。
- A-245 2026-08-12 [生效] 设计契约默认：根目录 `DESIGN.md`（Apple-design-analysis 原文件）为产品级设计令牌/组件规范单一事实源；`app/tokens.css` 是其 CSS 投影，并承载产品扩展 token（状态色、Agent 身份色、布局/控制尺寸、安全区）。扩展 token 不与 DESIGN.md 冲突时由 tokens.css 单独定义并记录于 spec；DESIGN.md 文件本身保持原样。默认理由: 用户要求 DESIGN.md 原封不动落盘为产品级契约；产品扩展 token 无法也不应塞进 Apple 营销站分析文件。
- A-246 2026-08-12 [生效] 视觉方向默认：按根目录 DESIGN.md 实施——单一交互 accent（Action Blue #0066cc，暗面 #2997ff）、近黑 ink 文本、白/羊皮纸/近黑交替表面、display 负字距排版、chrome 无阴影、浮层单一阴影族、无装饰渐变与玻璃拟态；既有暖色 Cool 身份（D-18/A-67/A-18 的色板条款）由本特性替换，待 ship 演示验收确认后正式生效；Agent 身份色与语义状态色作为扩展 token 保留且不单独依赖颜色表达状态。默认理由: 用户以「原封不动落盘 + 产品级 + 自动完成」表达以该 DESIGN.md 为实施契约；色板替换属可逆选择，演示验收仍可驳回回滚。
- A-247 2026-08-12 [生效] 布局模型默认：保留既有桌面三栏 + 窄屏单抽屉的 IA（D-9/A-17），只重构壳层视觉、密度与层级；新窄屏导航模型、全局消息 redesign 属范围外（018 曾列为未来项，不随本切片实现）。默认理由: IA 已是用户确认的产品结构，改版不推翻信息架构是可控边界。
- A-248 2026-08-12 [生效] 覆盖范围与首批面默认：全部页面统一到新 token 体系；本切片首批只收敛应用壳层（activity rail、侧栏、线程头、上下文面板框架）与共享公共组件（button/status chip/panel/message block/approval card/composer 及其三态），业务面板的组件级重构随后续切片逐面收敛。默认理由: 单一可演示用户结果「设计语言落到壳层与公共组件」，符合切片规模护栏（3–8 票）。
- A-249 2026-08-12 [生效] 设计资产落位默认：用户指示将根目录 `DESIGN.md` 与「暖陶工作台」UI 案例 `cool-ai-design-md-case.html` 归置到 `product/ui/`，作为后续 UI 设计的约束与参考目录；`DESIGN.md` 内容保持原样（A-245），仅变更落位，A-243 的「根目录」表述随之更新为 `product/ui/`。默认理由: 用户明确要求把设计约束从特性证据目录与根目录收敛到产品级 `product/ui/`，单一落位便于后续切片引用且避免证据目录被 gitignore 排除。
- A-250 2026-08-12 [生效] 036 T-01 项目域审计事件选型默认：入列 `project_created`、`workspace_bound`/`workspace_rebound`（同 workspaceKey 重申属幂等噪声不入列）、`member_joined`/`member_removed`（replaceMembers 每成员一行，移除按 agentId 升序先于加入，共享事务 joinedAt 时间戳）、`validation_policy_changed`（仅 outcome=saved）；不入列：系统播种空策略修订（initializeValidationPolicy 不产生审计行）、rejected 策略保存（未改变事实）、只读浏览与查询等内部噪声；actor 恒为 owner（四处写点均为 owner 操作）。默认理由：锚定 owner 可观察配置事实，沿用 035「状态事实入列、内容与噪声不入列」纪律，保持 MVP 最薄。
- A-251 2026-08-12 [生效] 036 T-01 project_validation_policy_audits 取舍默认：复用镜像——audits 表保持领域事实与既有查询数据源，appendAudit 在 saved 分支同事务把白名单字段（entryCount/policyHash/revisionNo/warningAccepted）镜像进 audit_event_outbox 并复用审计行 id 为 outbox id（035 task_events 镜像先例）；不让 consumer 直读 audits（consumer 协议零改动是本片硬约束）。默认理由：outbox 是唯一审计喂给缝，payload 从审计行派生而非另写第二份事实，避免双写语义漂移。
- A-252 2026-08-12 [生效] 036 T-01 宿主路径脱敏默认：workspace_bound/rebound 的 payload 只携带 workspaceName/previousWorkspaceName = canonical 路径末段目录名（`/` 与 `\` 双分隔符切分），再经 grapheme 截断（200）与既有 public-text 凭据分类缝，无末段或校验失败 fail-closed 为 `[redacted]`；完整宿主绝对路径绝不进入 outbox/投影；验证策略条目的 executable/workdir（同为宿主路径）不入白名单，payload 只留 policyHash/revisionNo/entryCount/warningAccepted。默认理由：目录名是 owner 在项目 UI 已可见的最小定位信息；「末段提取 + 分类校验」双闸满足完整路径零泄漏的 fail-closed 要求。
- A-253 2026-08-12 [生效] 036 T-01 identity 21→22 同波次迁移范围默认：current-schema identity 与 audit_event_outbox.source CHECK（audit_event_projection.source 无 CHECK 无需动）、13 个测试文件 23 处 user_version 断言、unsupported-schema 夹具 legacy 并集扩至 21 且 rejection 套件 future identity 22→23；035 mission-audit-outbox 套件因 createProject 新增 project_created 行适配为按 source 过滤读取 + seq 精确偏移（A-239 跨源契约在本波次的落地）。默认理由：A-237 同波次纪律；source 过滤读取是共享 outbox 空间下各 source owner 套件的既定形态。
- A-254 2026-08-12 [生效] 036 T-02 审计 UI 项目域呈现默认：域徽标取裸 `.status-label` 中性基类（030=status-queued 协作、执行=status-running、035=status-completed 已占，唯一剩余 status-failed 为 danger 语义对中性域徽标不合适；裸基类有 review-material/thread-policy/collaboration 面板既有先例，零新视觉语言）。文案映射集中 `PROJECT_WORKSPACE_EVENT_TYPE_COPY` 兼作域分类器（与既有三域键零冲突，未知兜底原文并保守归执行域）；「验证政策已变更」对齐 validation-policy-panel 既有词汇（非规格的"策略"措辞），政策摘要 `修订 #N · M 项` 同面板措辞。定位=项目规范身份路由 `/projects/{projectId}`（标签「定位来源项目」）：勘察确认 workspace/成员/政策子面板均无 URL 入口（018 message focus 面板内部缝先例），审计 API 已按项目作用域过滤且 DTO 不携带 projectId，故链接锚定面板 prop 而非 payload 键，非空严格校验、畸形不渲染。摘要按 T-01 白名单键逐字段严格校验（非空字符串/安全整数），畸形或空字段不渲染 excerpt，execution 域携带同名键也不渲染。默认理由：复用既有路由与徽章词汇零新视觉语言；逐字段校验满足「只渲染可严格校验的引用」且不伪造跳转。
- A-255 2026-08-12 [生效] 036 T-03 验收口径默认：smoke 落点 smoke:context 复用 035 段同文件，四域混合 trail 下既有段断言同步按域分类器分区；投影一致性沿用 project 作用域口径（A-241）；实现 subagent resource_exhausted 后由主会话接管验证收口，不视为新实现批次。默认理由: 同文件同面板验收段集中可降低 runner 维护面；主会话只补验证与记录，不改实现语义。
- A-256 2026-08-12 [生效] 编号双占事后处置默认：2026-08-12 两个并行会话各自推进切片——本会话审计纵切（035-mission-work-audit-events/S-51、036-project-workspace-audit-events/S-52）与另一会话整体 UI 改版（035-design-md-ui-redesign/S-51）——推送集成时发现 035/S-51 双占且 assumptions 连续撞号（先 A-242~A-247、改号后又撞对方新增 A-249）；处置=不改写任何已推送历史（不重命名已推送特性目录、不改已推送提交信息），仅改号未推送侧的假设（审计 036 最终落位 A-250~A-255）、backlog 双侧加消歧注记、后续新特性号/片号从 037/S-53 起继续递增；集成侧对 rebase 后整树补跑全量+受影响冒烟再推送。默认理由：改写已推送历史违反 git 纪律且成本远高于目录号美观；台账消歧 + 递增续号可恢复唯一性。
- A-257 2026-08-12 [生效] 并行 UI 改版遗留修复默认：集成时发现远端 035-UI 切片推送态全量套件红 5 例——(1) theme-activity-bar 写入失败用例改用 localStorage 实例属性补丁在 jsdom 下不拦截（探针实证：实例补丁 0 次命中、Storage.prototype 间谍 1 次命中且主题保持明色），回退为原型间谍；(2) theme-hydration 暗色 --surface-main 期望值随新 token 链更新为 #272729（--color-surface-tile-1）；(3) review/collaboration 可访问性套件改为解析 var() 链并对齐 visual-tokens 规范 AA 矩阵（text-subtle 对 surface-panel ≥3.0、danger 对 status-danger-surface ≥4.5）；(4) 真实 AA 回归：DESIGN.md 的 ink-muted-48(#7a7a7a) 定位是禁用/法律小字，被映射为 --text-subtle 后用于常规次级文本仅 4.30:1，亮色改直值 #666666、暗色 #98989d（theme-tokens 钉值同步更新，DESIGN.md 原文不动）；(5) 暗色 --interactive-soft/soft-hover 绿底配 #2997ff 蓝字仅 ~3.7:1，改中性 tile 色 #272729/#3a3a3c。另发现 cockpit.css 仍引用未定义的 --surface-muted/--border（声明静默失效，非 axe 阻塞），留待后续 UI 收敛切片处理。默认理由：WCAG AA 与 axe 零阻塞是硬门禁，优先以最小改动恢复绿态并保留 DESIGN.md 契约字面。
- A-258 2026-08-14 [生效] 038 色板事实源：以 `product/ui/cool-ai-design-md-case.html` 暖陶色板为产品视觉单一事实源，重写 `product/ui/DESIGN.md`；Apple 原文另存 `product/ui/archive/apple-design-analysis.md` 作历史附件，不再驱动 tokens。默认理由: 用户 grill Q1=A，要求严格按 case 颜色；令牌必须有单一事实源。
- A-259 2026-08-14 [生效] 038 覆盖面：本片一次对齐项目驾驶舱三栏视觉（暖陶 token + `56px 236px 1fr 304px` 栅格 + 左 Thread 目录 / 中群聊 / 右看板 chrome）；设置、引导、复核工作区只吃新 token、不重排。默认理由: 用户 grill Q2=A；单一可演示结果是 case 中的驾驶舱。
- A-260 2026-08-14 [生效] 038 触控目标：布局、色板、圆角、栏宽严格跟 case；可点击目标保持 `--control-min: 2.75rem`（44×44），视觉可更紧但热区不缩小。默认理由: 用户 grill Q3=A；WCAG / AGENTS.md 44px 底线优先于 case 的 40px 按钮。
- A-261 2026-08-14 [生效] 038 窄屏：桌面严格跟 case 四列；窄屏继续现有抽屉（项目 / 编辑 / 上下文），不改成 case 的 980px 堆叠或 620px 隐藏侧栏。默认理由: 用户 grill Q4=A；窄屏换导航是独立切片。
- A-262 2026-08-14 [生效] 038 票据粒度与推进：5 张票（T-01 契约 → T-02 壳层栅格 → T-03 对话/群聊 → T-04 右侧看板 → T-05 验收）；用户以 AAAAA 确认 grill 且原指令为严格按 case 实现，项目级 review 豁免后直接进入 implement，不另等票据确认。默认理由: 覆盖面已锁定为单一驾驶舱结果；5 票低于 8 票拆片阈值。
- A-263 2026-08-14 [生效] 038 ship 自动收口：用户指示「自动完成长任务开发，完成后提交并 push」，覆盖 interactive 演示等待；演示验收记 auto-approved 2026-08-14，下次交互须主动呈上驾驶舱与 preview。默认理由: 用户明确要求 auto 完成并推送；视觉已有 case 对照与 T-05 证据。
- A-264 2026-08-14 [生效] 039 打开文件夹即 Project：owner 提交本机已存在、可读取的绝对目录后，按 verified-handle 规范路径 create-or-resume 一个 Project；显示名取目录 basename（盘符根等空 basename 时用去尾分隔符后的路径文本）。默认理由: 用户要求对齐 Codex/Cursor「打开的文件夹就是项目」，且不得先填项目名。
- A-265 2026-08-14 [生效] 039 恢复语义：同一 `workspace_key`（Windows 上规范路径大小写折叠）已绑定则返回该 Project，不新建、不报 `WORKSPACE_ALREADY_BOUND`。默认理由: Codex/Cursor 再打开同一文件夹是恢复，不是复制空项目。
- A-266 2026-08-14 [生效] 039 打开动作形态：本片不引入系统文件夹对话框或 Electron；浏览器 File System Access API 不能给出 verified-handle 所需 Windows 绝对路径，Next 路由弹 FolderBrowserDialog 在无头/CI 下不可测且可能挂起。owner UI 为「文件夹路径」输入 + CTA「打开文件夹」。默认理由: 安全边界优先于原生拾取器；拾取器可另立项。
- A-267 2026-08-14 [生效] 039 绑定根：打开的目录就是 Project 的唯一绑定根，不向上走到最近 `.git`。默认理由: owner 显式选择该文件夹；静默提升到 git 根会绑错目录。
- A-268 2026-08-14 [生效] 039 单根：Codex 多文件夹 / `--add-dir` / 附加目录本片范围外；继续 `CAP-PWS-02` 单绑定根浏览。默认理由: 规模护栏，单一用户结果。
- A-269 2026-08-14 [生效] 039 夹具保留 `createProject(name)`：测试与内部造数仍可创建未绑定 Project；owner 入站 `POST /api/projects` 改为只接受 `{ path }`，UI 删除名称创建表单。默认理由: 避免一次性改写全部领域夹具，同时关闭 owner 名称创建路径。
- A-270 2026-08-14 [生效] 039 零 schema bump：复用 `projects.workspace_key` 查找与既有 bind 列；不改 canonical schema identity。默认理由: 查找缝已存在。
- A-271 2026-08-14 [生效] 039 期间 037/S-53 AUD-GOV 继续暂停于 T-02，本片不混入治理 outbox。默认理由: 同一时间只处理一张前沿票/一个用户结果。
- A-272 2026-08-14 [生效] 039 票据：4 张纵向票（命令 → API → UI/引导 → 冒烟验收）；项目级 review 豁免后 auto 进入 implement。默认理由: 单一「打开文件夹即进入项目」结果，低于 8 票拆片阈值。
- A-273 2026-08-14 [生效] 039 创建信封保持 `{ project: { id, name, createdAt } }`，以满足既有 `parseProjectCreateEnvelope` 单键严格解析；HTTP 201=新建、200=恢复。默认理由: 不扩大引导解析面。
- A-274 2026-08-14 [生效] 039 引导：project-select CTA 改为打开文件夹；打开成功且工作区已 ready 时仍可进入既有 workspace 步骤（改绑/继续），不删除 workspace 引导。遗留未绑定项目仍走 WorkspaceSetup。默认理由: 最小改动既有引导状态机。
- A-275 2026-08-14 [生效] 039 产品文案：同步 README/getting-started/project-workflow 中「先创建项目再绑工作区」的步骤，改为打开本地文件夹。默认理由: 用户可感知路径与文档必须一致。
- A-276 2026-08-14 [生效] 040 与 039 拆片：未选项目的单 Agent 聊天是独立用户结果（S-56）；039 打开文件夹立项暂停于 T-01。默认理由: 规模护栏禁止把两个可单独交付结果打成一片；用户最新指示覆盖空态。
- A-277 2026-08-14 [生效] 040 不新建 DirectChat 领域：个人对话复用 Project + Thread + Collaboration Run；Thread 仍属 Project。默认理由: 消息已要求 ownership tuple；新建实体会爆 schema 与 8 票阈值。
- A-278 2026-08-14 [生效] 040 个人对话容器允许 1 名项目成员；已绑定工作区的协作项目仍要求 ≥2 名成员（`replaceMembers` too_small 不变）。默认理由: 用户明确禁止无项目时多 Agent 群聊；组队不变量只对文件夹项目保留。
- A-279 2026-08-14 [生效] 040 无已配置 Agent 时，中栏空态引导去团队设置配置 Agent，不渲染可发送的假聊天。默认理由: 没有对端就不能聊；与 S-11 Provider/Agent 前置一致。
- A-280 2026-08-14 [生效] 040 不改引导主链、不实现打开文件夹、不删名称创建表单（039 范围）。默认理由: 单一用户结果是 `/` 无项目时的 1:1 聊天列。
- A-281 2026-08-14 [生效] 040 零 schema bump：个人对话容器是名称恰好为「个人对话」、且 `workspace_path` IS NULL 的单例 Project，由 `ensureDirectProject` 幂等创建。默认理由: identity 23 不为本片再 bump；查找缝可用现有列。
- A-282 2026-08-14 [生效] 040 只抄主流 Agent WebUI 的信息架构（中间永远是 chat、左侧是 session、默认单 Agent），不把 pi-agent / pi-outpost / pi-web-ui 源码或品牌资产拷进本仓库。默认理由: 版权与 AGENTS.md 禁止复制第三方品牌资产；GitHub clone 本机 443 失败。
- A-283 2026-08-14 [生效] 040 个人对话禁用使命看板、执行面板、线程成员策略改成多人、@其他 Agent 交棒；composer 只派给当前选中的那一名 Agent。无工作区则执行失败关闭（既有）。默认理由: 用户禁止无项目多 Agent 协作。
- A-284 2026-08-14 [生效] 040 票据 5 张（成员例外 → home 命令/API → 中栏聊天 → 左栏对话列表/约束 → 冒烟），auto 进入 implement。默认理由: 低于 8 票阈值。
- A-285 2026-08-15 [生效] 039 与 040 同时交付：owner 可以打开本地文件夹作为协作项目，也可以不选项目直接 1:1 聊天。A-276 的「039 暂停」与 A-280 的「040 不实现打开文件夹」被本条覆盖；两片仍分票实现，但不互相替代。打开文件夹不得把「个人对话」单例改绑成文件夹项目。默认理由: 用户澄清两条路径都要支持。
- A-286 2026-08-14 [生效] 选择性评审默认：用户指示「按建议优化」，2026-08-09 的「全部 review 豁免」修订为选择性评审——spec/architecture/`hf-review` 仍默认豁免；恢复 `hf-code-review` 代码门，凡命中 schema 变更、凭据/沙箱/verified-handle 安全边界、跨 owner 写/共享 outbox/投影写入点、或 >8 票的切片，implement 后必须执行（至少 Standards 轴）；纯 UI/机械/文档切片继续豁免。不得伪造评审结论或确认行；豁免在 progress.md 记录。默认理由：015/016 的教训证明评审抓到的都是测试盲区，schema/安全/跨 owner 面恢复代码门性价比最高，纯 UI 面保留豁免节省时间（AGENTS.md「当前开发阶段的评审策略」）。
- A-287 2026-08-14 [生效] 机械性改动 RED 豁免默认：identity/断言号同步、测试夹具补字段、纯配置/纯文档、无行为变化的迁移性编辑可豁免「行为性 RED」，以验证/回归测试收尾并在票或 progress.md 记录理由（先例 007 纯文档、032 T-01 identity 同步）；行为性功能改动不得伪装成机械改动绕过 RED。默认理由：027 T-02（五轮 RED/GREEN）与 032 T-01（三轮）中大量轮次是机械同步空转，不产生行为验证价值（AGENTS.md TDD 节 + hf-tdd「机械性改动豁免」）。
- A-288 2026-08-14 [生效] 轻量级切片路径默认：任务票 ≤3、不触碰 schema/安全边界/跨 owner 写、单一模块/接缝的切片，合并 to-spec 与 to-architecture 为一页 architecture.md（含规格要点与测试缝），跳过 to-tickets（任务票列于 progress.md 或 tickets.md 简表）；验收门禁不缩减；超出条件必须走完整主链。默认理由：小切片的全套文档与拆票开销大于其设计价值，分级简化可省文档/交接成本（AGENTS.md「轻量级切片」）。
- A-289 2026-08-14 [生效] subagent 时间预算浮动默认：默认 30 分钟改为按切片类型浮动——领域/机械/查询类 30 分钟，UI、浏览器验收与集成类 45～60 分钟（验收段写入量大允许 60 分钟）；超出必须停下报告并拆段。默认理由：035/036 验收段出现 resource_exhausted 中途交接损耗，UI/验收类切片需更长连续窗口（AGENTS.md「时间预算与失控保护」）。
- A-290 2026-08-15 [生效] 037 T-02 `approval_requested` 域碰撞分类默认：`eventDomain` 接收完整事件并检查 payload 自有键；带 `attemptNo` 归执行域，不带归治理域，其余四种 approval 生命周期类型按治理文案闭集归治理域。默认理由: Safe Execution 的 `appendExecutionAuditOutboxRow` 恒注入 `attemptNo`，Governance 的 `appendGovernanceAuditOutboxRow` 永不注入该键；事件类型单独不足以区分两个 owner。
- A-291 2026-08-15 [生效] 037 T-02 治理域徽标复用裸 `.status-label` 中性基类，与项目域同形；不使用唯一剩余的 `status-failed` danger 变体。默认理由: 治理是中性来源分类，不是失败状态；复用既有基类满足零新视觉语言且避免误传危险语义。
- A-292 2026-08-15 [生效] 037 T-03 浏览器验收落点选择 `smoke:execution`：复用 029 已有真实命令审批路径产生 Governance requested/approved/consumed/expired 四类事实，不新增第四轮完整 Agent execution；该 runner 的拒绝动作只裁决 Public Collaboration inline proposal，不能伪装为 Governance `approval_rejected`，故 rejection 由 T-01 `governance-audit-outbox.test.ts` 证明，smoke 在未来出现治理 rejection 时条件断言其呈现。默认理由: 保持真实 owner 写路径与 source ownership，不为“五类”字面造假或显著拉长长任务。
- A-293 2026-08-15 [生效] 028/037 execution smoke 的审计数量一致性改按当前 `project_id` 比较 outbox/projection/API，checkpoint==maxSeq 仍按全局 consumer 口径；home direct chat 会合法创建第二 Project，其两条 project_workspace outbox 不属于当前 execution 项目。默认理由: Audit API 是项目作用域查询，拿全局 outbox 与单项目 API 比较会把合法跨项目事实误报为投影缺失。
- A-294 2026-08-15 [生效] 041/S-57 AUD-RUN 只入列项目作用域 Runtime HTTP session（`callOpenAiChat` 成败），不入列全局 Provider verify（无 project_id，审计 API 是项目作用域）。默认理由: 与 CAP-OPS-02 项目隔离查询对齐；verify 属设置/健康面，交给后续 CAP-OPS-03。
- A-295 2026-08-15 [生效] 041 不夺取 `collaboration_model_calls` / `execution_model_calls` / `review_model_calls` 事实所有权；Runtime 只追加 `source='runtime'` outbox 信封。默认理由: write-ownership notes.model_calls 已声明用量事实归领域 owner；AUD-RUN 建立的是 CAP-RUN-07 session 可观察性，不是第二套 call 表。
- A-296 2026-08-15 [生效] 041 事件选型仅 `runtime_call_succeeded` / `runtime_call_failed`；不入列 started 噪声、token 明细、prompt/响应。payload 白名单 `surface, model, errorCategory` 加严格校验的导航 id。默认理由: 030 已把 model_call_* 当协作噪声；token 已有执行/协作用量事件；秘密零泄漏。
- A-297 2026-08-15 [生效] 041 三张票（outbox+schema → UI → smoke+code-review）；auto grill 默认；spec/architecture 豁免；因 schema 与共享 outbox 必须 hf-code-review。默认理由: 对齐 037 纵切模板且遵守 A-286 选择性代码门。
- A-298 2026-08-15 [生效] 041 T-03 浏览器验收落点继续使用 `smoke:execution` 的既有真实 `callOpenAiChat` 成功路径；该 runner 不注入 Provider/model fault，因此浏览器段必须断言 `runtime_call_succeeded`、source=`runtime` 数量与项目/全局投影边界，并在未来自然出现失败行时同时断言两类；本片不为制造 `runtime_call_failed` 新增第四轮完整 Agent execution，失败事件继续由 T-01 `runtime-audit-outbox.test.ts` 的同事务成功/失败、白名单与秘密脱敏用例证明。默认理由: 保持真实调用路径并遵守验收时长护栏，避免只为覆盖字面事件类型重复昂贵执行。
- A-299 2026-08-15 [生效] 041 code-review 对 Review slice 未持久化调用路径的裁决：没有同事务持久化 `review_model_calls` 领域调用行，就不追加 Runtime outbox；本次不为这些路径新增领域调用行，也不从 HTTP helper 或后续事务旁路写审计。默认理由：Runtime outbox 必须与既有领域 call 事实同事务提交，不能为了补审计覆盖而建立第二套调用事实或制造无法原子回滚的孤立审计行；维持 A-295 的所有权与事务纪律。
- A-300 2026-08-15 [生效] 042/S-58 AUD-UI 只在审计面板内按域做客户端筛选，不改审计 API、不加 URL 查询参数、不做时间轴/导出。默认理由: S-23 最终组合片的独立结果是「筛选已交付来源」；查询协议已能返回全部来源，筛选是 UI 状态。
- A-301 2026-08-15 [生效] 042 T-02 浏览器验收落在 `smoke:execution` 既有运行时审计段：断言「运行时」筛选 44px、选中态、运行时行保留与协作行隐藏；不为筛选再开一轮完整 Agent execution。默认理由: 遵守验收时长护栏，筛选是已加载列表上的 UI 状态。
- A-302 2026-08-15 [生效] 043/S-26 SOP 来源仅绑定根下 `features/<slug>/progress.md`（最多 20、路径排序）；不扫描 `.github/workflows` 或根 `SOP.md`。默认理由: 演示判据是仓库真实流程文件；HarnessFlow progress 是本产品已存在的 SOP 形态，GitHub Actions 执行状态会变成第二套外部状态机。
- A-303 2026-08-15 [生效] 043 零 schema、零 outbox；投影每次实时读取；DTO 带 `readAt`。默认理由: 不建立第二状态机，审计追溯靠来源路径与读取时间而非新事件。
- A-304 2026-08-15 [生效] work item 与 SOP 的匹配为标题包含 feature slug 或 `- 特性:` 名称的子串；无匹配不视为陈旧。默认理由: 无稳定外键，避免发明 SOP↔任务链接表。
- A-305 2026-08-15 [生效] 陈旧仅两种：文件不可读/非文本/敏感遮蔽；或声明阶段 `done` 与匹配任务完成态不一致（文件 done 但任务未全 done，或文件非 done 且任务已全 done）。默认理由: 只对照已有 work item 状态词，不解析更多流程引擎。
- A-306 2026-08-15 [生效] API 不返回文件正文，只返回相对路径、声明阶段、匹配任务；宿主路径脱敏。默认理由: 凭据与流程文件正文不属于公开投影。
- A-307 2026-08-15 [生效] 043 浏览器验收落在 `smoke:context` 既有工作区多种一棵 `features/demo-sop/progress.md`，不新开 Agent 执行。默认理由: 验收时长护栏。
- A-308 2026-08-15 [生效] 未绑定工作区时 GET sop-state 返回 200、`workspaceBound: false`、空列表，而不是浏览 API 的 404。默认理由: 使命看板在未绑定时仍可渲染 empty，避免把绑定缺失当成资源不存在。
- A-309 2026-08-15 [生效] 044/S-27 work item 租约 TTL 默认 15 分钟；心跳将 `lease_expires_at` 设为 now+TTL。默认理由: 与现有 execution/review lease 同量级，避免过短导致误回收。
- A-310 2026-08-15 [生效] 044 仅允许回收已过期租约；未过期拒绝。过期回收不走审批中心。默认理由: backlog「回收高风险运行需确认」针对执行合入；work item 过期回收只把任务退回 todo，不是破坏性 Git。
- A-311 2026-08-15 [生效] 044 不新增 audit outbox 类型；释放/回收复用既有 work item 状态变更事件。默认理由: 不建第二事件源；AUD-MWK 已覆盖 status_changed。
- A-312 2026-08-15 [生效] 045/S-28 记忆检索直接查询 `memory_entries`，不新增 schema/索引表。默认理由: 记忆已是项目隔离不可变事实；独立 FTS 索引与 checkpoint 是 S-29 的用户结果，本片只交付「搜到并定位」。
- A-313 2026-08-15 [生效] 记忆搜索默认只返回 active（无 superseding child）；不提供 search 的 includeInactive。默认理由: 演示判据禁止被替代事实的误导性展示。
- A-314 2026-08-15 [生效] 匹配用 `instr(lower(content), lower(q))` ASCII 折叠 contains，不用 LIKE 通配。默认理由: 与线程搜索一致，避免 `%`/`_` 注入；中文子串可接受。
- A-315 2026-08-15 [生效] GET `/api/projects/:projectId/memories/search` 必填 `q`（1～200 grapheme），可选 type/sourceType/version/limit；未知 key 拒绝；默认 limit 20、最大 50。默认理由: 对齐线程搜索校验纪律。
- A-316 2026-08-15 [生效] 「知识动态」即共享记忆列表；检索 UI 放在该 tab，命中滚动到既有 `#memory-{id}`，来源用已有 href。默认理由: 不建第二动态时间线。
- A-317 2026-08-15 [生效] 045 浏览器验收落在 `smoke:context` 既有 5 条记忆上，不新开 Agent 执行。默认理由: 验收时长护栏。
- A-318 2026-08-15 [生效] S-28 原 backlog 阻塞 `CAP-PWS-02`/`CAP-OPS-01` 已交付；本片不新写 verified-handle，文件证据跳转复用已有 source href。默认理由: 准入以 Capability 现状为准，不沿用过期「规划中」字样。
- A-319 2026-08-15 [生效] 专用记忆 FTS 索引、checkpoint、健康诊断与重建留给 S-29。默认理由: 切片规模护栏，单一用户结果。
- A-320 2026-08-15 [生效] 045 走轻量级路径（3 票、零 schema、无跨 owner 写）。默认理由: 命中 AGENTS 轻量级全部条件。
- A-321 2026-08-15 [生效] 046/S-33 能力画像只派生自 Agent/Skill/工具/复核/模型配置，不含任务完成数或复核质量。默认理由: 结果统计属 S-37；本片用户结果是可解释声明能力与只读路由建议。
- A-322 2026-08-15 [生效] S-33 原 backlog 阻塞 `CAP-OPS-01` 已交付，本片不消费审计 outbox。默认理由: 画像证据来自配置身份而非审计事件。
- A-323 2026-08-15 [生效] Identity Adapter 不查询 `work_items`；GET 路由组合已公开 membership/agent/mission 查询再调用纯函数。默认理由: 保持单一写 owner；读也走公开缝。
- A-324 2026-08-15 [生效] 「接受」只预填已有负责人控件且不自动保存；「忽略」仅会话 UI。默认理由: 推断不得写 Agent 配置，也不得偷偷改任务事实。
- A-325 2026-08-15 [生效] 路由打分规则固定为规格所列加权子串；不调用模型。默认理由: 可解释、确定性、无额外 Provider 费用与时长。
- A-326 2026-08-15 [生效] 046 浏览器验收落在 `smoke:context` 既有 Agent 与任务上。默认理由: 验收时长护栏。
- A-327 2026-08-15 [生效] 047/S-39 时间轴只读已有 `audit_event_projection`，读路径先 catchUp，零 schema。默认理由: 不建第二事件源；轨迹是审计投影的另一种排序/过滤。
- A-328 2026-08-15 [生效] 指定 missionId 时只包含 payload.missionId 精确相等的事件，不把无使命字段的项目事件猜进该使命。默认理由: 缺失来源不补造。
- A-329 2026-08-15 [生效] 去重保留最小 outbox_seq。默认理由: 第一次成为公开事实的那条是时间轴上的唯一代表。
- A-330 2026-08-15 [生效] 无可定位身份时 `sourceMissing=true` 且无 href，文案「来源缺失」。默认理由: 演示判据禁止补造事件或假跳转。
- A-331 2026-08-15 [生效] 047 浏览器验收落在 `smoke:execution` 既有审计段。默认理由: 该 runner 已有真实审计行；避免再开 Agent 执行。
- A-332 2026-08-15 [生效] S-39 原 backlog 阻塞（source producer、OPS-01、MWK-02）已交付。默认理由: 以 Capability 现状为准。
- A-333 2026-08-15 [生效] 047 时间轴 GET 仅首页 + limit（默认 50、最大 100），不实现 beforeSeq 正序游标。默认理由: 去重后按 outbox_seq 分页易错；本片验收只需一页正序轨迹，避免发明损坏分页。
- A-334 2026-08-15 [生效] 048/S-41 只用当前 Origin 的 Notification API，不接 Web Push、不保存订阅密钥。默认理由: 本地优先单 owner，避免第三推送方与密钥面。
- A-335 2026-08-15 [生效] 通知开关存 localStorage，不进 SQLite。默认理由: 与设置固定入口同类本机非领域偏好。
- A-336 2026-08-15 [生效] 仅 `document.visibilityState==='visible'` 时轮询审计，间隔 ≥15s。默认理由: 不在后台打满 API，也不做无人值守动作。
- A-337 2026-08-15 [生效] PWA 只提供可安装 manifest，不 cache 业务 API/数据库。默认理由: 离线缓存会把项目事实带出当前会话边界。
- A-338 2026-08-15 [生效] 048 浏览器验收 stub Notification，不新开 Agent 执行。默认理由: 时长护栏。
- A-339 2026-08-15 [生效] 048 不写 audit outbox。默认理由: Runtime 通知 Adapter 不是事件 producer。
- A-340 2026-08-15 [生效] 通知开关放在 `/team` 设置页独立 region「通知」，不新增 SETTINGS_SECTIONS id。默认理由: 避免 pinned-settings 契约与大量机械迁移；本片用户结果是授权与提醒，不是新设置分区检索。
- A-341 2026-08-15 [生效] 049 保留 D-46 暖陶色板与四列栅格；UI UX Pro Max 的青绿/橙 CTA 仅作技能推荐，不覆盖已确认视觉。默认理由: 用户要的是安静交互，不是再换皮肤。
- A-342 2026-08-15 [生效] 结构图标统一 `@phosphor-icons/react` regular 20px；禁止 emoji 图标。默认理由: 技能默认图标库，且与 A-19 同一图标库纪律一致。
- A-343 2026-08-15 [生效] 创建/编辑/绑定/打开文件夹等配置输入只在点按 opener 后以 dialog 出现，桌面不再常驻表单。默认理由: 用户明确要求输入框点击按钮后弹出。
- A-344 2026-08-15 [生效] 说明文案改为可键盘操作的 HelpTip，禁止 hover-only `title` 作为唯一帮助。默认理由: 技能禁止 hover-only 作为截断/帮助的唯一恢复路径。
- A-345 2026-08-15 [生效] 列表、消息、看板卡保持常驻；只把作者表单与长说明收入披露。默认理由: 安静 chrome 不能把工作结果也藏起来。
- A-346 2026-08-15 [生效] 浮层表单字段必须有可见 label，禁止 placeholder-only。默认理由: 技能 Forms/Accessibility 高严重级。
- A-347 2026-08-15 [生效] 图标控件的可访问名称保持现有中文动词/分区名。默认理由: 不改领域语义，也让既有 getByRole 名称可迁移。
- A-348 2026-08-15 [生效] 上下文 tab 与设置资源 tab 改为图标 + aria-label，名称与现文案一致。默认理由: 能用图标就不要用文字。
- A-349 2026-08-15 [生效] ActivityBar 每项使用 aria-label，title 不得作为唯一名称。默认理由: 图标按钮必须有 accessible name。
- A-350 2026-08-15 [生效] 目录搜索 pill 保持常驻过滤控件。默认理由: 搜索是列表过滤而非创建表单；藏起来会增加找不到线程的成本。
- A-351 2026-08-15 [生效] 群聊 composer 保持常驻主工作面，不藏进按钮。默认理由: 驾驶舱的主任务是说话；用户抱怨的是配置表单噪声。
- A-352 2026-08-15 [生效] 窄屏仍用现有三抽屉；工具栏改为图标；浮层叠在抽屉上。默认理由: 不重做窄屏导航模型。
- A-353 2026-08-15 [生效] 阶段 1 打开文件夹只走宿主原生目录选择器（Windows 文件夹对话框 / macOS choose folder / Linux zenity|kdialog）；浏览器不出现路径输入。测试与冒烟用 `COCKPIT_SCRIPTED_DIRECTORY` 脚本化返回路径，不模拟手输。脚本化短路仅在 `NODE_ENV=test` 或显式 `COCKPIT_ALLOW_SCRIPTED_PICKER=1` 时生效；生产 `next start` 即使带了脚本化路径也必须走原生对话框。默认理由: 用户明确要求系统资源管理器原则；浏览器 File System Access API 不能提供 verified-handle 所需真实路径。
- A-354 2026-08-15 [生效] 选择器取消时保持当前页、不报错、不回退到路径表单。无 DISPLAY/对话框失败时返回稳定脱敏错误「无法打开系统文件夹选择器」。默认理由: 失败关闭且不把路径手输当兼容层。
- A-355 2026-08-15 [生效] 自带 Agent 复用已有模板 id `planner`/`builder`/`reviewer`，落地稳定主键 `starter-planner`/`starter-builder`/`starter-reviewer`；绑定库中第一个已验证 Provider 的 id 与 defaultModel；无 Provider 时不创建。默认理由: `provider_id` 非空；凭据不能伪造。
- A-356 2026-08-15 [生效] 打开文件夹且成员为空、且已有 ≥2 名自带 Agent 时写入 roster。已有成员的项目不改写。再次打开同一路径时若 roster 仍空则补写，不依赖首次 `created` 门闩。默认理由: 降低首次组队摩擦，且重试必须能重放空 roster 入组。
- A-357 2026-08-15 [生效] 自带 Agent 允许在团队页编辑模型/权限，不允许删除（删除返回冲突）；ensure 幂等。默认理由: 系统班组必须可恢复，配置仍属 owner。
- A-358 2026-08-15 [生效] 阶段 1 驾驶舱卸载：使命看板 tab、记忆 tab、HelpTip、项目上下文右栏（记忆/审计/骨架/审批入口）。群聊 composer、线程列表、成员短名单、打开文件夹保留。`/team` 仍可配置 Provider。默认理由: 用户要求使命/记忆先不在前端展示，并去掉丑陋提示框。
- A-359 2026-08-15 [生效] 050/S-60 不拆片：原生选夹、自带 Agent、聊天优先卸载是同一「能开干」用户结果。用户明确要求先完成第一阶段。风险: 触及 picker 安全边界、Identity 写入、PWS 成员与 UI；验证需改打开文件夹冒烟并停用驾驶舱内使命/记忆点击路径。默认理由: AGENTS.md 允许用户明确不拆时写入例外。
- A-360 2026-08-15 [生效] 只抄 pi / DeepSeek Harness 的 session+chat 与 native directory picker 原则，不把其源码、商标或视觉资产拷进仓库。默认理由: 与 A-282 一致。
- A-361 2026-08-15 [生效] POST `/api/projects` 仍接受 `{ path }`（选择器结果与测试夹具）；UI 不再采集该字段。默认理由: 领域打开契约不变，只改路径来源。
- A-362 2026-08-16 [生效] 用户要求「继续下一片并进入 auto」：051 执行模式改为 auto；纯 UI 切片评审沿用项目级豁免；演示记 auto-approved，下次交互须呈上驾驶舱。默认理由: 用户明确切换 auto。2026-08-16 23:39 用户再次确认「hf进入auto模式」，维持 auto。
- A-363 2026-08-16 [生效] 协作时间线 Owner/Agent 发言按 UCD 渲染为头像气泡；thread_created 等系统事实改为安静系统行，不伪造思考链或提案数据。事实类型文案（如「所有者发来消息」）保留为 sr-only 以免打断既有断言。默认理由: 用户要求严格按 UCD，且本片只改展示不改事实模型。
- A-364 2026-08-16 [生效] 事实分页耗尽后「已加载全部事实」按钮改为 `sr-only` 而非卸载，以免打断「加载更多」后的焦点保持；主路径不再展示该非 UCD chrome。默认理由: UCD 聊天区无分页耗尽按钮。
