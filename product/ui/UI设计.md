# Cool AI 用户体验与界面设计说明书（UI / UCD 事实源）

- 文档状态：正式生效
- 编写日期：2026-08-15
- 设计角色：独立 UCD（User-Centered Design）设计师
- 适用范围：Cool AI 全局用户体验、信息架构（IA）、用户旅程、交互模式、界面全态矩阵与验收标准
- 关联事实源：
  - 产品规格说明书：`product/product.md`
  - 领域词汇表（统一语言）：`product/词汇表.md`
  - 开发计划（交付顺序单一事实源）：`product/development-plan.md`（P0–P5）
  - 视觉令牌与壳层规范：`product/ui/DESIGN.md`（暖米 + 暖金）
  - 文档地图：`product/README.md`
  - 关键架构与产品决策：`product/decisions.md`（D-49、D-50）
  - 运行时令牌投影：`app/tokens.css`

---

## 1. 文档定位与关系

本说明书是 Cool AI 系统的 **UI 与以用户为中心设计（UCD）的单一事实源**。它定义了产品如何被用户感知、理解、操作与信任，并作为前端实现、设计走查和自动化测试验证的权威依据。

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        product/product.md                              │
│                    （产品规格说明书：边界与第一性原理）                  │
└──────────────────────────────────┬─────────────────────────────────────┘
                                   │
         ┌─────────────────────────┴─────────────────────────┐
         ▼                                                   ▼
┌──────────────────────────────────┐      ┌──────────────────────────────┐
│  product/development-plan.md     │      │   product/ui/DESIGN.md       │
│  （交付顺序 P0–P5、前后端契约）   │      │ （视觉令牌、色板、字体、间距） │
└────────────────┬─────────────────┘      └──────────────┬───────────────┘
                 │                                       │
                 └───────────────────┬───────────────────┘
                                     ▼
                  ┌─────────────────────────────────────┐
                  │       product/ui/UI设计.md          │
                  │   （本文：IA、旅程、交互、全态、UCD）  │
                  └──────────────────┬──────────────────┘
                                     ▼
                  ┌─────────────────────────────────────┐
                  │    components/ 与 app/ 界面落地     │
                  └─────────────────────────────────────┘
```

### 1.1 职责边界划分
1. **本文（UI设计.md）**：专注用户心理模型、角色（Persona）、要完成的工作（JTBD）、核心场景、端到端旅程、信息架构、交互原则、关键界面规范、6 态矩阵及可访问性契约。
2. **`product/ui/DESIGN.md`**：视觉物理事实源，提供暖米 + 暖金两套色板的精确 Hex 值、字体堆栈、4/8px 间距阶梯、圆角与阴影标称。本文直接引用其定义，绝不另造一套视觉变量。
3. **`product/development-plan.md`**：交付顺序与切片计划事实源，规定 P0–P5 的演进步骤。本文严格遵循“先闭环、再变厚”的交付节奏，不越级暴露未交付功能。

---

## 2. 设计研究摘要

本设计基于 `@cursor/skills/ui-ux-pro-max` 技能库的系统检索，结合 Cool AI 本地优先、多 Agent 协作驾驶舱的真实约束进行参数调优与设计收敛。

### 2.1 技能检索参数与结论
根据驾驶舱定位，检索设定参数为：`--density 8`（紧凑驾驶舱）、`--variance 3`（克制极简）、`--motion 2`（安静微动效）。

| 维度 | 检索输出建议 | 在 Cool AI 中的设计采纳与落地 |
|---|---|---|
| **核心模式 (Pattern)** | Trust & Authority + Content-First Chat Shell | 建立以会话为中心的作业面；所有治理操作收归为按需视图；通过不可变时间线和可追溯来源建立确定性信任。 |
| **视觉风格 (Style)** | Minimalism & Swiss Style（极简主义） | 采用暖米画布与暖金强调，高留白、弱边框（1px hairline）、轻阴影；通过字重与明度建立信息层级，拒绝多余装饰。 |
| **交互原则 (UX)** | Focus Not Obscured + User Freedom + Active Feedback | 键盘焦点始终清晰可见且不被浮动栏遮挡；空状态提供明确下一步引导且允许自由跳过；状态切换在 80~150ms 内提供稳定反馈。 |
| **图标与无障碍 (Icons)** | Phosphor 线性矢量族 + 语义属性规范 | 统一采用 Phosphor 线性图标（描边 1.5px/2.0px）；严禁用 Emoji 作为系统结构图标；装饰图标 `aria-hidden="true"`，操作图标提供语义标签。 |
| **流式与响应 (Stack)** | Next.js Suspense + Streaming Composer | 对话流采用流式分块渲染，打字机滚动平滑无跳变；Composer 支持防抖与上下文即时加载。 |

### 2.2 核心反模式（严禁出现）
- ❌ **严禁主路径四栏常驻**：不再将使命看板、共享记忆、审批流、审计作为常驻右栏或并列面板强行塞在主屏。
- ❌ **严禁表单堆砌与未接线控件**：空状态或首页不铺陈复杂的未连接配置项；无项目时居中保持 1:1 纯净对话。
- ❌ **严禁手动输入本地文件路径**：打开与绑定工作区一律调用操作系统原生文件夹选择器，杜绝手动输入路径引发的格式与安全错误。
- ❌ **严禁 Emoji 充当结构图标**：导航栏、操作按钮、系统标签严禁用 📁、⚙️、🤖 等 Emoji 代替矢量图标。
- ❌ **严禁破坏历史与不可变性**：不以“最新状态”覆盖历史版本；任何由 Agent 产出的决定、执行与复核均保留冻结来源。

---

## 3. 目标用户与人物角色（Persona）

Cool AI 专为追求极致生产力、拥有全栈实现能力的独立创造者打造。

### 主 Persona：Alex —— 独立产品创造者（Solo Indie Creator）

```text
┌────────────────────────────────────────────────────────────────────────┐
│  Alex | 31 岁 | 独立全栈开发者 / 独立数字产品创造者                      │
│  工作方式：单兵作战，推动 1~2 个真实产品从创意、架构、编码到上线运营     │
│  工作环境：macOS / Windows 桌面工作站，熟悉 CLI 与 Git，配置有 API Key  │
│  性格特征：追求专注与心流，注重代码与资产所有权，对黑盒不可控工具极度敏感 │
└────────────────────────────────────────────────────────────────────────┘
```

- **典型日常**：
  - 在本地打开项目代码或设计资料文件夹。
  - 需要一个“规划伙伴”帮忙拆解架构任务，一个“编码搭档”快速执行受控改动，一个“挑剔同事”独立复核代码。
- **心理诉求**：
  - **掌控感**：我必须是唯一的决断者（Owner），Agent 可以提出建议或执行任务，但关键动作必须经我同意，且执行结果必须有据可查。
  - **免维护路由**：受够了在 Web 聊天窗口中手动复制粘贴：“A 模型给出的方案，复制给 B 模型修改，再复制给 C 模型检查”。
  - **安静与专注**：工作台必须干净，不要花哨的 3D 动画、不要杂乱的仪表盘堆叠，让我能够连续工作数小时而不产生视觉疲劳。

---

## 4. 要完成的工作（JTBD）与痛点对照

### 4.1 核心 JTBD 声明
> **当**我需要独自推进一个复杂的产品功能或架构迭代时，  
> **我想要**把规划、实施、复核等多名专业 Agent 组织进我本地打开的项目中，并在一个公开透明的对话流中发起协作与交棒，  
> **以便于**它们能够自主讨论、受控执行并独立验证，而我无需充当人工路由器，同时对整个过程保持绝对掌控与可追溯性。

### 4.2 核心痛点与 UI 解决方案矩阵

| 序号 | 用户的核心痛点 | 传统工具的缺陷 | Cool AI 的 UI/UCD 解决方案 |
|---|---|---|---|
| **P-1** | **人工路由器疲劳** | 多个 AI 窗口各自为政，用户频繁复制粘贴上下文。 | **共享公开会话**：Owner 在居中聊天流发起任务，自带 Agent 在同一时间线内自主接力、互相引用与交棒。 |
| **P-2** | **操作黑盒与越界恐惧** | 自动 Agent 随意修改本地文件，出了问题难以回滚。 | **统一审批与对话流内联证据**：高危动作触发 Header 暖金「Needs Me」徽标与内联卡片，清晰展示影响范围。 |
| **P-3** | **多栏堆叠造成认知过载** | 屏幕被项目树、看板、记忆、聊天、终端切成 4~5 列，极其压抑。 | **52px 图标轨 + 240px 侧栏 + 840px 居中流**：治理能力收归图标轨，按需唤起，一键「返回对话」。 |
| **P-4** | **无项目时的挫败感** | 刚进入必须先建库、建项目、配工作区，无法立即使用。 | **开箱即用 1:1 直聊 + 原生选夹**：未选项目时即为极简对话；选夹直接调起系统选择器，自带 3 名 Agent 秒级就绪。 |
| **P-5** | **上下文丢失与虚假记忆** | 每次刷新或重启，AI 忘了之前的决定，或编造历史。 | **持久对话与冻结来源导航**：消息以不可变块存储，关键结论带精确引用（Source Tuple），随时可溯源。 |

---

## 5. 关键用户场景（Key User Scenarios）

### 场景 1：首次打开（无 Provider 引导）
- **情境**：用户首次安装并启动 Cool AI，本地未配置任何模型 API。
- **界面行为**：
  - 图标轨与侧栏以骨架状态呈现；
  - 聊天主区居中显示欢迎卡片，文案克制友善：“欢迎使用 Cool AI。配置模型 Provider 后即可开始。”；
  - 呈现醒目的暖金主按钮「配置 Provider」及次级「查看引导」；
  - 点击直达 `/team` 或全局设置中的 Provider 配置弹窗，配置成功后平滑返回，无刷新白屏。

### 场景 2：打开文件夹进入/恢复项目（阶段 1 核心路径）
- **情境**：用户准备开始处理本地的 `cool-mobile` 项目。
- **界面行为**：
  - 用户点击 Header 右上角的「打开文件夹」按钮（或快捷键 `Cmd/Ctrl + O`）；
  - 浏览器调用操作系统原生文件夹选择器（File System Access API）；
  - 用户选中目录后，系统校验目录权限并绑定为当前 Project；
  - 侧栏立即恢复或创建该项目的持久对话；主区就绪，自动载入该项目的规划、实施、复核 3 名自带 Agent。
  - **严禁**提供任何手动输入路径的文本框。

### 场景 3：未选项目时的 1:1 直聊（阶段 1 核心路径）
- **情境**：用户刚打开应用，只想快速问一个算法问题或头脑风暴，不想绑定本地文件夹。
- **界面行为**：
  - Header 显示「未选择项目」，语境为「个人对话」；
  - 居中聊天流保持清爽，Composer 占位符提示“输入消息，与助手直接对话…”；
  - 具备完整的流式输出、复制、重试与 Markdown 渲染能力，体验轻快顺畅。

### 场景 4：对自带 Agent 公开发言与 @Agent 接力
- **情境**：在已绑定项目中，用户希望规划 Agent 梳理登录模块改造方案，并由实施 Agent 执行。
- **界面行为**：
  - 用户在底部 Composer 输入 `@`，弹出 Agent 快速选择浮层（规划、实施、复核）；
  - 选中「@规划」，输入“请分析当前登录模块的重构步骤”并发送；
  - 聊天流出现规划 Agent 的思考过程与流式输出；
  - 输出完毕后，规划 Agent 生成结构化任务卡片，并自动 `@实施 请根据方案进行文件改动`；
  - 协作时间线以公开气泡形式自然流转，用户作为 Owner 全程可见并随时可插话打断。

### 场景 5：打开治理视图并无缝返回对话
- **情境**：在协作过程中，用户想查看当前项目的任务依赖全景或记忆沉淀。
- **界面行为**：
  - 用户点击左侧 52px 图标轨的「任务」图标；
  - 居中聊天流平滑切换为全宽任务视图，展示 Mission 与 Work Item 进展；
  - 视图左上角显式保留 `← 返回对话` 暖金按钮；
  - 用户完成浏览或微调后，点击按钮（或按 `ESC` 键，或点击图标轨「对话」图标），主区立即恢复原会话流，保留滚动锚点与正在输入的草稿。

### 场景 6：统一审批与 Needs Me 响应
- **情境**：实施 Agent 在绑定工作区内准备写入修改文件 `auth.ts`，触发安全策略。
- **界面行为**：
  - 对话流中生成结构化审批卡片（清晰列出：待修改文件、Diff 预览、发起 Agent、操作风险评级）；
  - 顶部 Header 右侧与图标轨「审批」图标即刻亮起暖金「Needs Me」徽标；
  - 用户在卡片上直接点击「批准（Approve）」或「拒绝（Reject）」，亦可在审批中心统一批复；
  - 操作后卡片变为已审批状态，Agent 收到确认信号继续执行，整个过程可审计。

### 场景 7：异常与网络超时防御
- **情境**：调用模型 API 发生网络抖动或鉴权失效。
- **界面行为**：
  - 消息气泡下方出现温和的错误提示条（采用 `--color-surface-pearl` 底色与暗红警示字，脱敏报错信息，绝不暴露内部堆栈或系统绝对路径）；
  - 提供「重试」与「检查 Provider」快捷按钮；
  - 系统维持失败关闭原则，不生成虚假成功事实。

---

## 6. 端到端用户旅程（User Journey）

### 6.1 全局用户旅程图

```text
                                  【启动 Cool AI】
                                         │
                                         ▼
                            [检查 Provider 是否配置?]
                                   /           \
                           (否)   /             \   (是)
                                 ▼               ▼
                        【引导至 /team 配置】    【进入主界面】
                                 │               /           \
                                 │       (未选项目) /             \ (选择项目文件夹)
                                 │                ▼               ▼
                                 └────────► 【1:1 纯净直聊】   【打开系统选夹器】
                                                                  │
                                                                  ▼
                                                      【载入项目与三名自带 Agent】
                                                                  │
                                                                  ▼
                                                      【在持久对话中协作】
                                                       (输入 @规划 拆解任务)
                                                                  │
                                                                  ▼
                                                      【Agent 接力与方案确认】
                                                                  │
                                                                  ▼
                                                    [是否涉及文件写入/高危操作?]
                                                           /              \
                                                   (是)   /                \ (否)
                                                         ▼                  ▼
                                                【Header Needs Me 提醒】  【继续流式输出】
                                                【内联审批卡片批准/拒绝】          │
                                                         │                        │
                                                         └──────────┬─────────────┘
                                                                    ▼
                                                            【独立复核与交付】
                                                                    │
                                            ┌───────────────────────┴───────────────────────┐
                                            ▼                                               ▼
                                  【按需查看治理视图】                             【继续下一轮会话协作】
                                (点击 52px 图标轨：任务/记忆)                                │
                                            │                                               │
                                            ▼                                               ▼
                                  【点击 ← 返回对话】 ──────────────────────────────────────┘
```

### 6.2 主路径与支路步骤规范

1. **第 1 步：环境就绪**
   - 检查本地数据库与 Provider。未就绪则停留在欢迎引导，不展示破损界面。
2. **第 2 步：语境锚定**
   - 用户打开目标文件夹，Header 更新为 `[项目名] · [对话标题]`，侧栏高亮当前对话。
3. **第 3 步：意图输入**
   - 用户在底部 Composer 输入文本或 `@Agent`。输入框自适应扩展高度，最大支持 6 行滚动。
4. **第 4 步：公开流式协作**
   - 目标 Agent 响应，输出流式 Markdown。其他 Agent 根据讨论逻辑自主接力，过程透明。
5. **第 5 步：治理干预（按需）**
   - 遇到审批时快速处理；若需全局洞察，点按图标轨打开任务/记忆/审计/设置，随时按 `ESC` 返回。
6. **第 6 步：闭环沉淀**
   - 任务由非执行者 Agent 独立复核，裁决通过后沉淀为持久记忆与交付物，结束会话节点。

---

## 7. 信息架构（Information Architecture）

桌面端采用经典的 **52px 图标轨 + 240px 会话侧栏 + 居中聊天流（max 840px）** 三区布局。

```text
┌──────┬────────────────────┬──────────────────────────────────────────────────────────────────┐
│ 图标 │  会话侧栏 (240px)  │ Header (48px: 产品名 · 项目语境/对话 · 打开文件夹 · Needs Me)   │
│  轨  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
│ 52px │ 标题「对话」 +新建 │                                                                  │
│      ├────────────────────┤                       主工作区 (max 840px)                       │
│      │ 过滤 Tabs (全部/..) │                                                                  │
│      ├────────────────────┤   ┌──────────────────────────────────────────────────────────┐   │
│      │                    │   │ [Agent 消息流 / 结构化卡片 / 思考时间线 / 审批节点]       │   │
│      │  持久对话列表      │   │                                                          │   │
│      │  (倒序排列，带     │   │                                                          │   │
│      │   状态与头像)      │   │                                                          │   │
│      │                    │   │                                                          │   │
│      │                    │   └──────────────────────────────────────────────────────────┘   │
│      │                    │                                                                  │
│      ├────────────────────┤   ┌──────────────────────────────────────────────────────────┐   │
│      │ 底部「回收站」      │   │ 浮起 Composer (圆角 12px, 弱边框, 上下文附件, 发送按钮)   │   │
│      │                    │   └──────────────────────────────────────────────────────────┘   │
└──────┴────────────────────┴──────────────────────────────────────────────────────────────────┘
```

### 7.1 桌面三区详细定义

#### 区域 A：52px 图标轨（Activity Bar）
- **定位**：全局能力与治理视角的唯一一级切换中枢，常驻屏幕最左侧。
- **背景**：`--color-rail`（亮 `#EAE4DA` / 暗 `#16130F`）。
- **内容组织**：
  - **顶部主项组**：
    1. `对话`（MessageSquare 图标）：主路径入口，默认激活。
    2. `任务`（CheckSquare 图标）：按需打开当前项目的使命与任务看板视图。
    3. `记忆`（Brain 图标）：按需打开当前项目的结构化记忆与知识库视图。
    4. `审批`（ShieldCheck 图标）：打开统一审批中心；有待处理项时右上角显示暖金红点。
    5. `审计`（ListMagnifyingGlass 图标）：打开系统审计日志与事件时间轴。
  - **底部分组（经分割线隔离）**：
    6. `团队`（Users 图标）：直达 Agent 编排与 Provider 配置。
    7. `设置`（Gear 图标）：打开全局偏好设置。
    8. `主题`（Sun / Moon 图标）：一键切换亮/暗视觉主题。

#### 区域 B：240px 会话侧栏（Conversation Sidebar）
- **定位**：对话列表与切换面板，管理当前项目的公开协作容器。
- **背景**：`--color-canvas-parchment`（亮 `#FAF6F1` / 暗 `#26221D`）。
- **内容组织**：
  - **头部**：左侧大字标题「对话」（16px 600），右侧「+ 新对话」胶囊图标按钮（44px 触区）。
  - **筛选 Tab**：胶囊式小标签（全部、收藏、标签），高 32px，支持即时过滤。
  - **对话列表**：
    - 单项高 44px，展示：对话标题（超长省略）、最后活跃时间（12px muted）、参与 Agent 迷你头像组、正在运行中的暖金呼吸点。
    - 选中态：左侧 3px 暖金指示条 + 背景高亮（`--color-surface-pearl`）。
  - **底部固定项**：高 44px 的「回收站」入口，支持已软删除会话的查看与还原。

#### 区域 C：聊天主区与按需治理区（Main Stage）
- **定位**：用户与 Agent 的工作台。
- **背景**：`--color-canvas`（亮 `#F4EFE7` / 暗 `#1E1B17`）。
- **内容组织**：
  - **顶部 Header (48px)**：
    - 左侧：产品品牌标识「Cool AI」。
    - 中间：当前项目名与当前对话标题（如 `cool-mobile · 登录模块重构`）。
    - 右侧操作区：
      - 「打开文件夹」按钮（带系统文件夹图标，44px 触区）。
      - 「Needs Me」状态徽标（当有待办审批时高亮呈现，点击呼出审批详情）。
  - **居中聊天流 (Centered Chat Flow)**：
    - 最大宽度限制为 **840px**，水平居中。
    - 垂直方向弹性滚动，底部预留 120px 的 Scroll-Padding 避免内容被 Composer 遮挡。
  - **浮起 Composer (Floating Composer)**：
    - 宽度与聊天流对齐（最大 840px），浮动在底部 24px 处。
    - 样式：背景 `--color-surface-pearl`，圆角 12px，1px 弱边框 `--color-hairline`，极轻环境阴影。
    - 结构：左侧「添加附件/上下文」图标按钮（44px）、中间自适应多行输入框、右侧「发送/停止」主按钮（44px）。
  - **按需治理视图 (On-Demand Governance View)**：
    - 当从图标轨点击任务/记忆/审批/审计时，替代聊天流填满主区。
    - 顶部固定工具栏始终提供 `← 返回对话` 按钮，并在右上方保留上下文筛选。

### 7.2 响应式断点与退化策略

| 屏幕宽度 | 布局形态 | 侧栏行为 | 图标轨行为 | 聊天流宽度 |
|---|---|---|---|---|
| **≥ 1280px (宽屏)** | 三区完整展开 | 240px 常驻展开 | 52px 常驻 | 840px 居中 |
| **1024px ~ 1279px (标屏)** | 三区完整展开 | 240px 常驻展开 | 52px 常驻 | 自适应居中 (最大 840px) |
| **768px ~ 1023px (平板/窄屏)** | 抽屉模式 | 收起为抽屉，通过 Header 汉堡按钮呼出 | 52px 常驻 | 100% 宽度 (水平 Padding 24px) |
| **< 768px (极窄/移动)** | 极简模式 | 全屏抽屉滑出 | 底部 Tab 栏或 52px 紧凑轨 | 100% 宽度 (水平 Padding 16px) |

---

## 8. 交互设计原则与模式规范

### 8.1 选中态规范（Active States）
- **图标轨**：选中项图标颜色切换为暖金（亮 `#9A5F1A` / 暗 `#E4B066`），左侧出现 3px 宽、圆角 2px 的暖金垂直指示条。
- **对话列表**：选中项背景填充 `--color-surface-pearl`，文字字重提升至 600，右侧显示当前对话的活动状态徽标。
- **筛选胶囊 Tab**：选中项底色应用 `--interactive-accent-soft`（亮 `#FDF3E0` / 暗 `#3A2E1E`），文字为暖金主色。

### 8.2 Tooltip 触发与展示机制
- **延迟与过渡**：悬停（Hover）200ms 后浮现，鼠标移开立即消失；过渡时长 150ms。
- **位置与避让**：图标轨 Tooltip 固定向右弹出，垂直居中对齐；避开视口边缘。
- **无障碍实现**：原生结合 `title` 与 `role="tooltip"`，并通过 `aria-describedby` 关联。

### 8.3 模态对话框与弹出层规范（Modal & Dialog）
- **层级与遮罩**：遮罩层（Scrim）采用 `rgba(20, 16, 10, 0.4)`，背景应用 4px 轻微模糊。
- **容器规格**：圆角 16px，背景 `--color-surface-pearl`，内边距 24px，最大宽度通常为 520px。
- **焦点捕获与关闭**：
  - 弹出后焦点自动落入首个输入框或主确认按钮；
  - 严格限制 `Tab` 键在弹窗内循环（Focus Trap）；
  - 按 `ESC` 键或点击遮罩区域立即关闭，焦点平滑归还原触发点。

### 8.4 返回对话机制（Back-to-Chat Pattern）
- **单一退出通道**：所有从图标轨打开的二级治理视图（任务、记忆、审批、审计、设置），在视图 Header 最左侧提供明确的 `← 返回对话` 按钮。
- **快捷键直达**：在任何治理视图中，按下 `ESC` 或 `Cmd/Ctrl + 1` 均可瞬间返回对话主路径。
- **状态保持**：返回对话时，自动恢复前序会话的滚动进度、Draft 输入草稿和展开状态。

### 8.5 原生选夹交互流（Native Folder Picker）
- **触发入口**：Header「打开文件夹」按钮及空状态主操作。
- **API 管道**：调用 `window.showDirectoryPicker()` 原生接口。
- **异常捕获**：
  - 若用户在系统对话框中点击“取消”，界面保持原状，不弹出多余报错。
  - 若用户选择受限系统目录（如根目录或系统临时文件），弹出友好提示条解释安全规则并建议重新选择。

### 8.6 Composer 键盘与富交互规范
- **@ 唤起与过滤**：在输入框键入 `@` 时，光标上方弹出 Agent 列表菜单，支持键盘上下方向键导航、`Enter` 或 `Tab` 键自动补全。
- **发送与换行**：`Enter` 触发发送，`Shift + Enter` 插入换行符。
- **高度自适应**：初始单行（44px 触区），随内容增加自动弹性撑高，最高 180px，超出后出现内置滚动条。
- **附件操作**：支持直接将本地图片或文件拖拽入 Composer，或按 `Cmd/Ctrl + V` 粘贴剪贴板图片，生成内联预览小卡片并支持一键删除。

### 8.7 键盘导航与焦点管理（Keyboard & Focus）
- **全局快捷键表**：
  - `Cmd/Ctrl + K`：全局对话与命令搜索。
  - `Cmd/Ctrl + N`：新建对话。
  - `Cmd/Ctrl + O`：调出系统文件夹选择器打开项目。
  - `Cmd/Ctrl + B`：展开/折叠会话侧栏。
  - `Cmd/Ctrl + 1`：快速切换至「对话」主路径。
  - `Cmd/Ctrl + 2~5`：按序唤起任务、记忆、审批、审计视图。
  - `ESC`：关闭当前弹窗 / 从治理视图返回对话。
- **焦点环（Focus Ring）**：
  - 所有可聚焦控件在 `:focus-visible` 下显示 2px 暖金焦点环（`--color-primary`），`outline-offset: 2px`。
  - 严禁出现 `outline: none` 而不提供视觉替代。

---

## 9. 关键界面详细说明

### 9.1 驾驶舱主壳层（Cockpit Shell）
- **界面目的**：承载全局导航、会话管理与主要工作流的一体化桌面工作台。
- **主要元素**：
  - 左侧 52px 图标轨（主业务入口 + 底部系统设置/主题）。
  - 中左 240px 会话侧栏（新建、筛选、历史对话流、回收站）。
  - 右侧全局 Header（项目与对话标题、文件夹快捷入口、Needs Me 徽标）。
  - 居中聊天工作区（流式消息流 + 浮起 Composer）。
- **关键状态**：
  - 默认态：三区舒展呈现，当前对话高亮。
  - 窄屏态：侧栏自动收拢至左侧抽屉，Header 出现展开按钮。
  - 治理展开态：聊天流平滑替换为任务/记忆/审批全宽面板。
- **成功标准**：三区过渡流畅无抖动，键盘 Tab 键可顺畅遍历三个区域的所有控件。

### 9.2 空状态与欢迎引导（Empty & Onboarding States）

```text
┌────────────────────────────────────────────────────────────────────────┐
│                                                                        │
│                                ( 欢迎图标 )                             │
│                                                                        │
│                          欢迎使用 Cool AI 驾驶舱                        │
│                 本地优先、单 Owner 的多 Agent 协作工作台                 │
│                                                                        │
│     ┌────────────────────────────────────────────────────────────┐     │
│     │ 💡 快速开始：                                               │     │
│     │ 1. 点击下方按钮，打开本地项目文件夹（或直接开始 1:1 直聊）       │     │
│     │ 2. 在对话框中输入 @规划、@实施 或 @复核 发起协作任务        │     │
│     └────────────────────────────────────────────────────────────┘     │
│                                                                        │
│                      [ 打开项目文件夹 (暖金主按钮) ]                      │
│                          [ 首次使用？查看引导 ]                          │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

- **界面目的**：在新用户首次启动、未选择项目或进入空对话时，消除空白焦虑，引导用户建立心智模型并迈出第一步。
- **主要元素**：
  - Phosphor 线性欢迎图标（48px，`--color-ink-muted-48`）。
  - 主标题（18px 600）与副标题说明（14px muted）。
  - 核心引导卡片（推荐 `@规划`、`@实施`、`@复核` 典型动作）。
  - 暖金主操作按钮「打开项目文件夹」与次级操作按钮「开始引导」。
- **关键状态**：
  - 无 Provider 态：主按钮变为「配置 Provider」，点击直达设置。
  - 无项目态：引导打开文件夹，同时允许直接在下方 Composer 输入进行 1:1 直聊。
  - 空对话态：提示“当前对话暂无消息，输入你的想法或 @Agent 开始”。
- **成功标准**：用户在 5 秒内理解如何开始工作；点击主按钮即刻唤起下一步动作，无冗余表单阻碍。

### 9.3 居中聊天流与消息卡片（Chat Flow & Message Bubbles）
- **界面目的**：呈现 Owner 与多 Agent 之间的协作对话、思考过程、结构化结果与执行事实。
- **主要元素**：
  - **消息头部**：Agent 角色圆形头像（36px，带角色暖金色标识）、角色名称、时间戳、操作菜单（复制、引用、重试）。
  - **消息正文**：
    - Markdown 渲染层（代码高亮、表格、列表、任务清单）。
    - 思考折叠块（Thinking Block）：可展开/收起的淡雅灰色卡片，展示 Agent 的推理链条。
    - 结构化卡片（Proposal / Work Item / Checklist）：带明确边框与就地操作按钮。
    - 来源引用标签（Source Tag）：指向冻结文件的文件名与行号，点击可定位。
- **关键状态**：
  - 发送中：消息透明度微降，右上角显示排队/发送中指示。
  - 流式生成中：打字机平滑流出，末尾带有暖金光标闪烁，聊天流自动平滑向上滚动。
  - 报错态：卡片显示淡红边框，底部附带脱敏错误说明与「重试本轮」按钮。
- **成功标准**：长对话滚动流畅（60fps），代码块高亮准确且带有一键复制按钮，Agent 身份辨识度 100%。

### 9.4 浮起 Composer（Floating Composer）
- **界面目的**：提供无干扰、富交互的自然语言与指令输入环境。
- **主要元素**：
  - 左侧功能组：「添加附件/工作区上下文」图标按钮（44px）。
  - 中间输入区：自动伸缩多行 TextArea，支持 `@` 补全与 `/` 命令。
  - 右侧操作组：发送按钮（暖金底色加白箭头图标，44px）；生成中自动切换为「停止生成」方块按钮。
- **关键状态**：
  - 初始为空：发送按钮处于禁用态（Disabled），占位文字为“输入消息，按 Enter 发送，Shift+Enter 换行，输入 @ 提及 Agent…”；
  - 聚焦态（Focused）：输入框容器外层显现暖金 2px 环绕光晕；
  - 内容溢出：内容超出 6 行时内部滚动，容器高度锁定在最大高度；
  - 拖拽悬停：当文件拖入时，Composer 背景高亮并显示“释放以添加附件”。
- **成功标准**：打字毫无输入延迟，快捷键发送准确无误，粘贴与拖拽文件响应敏捷。

### 9.5 会话侧栏与对话列表（Session Navigation）
- **界面目的**：组织和切换项目的各个历史任务会话。
- **主要元素**：
  - 顶部栏：「对话」标题 +「+ 新对话」按钮。
  - 胶囊筛选器：「全部」、「收藏」、「标签」。
  - 虚拟滚动列表：每个列表项包含标题、最后活跃时间、关联 Agent 迷你头像。
  - 底部功能栏：「回收站」（带已软删除数量标记）。
- **关键状态**：
  - 选中项：左侧 3px 暖金竖条高亮。
  - 鼠标悬停（Hover）：底色变更为弱色 `--color-surface-pearl`，右侧显现更多操作（收藏、重命名、归档）图标。
  - 运行中（Running）：列表项右侧带有暖金呼吸光点。
- **成功标准**：对话切换在 50ms 内完成，切换无白屏闪烁，列表支持键盘上下键切换。

### 9.6 按需治理视图（Governance Views）
- **界面目的**：提供任务（Mission）、记忆（Memory）、统一审批（Approval）、审计日志（Audit）的专门管理深度作业面。
- **主要元素**：
  - **顶部通用工具条**：左侧固定 `← 返回对话` 按钮；中间显示当前视图名称与项目名称；右侧提供视图特定动作（如“添加记忆”、“导出审计”）。
  - **任务视图**：看板/列表双模切换，展示 Work Item 的状态、依赖关系图、负责人 Agent。
  - **记忆视图**：展示按不可变 Tuple 沉淀的结构化知识条目，支持按标签筛选、查看原始来源。
  - **审批中心**：汇聚所有待处理的越界/高危操作申请，展示 Diff 与影响面评估，支持批量或逐项审批。
  - **审计中心**：按时间顺序展示系统不可变事件流，支持按领域、Agent 和时间范围检索。
- **关键状态**：
  - 数据加载中：显示暖米骨架屏。
  - 空数据态：展示简洁的空状态插图与“暂无待处理审批/任务”文本。
- **成功标准**：点击 `← 返回对话` 或按 `ESC` 瞬间返回主会话且无状态丢失。

### 9.7 全局设置与团队配置面板（Settings & Team Panels）
- **界面目的**：管理模型 Provider（API Key、Base URL）、Agent 角色属性、界面偏好与快捷键。
- **主要元素**：
  - 统一模态对话框或居中宽面板。
  - 左侧二级导航：Provider 设置、Agent 团队编排、外观与主题、存储与工作区、安全策略。
  - 右侧表单区：输入框、测试连接按钮、保存按钮。
- **关键状态**：
  - API Key 字段：默认掩码显示（`••••••••`），支持点击小眼睛明文查看，验证中显示 Spinner。
  - 保存反馈：保存成功后在按钮右侧短暂显示绿色对勾与“已保存”文字，无需弹出阻断式 Alert。
- **成功标准**：输入敏感凭据自动过滤，连接测试 3 秒内给出明确成功/失败诊断信息。

---

## 10. 全态矩阵（Full State Matrix）

遵循 Cool AI 的可访问性与防御性设计原则，所有核心交互组件必须严格覆盖以下 6 种标准状态：

```text
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                                 全态矩阵 (6 态标准)                                     │
├─────────────┬─────────────┬─────────────┬─────────────┬─────────────┬──────────────────┤
│ 1. Loading  │  2. Empty   │  3. Error   │ 4. Disabled │ 5. Success  │    6. Focus      │
│ (加载/流式)  │ (空白/占位) │ (错误/警示) │ (禁用/不可选)│ (成功/就绪) │ (键盘聚焦指示)   │
└─────────────┴─────────────┴─────────────┴─────────────┴─────────────┴──────────────────┘
```

### 10.1 关键组件全态表现规范

| 组件 / 区域 | 1. Loading | 2. Empty | 3. Error | 4. Disabled | 5. Success | 6. Focus |
|---|---|---|---|---|---|---|
| **主按钮 (Primary Button)** | 内置 16px 暖金 Spinner，文字微透，禁止重复点击。 | N/A | 触发震颤动画，背景短暂停留警示红。 | 底色变灰 (`--color-hairline`)，光标为 `not-allowed`。 | 出现绿色 Checkmark，随后平滑恢复。 | 2px 暖金外光圈 (`outline-offset: 2px`)。 |
| **聊天流 (Chat Flow)** | 骨架屏占位 / 打字机光标流式推进。 | 居中显示欢迎引导与 @Agent 快速开始卡片。 | 气泡底色泛淡红，显示脱敏错误与重试按钮。 | N/A | 生成结束，展示完整渲染格式与操作栏。 | 键盘定位消息卡片，显示清晰焦点外框。 |
| **浮起 Composer** | 发送按钮转为停止方块，输入框只读等待。 | 显示标准引导 Placeholder，发送按钮置灰。 | 边框变淡红，下方浮现输入校验提示。 | 整体透明度 50%，占位符显示“当前不可输入”。 | 消息成功入列并清空输入框，高度复原。 | 容器边框高亮，呈现 2px 暖金光晕。 |
| **对话列表项 (Session Item)** | 侧栏条目骨架脉冲。 | 侧栏显示“暂无对话，点击上方新建”。 | 条目右侧显示红色感叹号小图标。 | 条目呈暗灰色，点击无响应（如已归档锁死）。 | 点击后主区平滑载入，该项保持暖金高亮。 | 条目外围显现 2px 矩形焦点虚线框。 |
| **审批卡片 (Approval Card)** | 操作按钮显示加载环，阻止二次点击。 | 审批中心显示“当前无待审批项目”。 | 显示“审批过期或已被撤销”提示。 | 已处理的按钮（批准/拒绝）变为静态标签。 | 状态盖章“已批准”，记录审批时间与批复人。 | 聚焦在 Approve / Reject 按钮上高亮。 |

---

## 11. 无障碍（Accessibility）与紧凑设计契约

### 11.1 对比度保证（WCAG 2.2 AA）
- **正文文字**：主文字 `--color-ink` 对画布背景对比度严格 **≥ 7:1**（远超 4.5:1 基准）。
- **次级文字**：`--color-ink-muted-80` 对背景对比度严格 **≥ 4.5:1**。
- **弱化文字与边界**：`--color-hairline` 与图形边界对比度严格 **≥ 3:1**。
- **主题独立验证**：亮色主题与暗色主题各自独立测量验证，禁止用同一套相对值换算。

### 11.2 触控与点击目标（Touch Target ≥ 44×44px）
- 即使视觉呈现为 20px 的小图标或 32px 的胶囊标签，其 CSS 物理外延或虚拟伪元素（`::after`）点击响应热区必须 **≥ 44×44px**。
- 保证在触控屏或高分辨率触控板上均可轻松点击，避免误触。

### 11.3 语义树与 ARIA 规范
- **图标语义解耦**：
  - 纯装饰性图标显式添加 `aria-hidden="true"`；
  - 独立操作图标按钮必须配置 `aria-label` 明确描述动作（如 `aria-label="新建对话"`）；
  - 折叠/展开控件关联 `aria-expanded="true/false"`；
  - 切换类按钮关联 `aria-pressed="true/false"`。
- **实时区域（Live Regions）**：
  - 对话流的流式输出外层容器配置 `aria-live="polite"` 与 `aria-atomic="false"`，确保屏幕阅读器能有序朗读而不打断用户操作。

### 11.4 减弱动效适配（`prefers-reduced-motion`）
- 当检测到系统开启减弱动效时：
  - 彻底关闭页面滑动、弹窗位移及复杂过渡；
  - 将所有动效降级为平滑透明度淡入淡出（Opacity Fade），时长压缩至 ≤100ms；
  - 停止所有呼吸灯光晕动画，转为静态色彩指示。

---

## 12. 视觉方向引用（Visual Reference）

视觉系统的色彩、字号、字重、圆角和阴影定义以 `product/ui/DESIGN.md` 及 `app/tokens.css` 为单一事实源。此处仅摘录关键基准值供设计校准对照：

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        核心视觉令牌对照表 (摘录)                         │
├───────────────────────┬──────────────────────┬─────────────────────────┤
│ 令牌名                │ 亮色模式 (Light)     │ 暗色模式 (Dark)         │
├───────────────────────┼──────────────────────┼─────────────────────────┤
│ --color-canvas        │ #F4EFE7 (暖米画布)   │ #1E1B17 (深褐画布)      │
│ --color-canvas-parchment │ #FAF6F1 (羊皮纸侧栏) │ #26221D (深色侧栏)      │
│ --color-rail          │ #EAE4DA (图标轨背景) │ #16130F (深暗轨背景)    │
│ --color-primary       │ #9A5F1A (暖金 600)   │ #E4B066 (暖金 300)      │
│ --color-ink           │ #221E1C (深墨文字)   │ #F2EDE6 (浅米白文字)    │
│ --color-hairline      │ #B2ADA9 (1px 弱边框) │ #4A453D (暗调边框)      │
└───────────────────────┴──────────────────────┴─────────────────────────┘
```

- **字体家族**：以 `-apple-system, "PingFang SC", "Noto Sans SC", "Segoe UI Variable", system-ui, sans-serif` 为基础，保证中文与西文在各桌面平台的原生高清晰度渲染。
- **排版节奏**：主标题 18px / 20px (600)，正文 16px (400, 1.5 行高)，次级文字 14px，元数据 12px。
- **间距标尺**：严格遵循 4/8px 节拍：`4px (xxs)`, `8px (xs)`, `12px (sm)`, `16px (md)`, `24px (lg)`, `32px (xl)`。
- **圆角标尺**：小按钮/标签 `8px`，输入框/卡片 `12px`，模态对话框 `16px`。

---

## 13. 分阶段 UI 暴露（Phased UI Exposure）

与 `product/development-plan.md` 严格同步，确保 UI 设计随着技术能力的演进而逐步、自然地展开，杜绝超前暴露未支持的前端假入口。

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        UI 阶段性暴露路线图                             │
├────────────────────────────────────────────────────────────────────────┤
│ 【P0 全局设计基座】                                                    │
│ └── 交付 DESIGN.md 暖金契约、app/tokens.css、52px 轨 + 240px 栏 + 840px 流 │
├────────────────────────────────────────────────────────────────────────┤
│ 【P1 基本可用 (阶段 1)】                                               │
│ └── 交付系统原生选夹、规划/实施/复核 3 名自带 Agent、1:1 纯净直聊、无 Provider 引导 │
├────────────────────────────────────────────────────────────────────────┤
│ 【P2 会话基础】                                                        │
│ └── 交付持久对话、结构化 Block 消息、就地决策、回复引用、输入草稿、图片附件 │
├────────────────────────────────────────────────────────────────────────┤
│ 【P3 会话整理】                                                        │
│ └── 交付收藏夹、对话全局搜索、标签批量整理、回收站还原、消息队列控制     │
├────────────────────────────────────────────────────────────────────────┤
│ 【P4 可信执行与任务真相】                                              │
│ └── 交付对话流内联执行证据、只读工作区文件树浏览、统一审批中心、审计时间轴 │
├────────────────────────────────────────────────────────────────────────┤
│ 【P5 知识与扩展】                                                      │
│ └── 交付记忆精准检索、能力画像建议、浏览器通知/PWA、受控外部扩展端口   │
└────────────────────────────────────────────────────────────────────────┘
```

### 13.1 P0：全局设计基座（Design Foundation）
- **UI 呈现**：暖米 + 暖金全局视觉基座；52px 图标轨、240px 会话侧栏、840px 居中聊天主区框架完整就位。
- **治理暴露**：任务、记忆、审批、审计图标就位，点击后打开按需视图，包含通用的 `← 返回对话` 逻辑。

### 13.2 P1：基本可用（Basic Usability）
- **UI 呈现**：
  - Header 挂载「打开文件夹」按钮，严格调用系统原生选择器，禁止路径手输。
  - 打开目录后自动展示规划、实施、复核 3 名自带 Agent 的角色头像与协作状态。
  - 未选目录时居中为 1:1 直聊。无 Provider 时提供醒目引导。

### 13.3 P2：会话基础（Conversation Core）
- **UI 呈现**：
  - 聊天流支持富结构化 Block（Proposal 提案卡、Checklist 检查清单、File Reference 冻结来源）。
  - 支持对历史发言的精准回复引用、Composer 草稿自动持久化恢复、图片附件粘贴与上传预览。

### 13.4 P3：会话整理（Session Management）
- **UI 呈现**：
  - 会话侧栏激活「收藏」Tab 与快速星标按钮。
  - 顶栏或侧栏增加搜索输入框（`Cmd/Ctrl + K`），支持游标分页与关键词高亮定位。
  - 侧栏底部「回收站」开放，支持已删除对话的列表浏览、彻底销毁或一键还原。

### 13.5 P4：可信执行与任务真相（Trusted Execution & Mission Truth）
- **UI 呈现**：
  - 对话流内联展示工作区沙箱执行过程、Diff 变更和验证日志。
  - 提供工作区只读文件树浏览浮层。
  - 统一审批中心全面接入高危越界操作；审计中心开放按领域筛选不可变事件。

### 13.6 P5：知识与扩展（Knowledge & Extensions）
- **UI 呈现**：
  - 记忆视图开放语义与关键词检索条。
  - 团队面板展示 Agent 能力画像与路由建议。
  - 接入浏览器推送通知与 PWA 安装提示。

---

## 14. 验收清单（Acceptance Checklist）

本清单用于 UI 开发完成后的功能自测、设计走查（Design QA）与自动化测试编写。

### 14.1 视觉与布局验收（Visual & Layout）
- [ ] **视觉基准一致**：所有颜色、边框、阴影严格调用 `DESIGN.md` 与 `tokens.css` 变量，无任何组件内写死 Hex。
- [ ] **双主题无缝切换**：在亮色与暗色主题下均经过实际渲染截图比对，无文字隐形、无边框消失、无对比度塌陷。
- [ ] **桌面三区尺寸精准**：图标轨固定 52px，会话侧栏固定 240px，居中聊天流最大限制 840px。
- [ ] **响应式退让合理**：视口小于 1024px 时，侧栏平滑收起为抽屉，图标轨稳定常驻。

### 14.2 交互与场景验收（Interaction & Scenarios）
- [ ] **原生选夹无手输**：点击打开文件夹百分之百调起系统目录选择器，无任何手输路径输入框。
- [ ] **未选项目直聊顺畅**：未选文件夹时，主界面不报错、不卡死，能够进行顺畅的 1:1 对话。
- [ ] **自带三 Agent 就绪**：项目就绪后，规划、实施、复核 3 名角色立即可用，输入 `@` 能准确触发补全。
- [ ] **治理视图一键返回**：从图标轨进入任务/记忆/审批/审计后，点击 `← 返回对话` 或按 `ESC` 瞬间返回且状态保留。
- [ ] **Needs Me 徽标联动**：当发生安全审批拦截时，Header 与图标轨徽标即刻亮起，点击能快速定位。

### 14.3 无障碍与全态验收（A11y & States）
- [ ] **全态矩阵 100% 覆盖**：所有核心按钮、输入框、消息卡片完整实现 Loading、Empty、Error、Disabled、Success、Focus 6 种状态。
- [ ] **触控热区达标**：所有可交互按钮与链接的物理或虚拟点击区域均 `≥ 44×44px`。
- [ ] **Axe 无 Critical / Serious 违规**：自动化无障碍扫描工具检查 0 致命缺陷。
- [ ] **纯键盘全流程闭环**：能够仅依靠 `Tab`、`Shift+Tab`、`Enter`、`Space`、`ESC` 及快捷键完成新建、选择项目、输入、审批与返回对话的全流程。
- [ ] **减弱动效生效**：在系统开启减弱动效时，动效自动降级为平滑透明度切换，无生硬抖动。
