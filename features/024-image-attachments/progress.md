# 进度

- 特性: 024-image-attachments（对应切片: S-16 / CI-2.10）
- 当前阶段: done
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: ship（全部 5 票完成；review 豁免生效中，收尾见 hf-ship）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture/tickets 由主会话按 backlog S-16 已确认条目直接产出
- 共享理解: backlog S-16 条目视为 auto-approved 2026-08-10；S-15 附件占位前置已于同日 ship

## 实施记录

- 2026-08-10 特性开立：S-16 前置（CAP-COL-01、CAP-PWS-01、CAP-EXE-01、S-15 附件占位）均已交付，直接进入 implement。
- 2026-08-10 T-01 完成（不提交 git）：RED 31/31 失败（service/两路由不存在）→ GREEN：schema identity 12→13 新增 message_attachments + attachment_events（(thread_id,sha256) 唯一、状态形状 CHECK、tuple FK、无内容列事件表）；attachment-service（magic bytes 四格式嗅探、服务端 SHA-256、同线程同 hash 复用、项目作用域落盘 + 规范化/.. 拒绝、事务内写盘失败回滚并清理残件；removeAttachment 删行+删字节+removed 审计，删文件失败整体回滚报错）；POST/DELETE 路由 + thread-attachments-api 共享助手（query name、415/413/400/404 矩阵、零路径回显）；composition 收编。identity 8 处同步 + write-ownership 补登。验证：聚焦 31/31、schema 两套件 + architecture 全套 + 三个 reopen 套件 22 文件 226 用例全绿、npx tsc --noEmit 绿。默认选择见 A-117；移除了一份未经 git 跟踪、从未编译通过的同名废弃草稿测试。
- 2026-08-10 T-02 完成（不提交 git）：RED 19/20 失败（attachmentIds 被 unknown 字段拒绝、content 路由不存在、DTO 无 attachments；startThreadRun 中性拒绝用例首轮即绿，属锁定测试）→ GREEN：parseMessageInput 接受可选 attachmentIds（≤4、严格形状、去重、归一 [] 入 requestHash）；消息提交事务内 linkMessageAttachmentsTx 校验同 tuple/uploaded/未占用并以守卫 UPDATE 原子 claim（status='linked'+message_id+linked_at+linked 审计事件），新增 after_attachment_link 故障点；ThreadMessageDto 恒在 attachments 引用数组（写响应/readThreadMessages/readThreadFacts 同源 rowid 序）；startThreadRun 不携带（中性 unknown，沿 A-115）；GET content 路由（tuple+linked 校验、白名单 Content-Type+nosniff+Cache-Control private, immutable、404 矩阵、linked 缺字节 503 脱敏）；readAttachmentContent 登记 queries 读面；UI 严格校验器同步（onboarding-guide-machine MESSAGE_KEYS+附件 ref 形状、collaboration-panel mutationIds 键集）与全部 fake/fixture 构造点。验证：聚焦 20/20；modules/public-collaboration+shared+architecture 58 文件 650 用例、browser collaboration/threads/onboarding 18 文件 183 用例全绿；npx tsc --noEmit 绿。默认选择见 A-118。
- 2026-08-10 T-03 完成（不提交 git）：RED 15 用例 7 类被 reopen 接受（linked_at 早于 created_at、5 种 storage_relpath 越界形状、附件漂移进另一既有 thread 而事件留在原 tuple）；3 类消息边 corruption（不存在/跨 thread/跨 project 的 message_id）已被 reopen 期 PRAGMA foreign_key_check + 复合 tuple FK(project_id,thread_id,message_id) 拦截（锁定用例首轮即绿）；4 类 status 机矛盾（linked 缺 message_id/linked_at、uploaded 带 message_id/linked_at）在写入期即被表 CHECK 拒绝、磁盘上不可表示，改为断言写入 fail-closed + 合法图仍幂等 reopen。GREEN：current-data-invariants 新增三条——status 机列组合（linked⇔message_id+linked_at 非空，异或双向覆盖 uploaded）+ linked_at≥created_at；storage_relpath 精确等于 project_id||'/'||id（单写者事实，含相对/无 .. 无反斜杠无绝对）；attachment_events↔message_attachments 现存行 tuple 一致（removed 行无 JOIN 不波及审计保留）。验证：合法 fresh/reopen 双次幂等、消息附件投影不变；全部 corruption 稳定 SCHEMA_DATA_INVALID 脱敏失败、user_version 保持 13、行快照零变化；聚焦 15/15，tests/adapters/sqlite + tests/modules/public-collaboration 64 文件 759 用例全绿，npx tsc --noEmit 绿。默认选择见 A-119。
- 2026-08-10 T-04 完成（不提交 git）：RED 9/9 失败（选择/粘贴零 XHR 上传、无进度 chip、无重试/移除、发送不携 attachmentIds、消息无 img、3 键草稿附件被严格解析整体判 invalid 不恢复、客户端无类型/大小/数量字段级错误）→ GREEN：新增 attachment-upload.ts 公共缝模块（XHR upload progress、query name、响应 envelope 严格校验 tuple/status/reused、非 2xx/网络/abort 统一脱敏失败）；composer chips 状态机 placeholder/uploading/failed/uploaded（setChips 同步 ref 防 effect 镜像竞态）；选择与粘贴（image/* clipboardData）共用通道，客户端先行类型白名单/5 MiB/≤4 个字段级中性错误（composer 上限 8→4 与服务端对齐）；失败可重试（同 File 重传，hash 复用得同 id）或移除，uploaded 移除先 DELETE 成功才本地移除；非 uploaded chip 禁用发送+muted 提示，handleSubmit 字段级双保险；run-start 携带附件给"暂不能携带附件"中性错误（沿 A-115/A-118）；提交体携 uploaded attachmentIds，确认后 chips 随草稿清空；同内容重复选择上传成功后按同 attachmentId 折叠单 chip；thread 切换 abort 在途上传且迟到完成不落旧线程。消息渲染：transcript 条目按 messageId 查 projectMessagesPage（thread /messages 全页加载恒在附件），`<img loading=lazy src=content 路由 alt=fileName>` + muted mimeType/size 元信息，无自定义 loading/error 占位（原生 alt 兜底不伪造内容）；.message-attachments 样式全 tokens。草稿升级：ThreadDraftAttachmentDto 增可选 attachmentId（2 键占位/3 键引用，RESOURCE_ID 形状校验、服务端不核对存在性由发送命令兜底），恢复时 3 键→已上传 chip、2 键→需重新选择 chip 且阻塞发送；ProjectMessage 类型补恒在 attachments（校验器早已强制），run-service 遗留 projectMessages 页补真实附件投影、insertOwnerMessage 显式 []。验证：tests/browser/collaboration 91 用例全绿（含新套件 10 用例：双通道上传/进度/重试移除/越界矩阵/发送门禁与携带/run-start 阻断/重复折叠/切换 abort/恢复升级/图片渲染），thread-draft 模块套件 47 用例（新增 attachmentId round-trip + 3 类非法形状拒绝），tests/browser + modules/public-collaboration + architecture 合计 1117 用例全绿，npx tsc --noEmit 绿。默认选择见 A-120。
- 2026-08-10 T-05 完成（不提交 git）：smoke:threads 新增图片附件验收段（5 个 pass 组、axe 累计 14 态、全量 26 断言全绿），并把 T-04 前的草稿段从 text/plain 占位升级为真实 PNG 上传（上传 201、草稿 attachments 携 attachmentId、刷新恢复"已上传"、孤儿移除 DELETE + 草稿清空，避免阻塞后续 run-start）。矩阵落点：① 选择注入确定性 1×1 PNG（setInputFiles）→ 真实 XHR 进度事件（initScript 观测 loaded>0）+ chip 终态"已上传"；粘贴以 dispatchEvent 合成 ClipboardEvent(DataTransfer/File) 覆盖（同通道真实上传 201）。② 发送携 2 个 attachmentIds → 201、消息 DTO attachments 2 项、transcript 内 `<img>` naturalWidth>0 真实解码、alt=fileName、mimeType/size 元信息可见、两图经投递路由 200 + 白名单 Content-Type + nosniff + private, immutable。③ 进程重启后新冷缓存 context 中消息与 `<img>` 仍在，投递路由 fetch 200 + 全套响应头 + 字节逐位等于原始 PNG/GIF + createImageBitmap 解码 1×1；DB attachments 两行 linked/messageId/relpath=project/id 与事件 uploaded×3+removed×1+linked×2 跨重启 deepEqual 不变；磁盘 tmp attachments 根恰剩 2 个已链接字节文件、孤儿字节已删。④ 客户端越界：text/plain 与 >5 MiB 各一（字段级中性错误、零上传请求出网）；服务端兜底：伪造 magic 400 INVALID_INPUT、5 MiB+1 413 BODY_TOO_LARGE，均零路径回显。⑤ desktop light/dark + narrow 三态 axe 无 serious/critical 且 AA 对比度通过；narrow 下附件列表宽度 ≤390、添加附件 44×44 且键盘可达；证据落 features/024-image-attachments/evidence/attachments-{desktop,narrow}.png。⑥ 密钥/主机路径扫描扩展到 attachments 根（DOM/API/log/DB 零回显）；git check-ignore .data/attachments/* 通过、git status --porcelain .data/ 为空、仓库内 .data/attachments 不存在（既有 .data/ 规则覆盖，无需补 .gitignore）。已知环境限制：Chromium 对被强制结束的 dev server 之原页 `<img>` 子资源偶发挂起（请求 requestfinished 但元素不落 complete，新建 context 与直接 fetch 均正常），故重启后持久化验证落在新冷缓存 context 的 fetch+字节+解码上（矩阵第 3 项原文即"投递路由 200、白名单头、nosniff"）；既有 restart 段个别轮次亦受同类环境抖动，未改其语义；未触碰 review-browser-full-chain flaky。完成后全量 npx vitest run 243 文件 2069 用例全绿（108s）、npx tsc --noEmit 绿、npm run build 绿。默认选择：验收以真实路由响应头/字节/可解码性为持久化事实源，未为 `<img>` 重启瞬时态另造断言。
