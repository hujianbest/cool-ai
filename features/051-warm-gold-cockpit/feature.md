# 051 — 暖金驾驶舱（warm beige + gold 全局 UI 重构）

- 对应切片: S-61
- 模式: 建造
- 用户可感知: 是
- 状态: done（2026-08-18 ship）
- 一句话: 从全局设计出发，把驾驶舱整体切换为暖米 + 暖金视觉方向，并收敛为 Clowder 式「图标轨 + 会话侧栏 + 聊天主区」的会话优先壳层；治理面板收进图标轨按需打开。

## 用户确认（2026-08-15）

1. 色板：整体换成暖米 + 暖金视觉方向，替换当前陶土绿 accent。
2. 治理面板：使命/记忆/审批/审计全部收进左侧图标轨，主路径只留会话。
3. 落地方式：先产出 DESIGN.md（令牌 + 壳层规范），再按「图标轨 → 会话栏 → 聊天主区 → 空状态」四步改壳层，最后逐页收敛。

## 范围

- 重写 `product/ui/DESIGN.md` 为暖金驾驶舱设计契约（颜色/字体/间距/圆角/组件/IA）。
- `app/tokens.css` 亮暗两套令牌切换到暖米 + 暖金，并新增壳层布局令牌（52px 图标轨、240px 侧栏、840px 聊天流）。
- 壳层四步：图标轨（含治理入口）→ 会话侧栏（过滤 tabs、回收站、新建对话）→ 聊天主区（header、居中流、composer）→ 空状态（欢迎 + @Agent 引导）。
- 治理视图（使命/记忆/审批/审计）由图标轨按需打开，不占主路径。
- 同步更新视觉契约测试（theme-tokens / visual-tokens / preview-pages / cockpit-layout）。

## 范围外

- 不复制 Clowder AI 的名称、猫角色、商标、文案或品牌资产；只取信息架构与视觉方向。
- 不改后端 API、schema 与领域行为；本切片纯前端壳层与令牌。
- 不重做协作/执行/复核等业务面板的领域逻辑；只做视觉收敛与按需展示。
