# 进度

- 特性: 025-thread-favorites（对应切片: S-19 / CI-2.7）
- 当前阶段: done
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: 全部 3 票完成，进入 ship（演示验收记录见下）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture/tickets 由主会话按 backlog S-19 已确认条目直接产出
- 共享理解: backlog S-19 条目视为 auto-approved 2026-08-10

## 实施记录

- 2026-08-10 特性开立：S-19 前置（CAP-COL-02 Thread Catalog 核心、S-9 偏好 Adapter）均已交付，直接进入 implement。
- 2026-08-10 T-01 收藏命令与列表投影完成（auto，review 豁免）：identity 13→14 新增 `thread_favorites`（PK tuple、FK→collaboration_threads ON DELETE CASCADE），同步 current-schema/rejection/reopen/smoke 全部 identity 引用与 write-ownership manifest，新增 favorites 目标存在性不变量；`setThreadFavorite` 幂等 upsert/delete（draft 先例无 receipt），重复 true 保留原 created_at；`listThreads` 恒在投影 isFavorite/favoritedAt + `favoritesOnly` 过滤（favorited_at DESC, thread_id ASC，普通列表不变），收藏视图 cursor 拒绝 not_supported 且 nextCursor 恒 null；共享契约下沉 ThreadListItemDto/ThreadListResponseDto/ThreadFavoriteSetResponse（detail 用 ThreadSummary 不动，THREAD_KEYS 白名单不受影响）；路由取最少面：列表 GET `favorites=true` + PUT `/threads/[threadId]/favorite`（严格 DTO）；装配根登记 threadFavoriteService；组件侧新增 threadListItemSchema 与共享测试夹具同步。RED（11 例全红：命令/投影/路由不存在）→GREEN 全绿；聚焦套件 thread-favorite 11/11，modules/public-collaboration+adapters/sqlite+architecture 800/800，受影响 browser 套件（threads/collaboration/cockpit-shell/onboarding）257 全过（修复 2 处列表夹具缺新字段），`npx tsc --noEmit` 干净。默认决策见 A-121/A-122。
- 2026-08-10 T-02 列表星标与收藏视图 UI 完成（auto，review 豁免）：`project-thread-navigation.tsx` 列表项新增星标 toggle（SVG currentColor 图标、aria-pressed、可访问名称“收藏线程 X/取消收藏 X”、--control-min 44px、原生键盘与全局 focus-visible）；乐观更新（本地 ISO 占位）+ PUT 严格校验 tuple 对账 + 失败回滚与 inline role=alert 脱敏错误，请求期间按 threadId disabled；收藏视图复用 role=tablist/tab 分段控件（全部/已收藏，方向键+漫游 tabIndex），GET `favorites=true` 单页（组件断言 nextCursor null、全 isFavorite、favorited_at DESC/id ASC 序，fail-closed）；视图切换不触碰 URL 选择、不在收藏视图做无效选择报错/自动跳转（消息区保持现状）；收藏视图内取消即乐观移除、失败按稳定序插回，取消至空派生 empty 渲染（listState “empty” 语义仍专属“项目无线程”）；创建入口仅在“全部”视图；空收藏态文案+“查看全部线程”回退；样式全部 tokens（cockpit.css 新增 thread-view-tabs/thread-list-item/thread-favorite-toggle）。RED（8 例全红：控件/视图/回滚/empty 缺失）→GREEN 全绿；聚焦 tests/browser/threads 50/50，受影响套件（collaboration/cockpit-shell/onboarding/project-context/execution/review/team-settings）439/439，`npx tsc --noEmit` 干净（夹具 thread() favoritedAt 拓宽 string|null）。默认决策见 A-123/A-124。
- 2026-08-10 T-03 真实浏览器验收收藏完成（auto，review 豁免）：`persistent-threads-browser-smoke.mjs` 新增 favorites 验收段（置于附件段后、复用既有 restartApp 环节），inspectDatabase 增加 thread_favorites 行投影，axe 助手在 results.json 补记全部 violation id/impact（原仅记 blocking/contrast 明细）。验收路径：desktop light 下 Tab 自线程条目聚焦星标（focus ring 可见、44px）→ Space 收藏 Distinct title、Enter 收藏历史协作（PUT 200 严格 4 键响应）→ API `favorites=true` 验证 favorited_at DESC（nextCursor 恒 null）与普通列表投影不受影响 → 切“已收藏”tab 验证 DOM 序 [历史协作, Distinct title]、aria-selected 切换、URL 线程上下文保留且无“所选线程无效”→ 视图内取消收藏即消失；desktop dark 复验 aria-pressed；narrow 抽屉内切收藏视图（44px、Tab 可达、focus 可见）；restartApp 后 DB 行 deepEqual 保持、API favoritedAt 逐字一致、星标仍 pressed、收藏视图仍列出。矩阵覆盖：desktop/narrow × light/dark 各至少一次关键路径，keyboard Tab+Space/Enter，focus 可见，44px 双形态。结果：smoke 全绿 assertions=30（新增 favorites 4 项）、axeStates=18（新增 favorites 4 状态：desktop light/dark、narrow、重启后），serious/critical=0、color-contrast=0；唯一非阻断项为 narrow 抽屉 aria-allowed-role minor（既有 aside role=dialog 抽屉模式，与收藏无关，本片不修，见 A-125）。修复 2 处既有松散选择器（`Duplicate title`/`Distinct title` 的 getByRole 子串匹配被星标可访问名称命中，加 exact:true）——属测试选择器缺口，非生产缺陷。一次性全量：`npx vitest run` 244 文件/2089 测试全过（110.9s，基线内），`npx tsc --noEmit` 干净，`npm run build` 通过。证据：features/025-thread-favorites/evidence/favorites-desktop.png、favorites-narrow.png（新增），results.json 含全部断言与 axe 明细；秘密扫描（DOM/DB/API/日志/证据）无泄漏。默认决策见 A-125。
