# 进度

- 特性: 030-collaboration-audit-events（对应切片: S-23 的 AUD-COL 纵切 / CI-3.8 第二子片）
- 当前阶段: done（T-03 已验收，待 ship 收尾）
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: ship（评审豁免；任务票已全勾，验收与全量证据齐备）
- 用户可感知: 是（审计中心扩展协作事件）
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture 由主会话按 backlog S-23/CAP-COL-07 已确认条目直接产出
- 共享理解: backlog 相关条目视为 auto-approved 2026-08-10

## 实施记录

- 2026-08-10 特性开立：AUD-COL 解锁 S-17 线程搜索（其准入要求 AUD-COL 先证明 Collaboration 事件可查询/导航）并推进 S-23 汇总。028 AUD-MVP 基座（CAP-OPS-01/02）今日 ship，consumer 协议 source 无关，本片预期显著小于 AUD-MVP。
- 2026-08-10 T-01 完成（不提交 git）：schema identity 15→16，`audit_event_outbox.source` CHECK 放宽为 `('safe_execution','public_collaboration')`（projection 表 source 列本无 CHECK，无需同步），全部 identity 引用同波次更新。新缝 `public-collaboration/audit-event-outbox.ts`（028 同构）：选型常量 16 类型入列（run 生命周期六件/run_planned/boundary_paused/owner_message/agent_message/handoff/decision_requested/decision_answered/tasks_created/task_claimed/action_rejected/context_changed），model_call_*/usage_recorded/attempt_interrupted 噪声不入列；白名单扁平公开标量键 + 公开文本键 200 grapheme 截断 + public-text 分类缝校验，拒绝降级 `[redacted]` 不阻断业务写；owner/agent_message 以 messageId 回查正文生成 messageExcerpt；tasks_created.items 只留 taskCount。写入点：5 个 live collaboration_events 写入点（thread-service startThreadRun、run-service controlThreadRun/answerThreadDecision、turn-orchestrator/action-committer 的 appendEvent）同事务追加；owner 消息无事件对应行，镜像挂 appendBatchTx 单点（type='owner_message'，eventId=factId，runId 可空）覆盖全部现在与未来事实写入点；run-service 四个 legacy 项目级函数共享的 appendEvent helper 因自身 INSERT 缺 NOT NULL thread_id/project_id 在 current schema 下不可达成功，确认不接线（A-161）。write-ownership：manifest 新增顶层 `sharedAppendWriters` 永久机制登记 audit_event_outbox 多源追加（safe-execution+public-collaboration），writers.test 放行登记并集，TRANSITIONAL 豁免清单不动。验证：原子回滚（after_event 注入故障双表零痕迹）、白名单精确等值断言无敏感键、200 grapheme 截断、凭据内容 [redacted]、选型 16 类型逐一行为覆盖、噪声零 outbox 行、seq 单调、reopen 幂等（行一致+identity 16）。另修复前序会话遗留：grapheme 截断把 Intl.Segmenter segment 对象 join 成 "[object Object]" 的缺陷、测试文件整段重复块。聚焦 14/14 绿；tests/adapters/sqlite 14 文件 170 用例、tests/architecture 6 文件 25 用例、tests/modules/public-collaboration+operations-projection 56 文件 685 用例全绿；`npx tsc --noEmit` 绿。默认选择见 product/assumptions.md A-159～A-163。全量套件 260 文件 2283 用例全绿，墙钟 132.68s（超出 2026-08-09 基线 98s 约 35%，主要为 021～029 系列特性用例累积增长，本票新增仅 11 个内存库用例；测试执行速度治理属特性 020 范畴，在此标记待关注）。
- 2026-08-10 T-02 完成（不提交 git）：审计面板扩展协作呈现与定位，4 轮 RED/GREEN（文案映射→域徽标→摘要截断→定位链接），每轮先一个行为缺失 RED 再最小 GREEN。域分类走 UI 侧类型映射集（协作文案映射镜像服务端 16 类型闭集，命中即协作），wire DTO 零协议改动；徽标复用 `.status-label` status-running/status-queued 既有变体零新增颜色；messageExcerpt 仅消息类渲染，新增 `.audit-event-excerpt` tokens 截断规则；协作定位落规范目标身份链接（thread 必带、run 可空省略），message 精确跳转经勘察不可复用（面板内部 ref 机制 + 项目选择路由只认 thread/run），如实链接到 thread/run 级；payload 引用畸形不渲染链接。执行域既有定位/分页/freshness/empty/error/防陈旧行为零改动（既有 8 用例全绿锁定）。聚焦 12/12 绿、project-context 14 文件 74 用例绿、`npx tsc --noEmit` 绿、ReadLints 零告警。默认选择见 product/assumptions.md A-165～A-167。全量套件与浏览器冒烟按纪律留待 T-03 收尾一次性运行。
- 2026-08-10 T-03 完成（不提交 git）：真实浏览器验收段落点勘察后择定 smoke:threads（persistent-threads-browser-smoke.mjs）——其真实协作造数现成（7 条 owner 消息含回复/附件、2 次 run 生命周期、decision_requested、跨线程隔离），smoke:execution 保留 028 执行域审计段不混入协作造数；验收段置于 favorites 重启段之后、终扫之前，证据落 features/030-collaboration-audit-events/evidence/ 三截图（desktop light/dark/narrow）。验收路径全绿：①API 走查——14 协作事件单页 caught_up、全局 DESC、五必备类型齐备（owner_message/agent_message/run_started/run_stopped/decision_requested）、messageExcerpt 逐字投影（"Owner message with image attachments."）、run 事件携规范 threadId+runId、run-less owner 消息 runId=null、foreign-project 404 且无宿主路径回显、API JSON 对 apiKey/masterKey/Bearer/临时目录/工作区/附件根/Authorization 零泄漏；②投影一致性——outbox==projection==API 事件数==14、checkpoint==MAX(outbox_seq)、source 全为 public_collaboration、噪声类型（model_call_*/usage_recorded/attempt_interrupted）零 outbox 行；③桌面 UI——tablist End 键选中审计 tab（aria-selected+activeElement 实核）、14 行全渲染且每行协作徽标（status-queued 复用变体）、摘要行 `.audit-event-excerpt` 逐字呈现、定位链接 href 规范身份（run_stopped→`?thread=…&run=…`、run-less 消息→仅 thread）、链接 focus 可见焦点环、Enter 真实导航落点以运行选择器 inputValue 实核、tab/链接 44px、axe desktop light/dark 各一次；④窄屏——抽屉键盘开启、tab Enter 选中、首行文案/摘要/链接 href/44px、axe 一次、Escape 焦点归还 opener。axe 22 态 blocking 与 color-contrast 全零；唯二违规为既有 aria-allowed-role:minor@task-context-drawer（A-125/A-135/A-148 同例，低于门禁不修），审计面板三态零违规且同页 memory tab 基线零违规（无 chrome 归因需求）。秘密扫描面扩展 auditFacingText/narrowAuditFacingText/三截图字节，终扫 no-secret-db-api-dom-evidence 全绿；消息摘要可见属预期。一次性全量：`npx vitest run` 260 文件 2287 用例全绿、墙钟 109.32s（较 T-01 的 132.68s 回落至约 2 分钟基线内，020 治理生效）；`npx tsc --noEmit` 绿；`npm run build` 绿。未发现生产缺陷，零生产代码改动。默认选择见 product/assumptions.md A-168。
