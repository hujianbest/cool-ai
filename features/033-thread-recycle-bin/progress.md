# 进度

- 特性: 033-thread-recycle-bin（对应切片: S-20 / CI-2.8）
- 当前阶段: implement（T-01..T-06 已勾选，待 ship 收口）
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: 进入 ship（整理验收证据与切片提交）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（AGENTS.md 2026-08-09 起生效）；不伪造评审工件；spec/architecture/tickets 由主会话按 backlog S-20 已确认条目直接产出
- 共享理解: backlog S-20 条目视为 auto-approved 2026-08-11

## 实施记录

- 2026-08-11 特性开立：S-20 准入全部成立——`CAP-COL-02` Thread Catalog 核心已交付；阻塞前置 S-14（回复引用来源占位，022 于 2026-08-10 ship）与 S-24（`CAP-GOV-02` 跨域统一审批中心，029 于 2026-08-10 ship）均已交付；同模块最新先例 S-18（032 于 2026-08-11 ship）。spec/architecture/tickets 同批产出；规格/架构评审按项目级 review 豁免跳过并在此记录。
- 2026-08-11 关键默认决策（台账 `product/assumptions.md` A-207..A-222）：软删=`deleted_at` 列标记 + 全缝排除（A-208/A-209）；软删/恢复幂等 receipt-less（A-210）；非终态 run 守卫（A-211）；回收站独立查询缝（A-212）；永久删除确认=UI 强确认不走 CAP-GOV-02（A-213）；purge 仅自回收站（A-214）；触发器门=瞬时 purge 标记表（A-215）；purge 单事务显式级联序（A-216）；executions provenance 守卫（A-217）；`next_activity_sequence` 重算（A-218）；审计三事件落 outbox 且历史行保留（A-219）；搜索索引软删留行/purge 清行/consumer 零改动（A-220）；系统线程现状与未来约束（A-221）；UI 形态与当前导航占位（A-222）。
- 2026-08-11 T-01 完成（项目级 review 豁免，实现委派 subagent）：两轮 RED/GREEN——R1 schema（`thread-recycle-bin-schema.test.ts` 6 断言先红：identity 19/`deleted_at`/部分索引/`thread_purge_markers`/三触发器豁免/markers 非空 reopen 失败关闭）→ GREEN（`current-schema.ts` identity 18→19 + 列/索引/表/触发器 WHEN 扩展；`current-data-invariants.ts` +1 空表断言；identity 引用同波次同步 15 文件；write-ownership manifest 登记 + `thread-purge-marker-store.ts` 锚写入器满足 writers 护栏）。R2 排除（`thread-recycle-bin-exclusion.test.ts` 13 断言 11 处泄漏先红）→ GREEN（新 `active-thread-guards.ts` 统一 `ensureActiveThread`/`threadDeletedNotFound`；`CollaborationError.details.reason="thread_deleted"` 经 `collaborationErrorBody` 与 `CollaborationApiError` 落 wire；thread-service/run-service/draft/attachment/favorite/tag/inline-decision/run-timeline 全缝收口；`listThreads` 三变体、`searchProjectThreads` HIT_SELECT、`searchInputHistory`、`listProjectTags` threadCount 增活跃谓词；`requireBatchThreads` 只计活跃）。验证：聚焦 83 文件 1004 测试全绿（adapters/sqlite + public-collaboration + operations-projection + architecture），`npx tsc --noEmit` 干净；排除矩阵覆盖列表/搜索/收藏/标签筛选/输入历史/详情/消息/facts/草稿/附件/运行/操作，reason 标记仅 tuple 内出现，跨 tuple 与缺失线程保持无标记 404。实现默认落台账 A-223。未 commit/push。
- 2026-08-11 T-02 完成（项目级 review 豁免，实现委派 subagent）：四轮 RED/GREEN——R1 `deleteThread` 缝（7 用例先红：模块不存在）→ GREEN（新 `thread-lifecycle-service.ts`：幂等冻结首删 deletedAt/零重复审计、非终态 run 守卫 409 `fields.threadId="has_active_run"`、同事务 UPDATE deleted_at/updated_at/version + `appendCollaborationAuditOutboxRow` 写 `thread_deleted`；`AUDITABLE_COLLABORATION_EVENT_TYPES` +3（deleted/restored/purged）+ `EXCERPT_TEXT_KEYS` +title）。R2 `restoreThread` 缝（4 用例先红：导出不存在）→ GREEN（幂等 `restored:false`、同事务清 deleted_at + `thread_restored`）。R3 路由（6 用例先红：DELETE 处理器与 restore 路由文件不存在）→ GREEN（`DELETE …/threads/[threadId]` 忽略 body/严格 URL 校验；`POST …/threads/[threadId]/restore` 严格空 JSON `{}`；两路由全响应 no-store + 脱敏 envelope 保留 fields/reason；DTO `ThreadDeleteResponse`/`ThreadRestoreResponse` 下沉 `src/shared/collaboration-contracts.ts`；装配根登记 `threadLifecycleService`）。R4 审计中心文案（1 用例先红：三类型落原始串+执行域徽章）→ GREEN（`COLLABORATION_EVENT_TYPE_COPY` 补「线程已移入回收站/线程已恢复/线程已永久删除」，in-map 即协作域）。验证矩阵全覆盖：幂等矩阵（重复删除零新审计事件+冻结首删时间、重复恢复 restored:false）、run 守卫四非终态各一+failed/stopped 放行、审计事件落盘与载荷脱敏（凭据标题 [redacted]、250 字标题截 200 grapheme+…）、恢复后列表/收藏/标签/草稿还原且排序位不变、跨 tuple 无标记 404、路由严格校验（query/fragment/path id/body 形态/媒体类型）。聚焦全绿：`tests/modules/public-collaboration` 58 文件 718 测试、`tests/browser/project-context/audit-panel` 13 测试、architecture+operations-projection 12 文件 134 测试；`npx tsc --noEmit` 干净。实现默认落台账 A-224/A-225。未 commit/push。
- 2026-08-11 T-03 完成（项目级 review 豁免，实现委派 subagent）：两轮 RED/GREEN——R1 查询缝（新套件 `tests/modules/public-collaboration/thread-recycle-bin.test.ts` 8 用例先红：`listDeletedThreads` 导出不存在）→ GREEN（`thread-lifecycle-service.ts` 增 `listDeletedThreads`：`WHERE project_id=? AND deleted_at IS NOT NULL` + `deleted_at DESC, id ASC` 决胜、游标 `{v:1,d,id}` base64url JSON 同构 listThreads 编码（canonical 回环严格校验，d=deleted_at ISO 串）、limit 默认 50 上限 100、messageCount/attachmentCount 页面 id 批量两条 GROUP BY 第二查询无 N+1（附件计 tuple 全部行含 uploaded 未链接）；DTO `RecycleBinItemDto`/`RecycleBinListResponseDto` 下沉 `src/shared/collaboration-contracts.ts` 并经 dto.ts re-export、`PublicCollaborationQueries` 登记查询缝）。R2 路由缝（6 用例先红：`GET …/thread-recycle-bin` 路由文件不存在）→ GREEN（`app/api/projects/[projectId]/thread-recycle-bin/route.ts`：`[projectId]` 下新静态段与 threads/thread-tags/thread-tag-batch 平级零碰撞；query 白名单 `{cursor,limit}` 单值（unknown/duplicate/fragment 400）、limit 形态/范围校验、cursor 不透明透传由服务缝校验编码；全响应 no-store + `collaborationErrorBody` 脱敏保留 fields；经 `threadLifecycleService` 命名空间调用零新装配边）。验证矩阵全覆盖：倒序与同刻 id ASC 决胜、游标翻页与全量列表逐位一致且无重复、消息/附件计数如实（2 消息 1 链接附件/仅上传未链接 1 附件/零计数恒在）、空页 200 `{threads:[],nextCursor:null}`、只含已删（活跃线程不出现、恢复后出列）、跨项目隔离（他项目回收站空页、missing project 404 PROJECT_NOT_FOUND）、重启一致（新鲜连接两次查询逐字节相等）、路由严格校验（白名单/单值/fragment/path id/limit/cursor）与错误体不泄漏 databasePath。聚焦全绿：`tests/modules/public-collaboration` 59 文件 732 测试；`npx tsc --noEmit` 干净。实现默认落台账 A-226/A-227。未 commit/push。
- 2026-08-11 T-04 完成（项目级 review 豁免，实现委派 subagent）：两轮 RED/GREEN——R1 命令缝（`tests/modules/public-collaboration/thread-lifecycle.test.ts` 新增 purge 断言先红：`purgeThread` 缺失、触发器门负例缺失、executions 守卫缺失）→ GREEN（`thread-lifecycle-service.ts` 增 `purgeThread`：tuple+回收站校验、executions 预检 409 `fields.threadId="has_executions"`、事务内 `insertThreadPurgeMarkerTx`→显式删除 facts/state_heads/state_revisions/blocks/business_action_receipts/inline_decisions→`deleteThreadSearchIndexRowsTx` 清索引→收集 attachment `storage_relpath`→`DELETE collaboration_threads` 级联余图→同事务 unlink 字节失败即回滚→重算 `next_activity_sequence=1+COALESCE(MAX(...),0)`→删 marker→写 `thread_purged` 审计并提交；返回 `ThreadPurgeResponse`）。R2 路由缝（`app/api/projects/[projectId]/threads/[threadId]/purge/route.ts` 先红后绿）→ GREEN（`POST` 严格空 JSON `{}`、拒绝 query/fragment/尾缀、脱敏错误映射、全响应 `cache-control: no-store`）。验证矩阵全覆盖：逐表零残留、无 marker 直删 facts 触发器 ABORT、executions 引用稳定 409 且 FK 兜底、附件字节实删、搜索零命中、重试 purge 明确 404、审计 outbox 落 `thread_purged`、跨 tuple 404。聚焦验证全绿：`npx vitest run tests/modules/public-collaboration/thread-lifecycle.test.ts`（28/28）；`npx vitest run tests/modules/public-collaboration tests/adapters/sqlite tests/modules/operations-projection`（79 文件 1021 用例）；`npx tsc --noEmit` 干净。实现默认落台账 A-228。未 commit/push。
- 2026-08-11 T-05 完成（项目级 review 豁免，主会话实现）：三轮 RED/GREEN——R1 回收站导航缝（`tests/browser/threads/thread-recycle-bin-ui.test.tsx` 首轮红：缺第三 tab/空态/删除确认）→ GREEN（`components/project-thread-navigation.tsx` 增 `recycle_bin` 视图状态机、`listDeletedThreads` 拉取与 cursor 分页、列表项「移入回收站」轻确认、回收站行内「恢复/永久删除」与强确认、`has_executions` 409 行内 `role=alert`、删除当前线程回项目根、静默刷新与 target guard）。R2 当前导航占位缝（同套件红：`thread_deleted` 404 未落占位）→ GREEN（`components/collaboration/collaboration-panel.tsx` 识别 404 reason=`thread_deleted`，渲染「该线程已移入回收站。」+「恢复线程/返回线程列表」，恢复走既有 restore 路由并回填成功状态，保持 epoch+Abort 防陈旧）。R3 样式契约缝（同套件红：缺回收站 token 规则）→ GREEN（`app/cockpit.css` 补 `.thread-recycle-*`、`.thread-delete-confirm`、`.thread-purge-confirm`、`.thread-deleted-placeholder` 全 token 规则，`thread-recycle-item-action` 明确 `min-height/min-width: var(--control-min)`）。验证：`npx vitest run tests/browser/threads/thread-recycle-bin-ui.test.tsx`（5/5）、`npx vitest run tests/browser/threads`（8 文件 94 用例全绿）、`npx tsc --noEmit` 干净。实现默认落台账 A-229。未 commit/push。
- 2026-08-12 T-06 完成（项目级 review 豁免，主会话实现）：延续既有 033 验收段并补齐稳健性修复——`thread-tags-ui` 歧义 `role=status` 断言改为精确文案；回收站恢复排序基线改为删除前快照；输入历史空结果等待加入一次重试；当前线程“返回线程列表”断言改为作用域点击并等待回收站视图可见；永久删除段兼容瞬时 503 后重试；执行守卫分支兼容 `has_active_run` 先停后删与 purge 结果分支。实现侧同步修复：`restoreThread`/`deleteThread` 不再改写 `updated_at`，避免恢复后列表顺序漂移；`purgeThread` 仅在无跨线程引用时 unlink 字节并容忍 ENOENT，避免哈希去重附件导致假性 503。一次性验证通过：`npm run smoke:threads` PASS（`THREAD SMOKE PASS`，含 033 四张证据图）、`npx vitest run` PASS（272 文件 2499 用例）、`npx tsc --noEmit` PASS、`npm run build` PASS。