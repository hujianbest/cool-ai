# 进度

- 特性: 027-workspace-readonly-browser（对应切片: S-22 / CI-3.2）
- 当前阶段: done（T-01～T-04 全部完成，待 ship）
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: ship（评审按项目级豁免；验收记录与证据已落盘，commit/push 待用户明确要求）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture 由主会话按 backlog S-22 已确认条目直接产出
- 共享理解: backlog S-22 条目视为 auto-approved 2026-08-10

## 实施记录

- 2026-08-10 特性开立：S-22 前置 `CAP-PWS-01`、`CAP-EXE-01` 已交付核心、无阻塞项；选它因解锁 S-26/S-28/S-34/S-42/S-43。勘察结论：现有 WorkspaceBinding 为单根（path+status ready），verified-handle 设施在 `src/adapters/outbound/workspace/`；多根管理 UI 与 diff 预览依 spec 记为范围外/勘察后假设。
- 2026-08-10 T-01 完成（只读浏览查询与路径安全，实现 subagent）：两轮 RED→GREEN——查询缝 RED（服务模块不存在）→ GREEN 30 例全绿；路由缝 RED（两路由不存在）→ GREEN 7 例全绿。产出：模块公开 queries/dto/errors 扩展（`listWorkspaceDirectory`/`readWorkspaceFilePreview` 判别联合预览）；新 Adapter `src/adapters/outbound/workspace/workspace-browse-adapter.ts`（verified 遍历、敏感词汇与分类常量集中一处、512KiB 文本截断、2MiB 图片 dataUrl 内联上限）；`workspace-browse-service.ts`（tuple 校验 + 绑定根解析）；路由 `workspace/files` 与 `workspace/file` 两个 GET + `_shared/workspace-browse-api.ts` 传输助手（严格 path 校验、脱敏 envelope、no-store）；装配根登记 `workspaceBrowseService`；imports.test 登记一条 sqlite→workspace 过渡边（027 T-01）。假设 A-130（查询/分类/遮蔽默认）、A-131（路由默认）。验证：`tests/modules/project-workspace/` 9 文件 61 例、`tests/architecture/` + `tests/adapters/workspace/` 全绿；`npx tsc --noEmit` 通过。评审按项目级豁免跳过；全量测试与构建留待 T-04 收尾。
- 2026-08-10 T-02 完成（目录树 UI，实现 subagent）：五轮 RED→GREEN——树渲染/排序/aria RED（组件不存在）→ GREEN；懒加载展开/收起/缓存 RED → GREEN；键盘全键位+Enter 选择回调 RED → GREEN；状态矩阵（根 loading/empty/error+重试、分支级 inline 错误+重试不崩整树、敏感徽章可选中）RED（一处为测试夹具自身 503 逻辑错误，修正后过）→ GREEN；target switch epoch 防陈旧一轮即过（护栏已随 GREEN 内建）。产出：新组件 `components/project-context/workspace-file-tree.tsx`（扁平 role=tree + aria-level/expanded/selected、roving tabindex、方向键/Home/End、SVG 图标、`onFileSelect` 回调缝）；挂载点=WorkspaceSetup 面板新增"工作区文件"区（未绑定提示"绑定工作区后即可浏览文件。"，`key={projectId:path}` 重挂载防串，选中占位 role=status"已选中文件"行留待 T-03 替换为预览区）；`app/cockpit.css` 新增 `.workspace-tree-*`（全 tokens，44px 行高、focus-visible 环、选中态 interactive-soft）；既有夹具同波次补 `/workspace/files` 空列表响应 7 处。假设 A-132（树 UI 挂载/aria/图标/徽章/缓存与防陈旧默认）。验证：`workspace-file-tree.test.tsx` 9 例全绿（含 CSS 契约断言）；onboarding+project-context+cockpit-shell 26 文件 167 例全绿；`npx tsc --noEmit` 通过。评审按项目级豁免跳过；全量测试、构建与真实浏览器验收留待 T-04。
- 2026-08-10 T-03 完成（只读预览 UI，实现 subagent）：四轮 RED→GREEN——A 轮 idle 引导/text 分支/路径头/元信息 RED（组件不存在）→ GREEN 2 例；B 轮截断提示条/图片内联/二进制降级/敏感遮蔽+DOM 无泄漏 RED（解析器仅 text 分支，4 例落错误态）→ GREEN 6 例；C 轮快速切换防陈旧+error 脱敏重试+只读断言+CSS 契约 RED（重试按钮与 `.workspace-preview-*` 缺失；防陈旧与只读断言一轮即过，护栏已随 GREEN 内建）→ GREEN 10 例；D 轮 WorkspaceSetup 接线 RED（占位行仍在、region 未挂载）→ GREEN。产出：新组件 `components/project-context/workspace-file-preview.tsx`（判别联合四分支渲染、AbortController+epoch+渲染期 path guard 三重防陈旧、字段重建式解析器保证遮蔽零泄漏、formatWorkspaceFileSize、section role=region"文件预览"）；`app/cockpit.css` 新增 `.workspace-preview-*`（全 tokens）；WorkspaceSetup 接线（预览与树同 key 重挂载、改绑两成功路径清选中、T-02 占位行删除，mount 测试同波次迁移到预览缝）。假设 A-133（预览 UI 默认）、A-134（接线默认）。验证：`workspace-file-preview.test.tsx` 10 例全绿；project-context 12 文件 54 例、onboarding+cockpit-shell 15 文件 124 例全绿；`npx tsc --noEmit` 通过。评审按项目级豁免跳过；全量测试、构建与真实浏览器验收留待 T-04。
- 2026-08-10 T-04 完成（真实浏览器验收 + 一次性全量验证）：验收段落点 `smoke:context`（既有工作区绑定 smoke，勘察确认其覆盖 WorkspaceSetup 面板与绑定造数；onboarding smoke 走 guide 路线不承载本片）。造数=临时目录真实工作区（嵌套 docs/inner+deep.txt、assets/logo.png 程序生成 1×1 png magic bytes、app.bin 含 NUL、.env 植入 canary `WORKSPACE_ENV_CANARY_DO_NOT_LEAK_2026`、540KiB 大文本带首/尾标记、改绑目标 rebound-notes.txt），smoke 结束随临时目录 rmSync 清理、零仓库残留。验收：desktop-light 全分支——树根排序（目录优先+名称序）、.env 已遮蔽徽章、点击展开 docs、键盘 ArrowDown/Enter/ArrowLeft 导航与展开收起、focus-visible 环、Enter 选中 guide.md 文本预览（相对路径头+行数/大小元信息+内容逐字一致）、large.txt 截断条（含首标记、无尾标记、≤512KiB、完整大小元信息）、logo.png dataUrl 内联+类型/大小元信息、app.bin 二进制降级零内容、.env 遮蔽占位零内容+DOM 无 canary、树与预览区只读断言（无 button/textbox/link/contenteditable/表单控件）、treeitem ≥44px；越界直调 API 9 探针全稳定脱敏拒绝（`..%2F` 各形态/绝对路径/NUL→400 INVALID_INPUT、敏感列表路径 `.env`→422 WORKSPACE_PATH_REJECTED、`.env` 预览→200 最小负载 `{"kind":"sensitive-masked"}`，逐探针断言无 canary 无宿主路径）；desktop-dark/narrow-light/narrow-dark 各一次关键路径（暗色桌面文本预览、窄屏抽屉改绑后树+预览验证 key 重挂载反映新根、暗色窄屏树）。终扫：canary 在全部项目 API 响应与服务端日志=0。结果：断言 71、axe 4 态全 0 serious/critical（narrow 抽屉各 1 条 aria-allowed-role minor 为既有 aside 抽屉模式已知项，A-125 同例，低于门禁不修）、矩阵 desktop-light/desktop-dark/narrow-light/narrow-dark、既有 AUDIT/SECURITY/DEPENDENCY/PERSISTENCE 各 PASS 保持。未发现生产缺陷（一轮即全绿，无修复）。证据：`features/027-workspace-readonly-browser/evidence/`（desktop-light/desktop-dark/narrow-light/narrow-dark 四张截图 + workspace-browse-acceptance-results.json）。一次性全量：`npx vitest run` 250 文件 2167 例全绿（116.55s，基线 ~117s 内）、`npx tsc --noEmit` 通过、`npm run build` 通过。假设 A-135（验收默认）。评审按项目级豁免跳过。
