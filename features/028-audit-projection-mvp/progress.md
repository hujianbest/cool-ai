# 进度

- 特性: 028-audit-projection-mvp（对应切片: S-23 的 AUD-MVP 纵切 / CI-3.8 第一子片）
- 当前阶段: done
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: ship 阶段（T-01～T-04 全部完成；演示验收证据已落盘 features/028-audit-projection-mvp/evidence/）
- 用户可感知: 是（最薄只读审计列表）
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture 由主会话按 backlog S-23/CAP-OPS-01/02/CAP-EXE-05 已确认条目直接产出
- 共享理解: backlog 相关条目视为 auto-approved 2026-08-10

## 实施记录

- 2026-08-10 特性开立：AUD-MVP 是全局解锁器（S-17/S-24/S-26/S-28/S-33/S-35/S-38/S-39/S-41/S-42/S-43 均阻塞于 CAP-OPS-01/02）。勘察结论：source-owner 事件事实已丰富（execution_events、collaboration_events、collaboration_thread_facts、review_events 等），`src/` 无 outbox/consumer/checkpoint 骨架；本片选 Safe Execution execution_events 为第一个纵切，采用"同事务 outbox + checkpoint 幂等追平 + 确定性 rebuild + freshness lag"最薄范式，MVP 读路径同步 catchUp、不建后台守护。
- 2026-08-10 T-01 完成（不提交 git；项目级 review 豁免 2026-08-09 生效，独立评审跳过）。RED 16 例行为性失败：新建 `tests/adapters/sqlite/safe-execution/execution-audit-outbox.test.ts` 13 例（no such table/NOT_IMPLEMENTED/identity 14）；orchestrator 三故障位扩展断言 outbox 镜像（before/after_call_terminal_update 回滚后 outbox 空、after_call_terminal_commit 恰镜像已提交 usage 事件）；identity 引用同步 15 后 current-schema ×3 + rejection legacy-14 失败。GREEN：identity 14→15 三表 DDL；`audit-event-outbox.ts` 集中白名单提取 + 同事务 append，15 个调用点覆盖全部 14 处 execution_events INSERT（4 泛型 helper + execution-service 双事件 + approval + conflicts×2 + frozen-input + action-orchestrator + process-runner + file-tools×4）；current-data-invariants 三条（projection.outbox_seq⊆outbox、checkpoint.last_outbox_seq≤MAX(outbox_seq)、projection 行数≤outbox 行数）；write-ownership manifest 登记两 owner 三表，writers.test 登记 operations-projection 目录与 migrated 阻断，锚点 `operations-projection/audit-projection-store.ts`；execution-conflicts 最小 schema 夹具补 outbox 表。验证：聚焦 63/63；tests/adapters/sqlite + tests/architecture 194/194；safe-execution + public-collaboration 中 5 例失败修复（3 例最小 schema 缺表、2 例并行负载超时隔离复跑 70/70 通过）；review-delivery 200/200；全量 251 文件 2181/2181 通过（122.6s，基线 98s 内可接受）；`npx tsc --noEmit` 绿。identity 引用同步点：current-schema.test ×3、rejection legacy 矩阵 +14/unsupported→16、unsupported-schema-input 类型联合、attachment/structured-message×2/source-api/reply-reopen×2/thread-favorite 断言、persistent-threads smoke（浏览器冒烟留 ship 阶段运行）。默认选择见 A-136～A-140。
- 2026-08-10 T-02 完成（不提交 git；项目级 review 豁免生效）。RED：新建 `tests/modules/operations-projection/audit-projection-consumer.test.ts`，首轮模块不存在失败、次轮 rebuild/freshness 9 例行为缺失失败。GREEN：新领域模块 `src/modules/operations-projection/`（public/{commands,queries,dto,errors}.ts + index.ts，对齐 mission-work 布局；Commands 模块公开但仅供装配/测试缝，无 HTTP 写路由；freshness 按架构归入 Queries）；Adapter `operations-projection/audit-projection-consumer.ts` 实现 `catchUpAuditProjection`/`rebuildAuditProjection`/`getAuditProjectionFreshness`；装配根登记命名空间 `auditProjectionConsumer`；架构测试无需新登记（writers/ownership 已于 T-01 注册 operations-projection）。设计要点：catchUp 批次 500、每批次一个 BEGIN IMMEDIATE 事务（批次行 INSERT OR IGNORE + checkpoint 前移同 commit），中断从批次边界恢复、重叠窗口重放靠 outbox_seq UNIQUE 去重、空 outbox 自愈建 idle/0 checkpoint；rebuild 单事务（条件 UPDATE CAS idle→rebuilding、缺行以 claimed 态插入、DELETE 清投影、按 seq 分批全量重放、COUNT(projection)==重放数确定性校验否则 PROJECTION_REBUILD_INCOMPLETE 回滚、回 idle），异常整体回滚不留半成品；互斥=checkpoint 状态机，rebuilding 中 catchUp/rebuild 均拒绝（PROJECTION_REBUILD_IN_PROGRESS），持久态 rebuilding 本实现永不提交、MVP 不自动接管；损坏 checkpoint（last>max）择 fail-closed——公开路径 reopen 不变量 SCHEMA_DATA_INVALID 拦截，consumer 内 PROJECTION_CHECKPOINT_CORRUPT 仅纵深防御；freshness 全局签名（未初始化=lag 全量、空 outbox 报 caught_up）。验证：新套件 18 例全绿（基本追平、两遍逐字节幂等、空 outbox 自愈、1200 行 3 批次、中断边界恢复无重复、重叠窗口重放去重、多项目混写追平、互斥负例 ×2、损坏 fail-closed ×2、rebuild 从零/修复分叉/定点幂等/空 outbox、freshness 三态与 lag）；聚焦 tests/architecture + tests/adapters/sqlite + 新套件 21 文件 212/212 通过；`npx tsc --noEmit` 绿。默认选择见 A-141、A-142。
- 2026-08-10 T-03 完成（不提交 git；项目级 review 豁免生效）。RED 两轮：新建 `tests/modules/operations-projection/audit-projection-queries.test.ts`（Adapter 不存在失败）与 `audit-events.api.test.ts`（路由不存在失败）。GREEN：查询缝 `listProjectAuditEvents(databasePath, projectId, {limit?, beforeSeq?})` 落新 Adapter `operations-projection/audit-projection-queries.ts`（选项防御校验→tuple 404→同步 catchUp→单读事务取页+freshness 同快照；beforeSeq 排他游标 DESC、limit 默认 50 上限 100、limit+1 判 hasMore、nextBeforeSeq=末行 seq|null；payload 白名单对象原样）；consumer 提取 `readAuditProjectionFreshness(database)` 快照助手复用；模块 errors 增 INVALID_INPUT/PROJECT_NOT_FOUND；wire DTO 下沉 `src/shared/audit-contracts.ts`（freshness 类型迁 shared 并经模块 dto re-export）；装配根登记 `auditProjectionQueries`。路由 `GET /api/projects/[projectId]/audit-events?limit=&before=`（query 白名单、fields 数组词汇、projectId RESOURCE_ID、全响应 no-store；404/409/400/500/503 稳定脱敏 envelope）。验证：operations-projection 三套件 51/51（空项目 empty、读路径触发 catchUp 后追平、DESC 与 payload 原样、beforeSeq 分页三段、before==最小 seq 空页、默认 50 截断续页、tuple 隔离、PROJECT_NOT_FOUND、非法选项 ×7、rebuilding fail-closed；路由 200 形状/no-store、limit/before 分页、400 矩阵 ×12、404、畸形 projectId）；tests/architecture + tests/adapters/sqlite 20 文件 194/194；tests/adapters/inbound 37/37；`npx tsc --noEmit` 绿。默认选择见 A-143～A-145。
- 2026-08-10 T-04 完成（不提交 git；项目级 review 豁免生效）。RED：新建 `tests/browser/project-context/audit-panel.test.tsx` 9 例（列表渲染与倒序、文案映射含未知类型兜底原文、freshness 三态徽标、加载更多游标与失败保留已加载可重试、empty、rebuilding 409 友好文案、通用错误、来源定位正负例、projectId 切换防陈旧、只读断言无编辑入口）+ `context-accessibility.test.tsx` 同步第 4 tab（End→审计、ArrowLeft 环绕、空态、fetch 夹具补 audit-events）。GREEN：新组件 `components/project-context/audit-panel.tsx`——EVENT_TYPE_COPY 单处 15 类映射（未知兜底原文）、actor 映射、freshness 徽标复用 `.status-label` 既有变体（caught_up→completed「已追平」/behind→queued「落后 N 条」/rebuilding→running「重建中」）、nextBeforeSeq 排他游标追加式分页、epoch 计数 ref+AbortController 防陈旧、逐字段防御解析（畸形页静态文案）、来源定位复用执行卡 `execution-{id}-title` 焦点缝并实核 document.activeElement——目标不在渲染窗口或窄屏抽屉已关闭时如实提示「该执行未显示在运行详情列表中（仅展示最近的执行）。」（role=status aria-live）不伪造跳转；ProjectContextPanel 挂「审计」tab（复用既有 tablist/roving tabindex）；api-error-copy 补 PROJECTION_REBUILD_IN_PROGRESS「审计投影正在重建，请稍后重试。」。浏览器验收落点 smoke:execution 新增审计段（复用其真实执行造数：5 执行、74 事件、2 页）：API 全游标走查（全局 DESC、排他游标、74==outbox==projection、checkpoint==MAX）+audit API 全页 JSON 秘密扫描；桌面 light/dark 各一次——tablist End 键选中审计 tab、呈现与文案映射断言、freshness 已追平、连续「加载更多」至全量 74 行、定位按钮与 tab ≥44px、来源定位真实焦点落地断言、axe；narrow 抽屉一次——键盘开 tab、44px、定位如实未渲染提示、焦点归还抽屉开关、axe；axe 方法学=先扫同页 memory tab 基线（审计面板未挂载）再扫审计态以归因——遗留 landmark-unique@execution-timeline 与 aria-allowed-role@task-context-drawer 经基线确认为既有座舱 chrome（A-125/A-135 同例，低于门禁不修），本片引入的 heading-order（事件标题 h4 跳级）已修为 h3 并复扫归零；axeScan 对齐 persistent-threads 先例增 color-contrast 全零门禁，并记录全部 violation id/impact/target；秘密扫描面扩展 narrowAuditFacingText 与两张审计截图字节；修复 smoke 腐化一处——browser.newPage→browser.newContext（AxeBuilder 硬要求，026 A-129 同例）。验证：聚焦 10/10；全量 255 文件 2240/2240 通过（118.4s，基线 117s 内）；`npx tsc --noEmit` 绿；`npm run build` 绿；smoke:execution 全绿（AUDIT API/DESKTOP/NARROW PASS、SECURITY SCAN PASS，BROWSER PASS 计数 audit_event_outbox=74/projection=74/checkpoints=1）。证据：features/028-audit-projection-mvp/evidence/demo-audit-desktop.png、demo-audit-narrow.png。默认选择见 A-146～A-148。