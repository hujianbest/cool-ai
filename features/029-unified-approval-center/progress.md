# 进度

- 特性: 029-unified-approval-center（对应切片: S-24 / CI-4.3）
- 当前阶段: done
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: T-04 真实浏览器验收（实现 subagent）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（2026-08-09）；不伪造评审工件；spec/architecture 由主会话按 backlog S-24 已确认条目直接产出
- 共享理解: backlog S-24 条目视为 auto-approved 2026-08-10

## 实施记录

- 2026-08-10 特性开立：S-24 前置全部成立——`CAP-EXE-01`/`CAP-COL-01` 已交付，`CAP-OPS-01/02` 由 028 AUD-MVP 今日 ship，`CAP-COL-03` 高风险卡片来源随 S-13 ship 验证。勘察结论：governance 模块已有 execution_approvals 完整读写缝；内联决策 Proposal 有 015/018 的裁决与 stale 语义。本片关键边界：审批中心只读聚合 + 分派，零新写路由、零 schema 变更。
- 2026-08-10 T-01 完成（项目级 review 豁免，2026-08-09）：`listPendingApprovals(databasePath, projectId)` 落 `src/adapters/outbound/sqlite/governance/approval-center-queries.ts`（装配根命名空间 `governanceApprovalCenterQueries`）；DTO 下沉 `src/shared/approval-center-contracts.ts`（zod strict），governance 公开面新增 `GovernanceApprovalCenterQueries`/`GovernanceError`；内联决策经 public-collaboration 新公开缝 `listPendingProposals`（structured-message-store 实现）取数，零直读他域表、零过渡边。失效映射：执行域 pending→可裁决，expired/replaced/revoked→decisionHint 如实映射，approved/consumed/rejected 不入列；proposal 无持久化 stale 态（VERSION_CONFLICT 为裁决时结果），pending→可裁决、resolve 后出列（A-149/A-150）。RED=8 用例行为性失败（缝不存在）；GREEN=8/8 通过；受影响套件 tests/modules/governance+public-collaboration+execution-structured-repair+tests/architecture 60 文件 659 通过；`npx tsc --noEmit` 零错误。夹具同波次调整：current-advance 增可选 idPrefix（默认保持既有 id），测试内 execution 图按 execution_one_pending_approval/execution_one_active_agent/work_item 唯一索引约束逐审批独立 execution+agent+work_item。
- 2026-08-10 T-02 完成（项目级 review 豁免，2026-08-09）：补建 `GET /api/projects/[projectId]/approvals/pending`（T-01 只交付模块查询缝；`{approvals}` 快照、no-store、拒绝一切 query、GovernanceError/SchemaError/storage 映射对齐 audit-events 先例，A-151）；审批中心 UI 落 `components/project-context/approval-center-panel.tsx`，挂载为 ProjectContextPanel 第四 tab「审批」（审计保持末位，A-153）；裁决分派零新写路由——执行域 GET execution detail 取 version 后 POST `/api/executions/{id}/approvals/{approvalId}`，内联决策域 GET 块取 stateVersion 后 POST `…/blocks/{blockId}/decision`（批准→accept），409 漂移兜底脱敏行内错误（A-152）；失效项不渲染裁决按钮+status-failed 徽章+原因行；执行来源定位复用 execution-{id}-title 焦点缝，内联来源走规范目标身份链接（018 无跨面板消息焦点缝，勘察结论 A-153）；empty/loading/error/刷新/epoch+abort 防陈旧齐备（A-154）。RED=路由 5 用例（模块不存在）+UI 8 用例（缝不存在行为性失败）；GREEN=13/13 通过；同波次调用方更新=context-accessibility tab 序+fetch stub、context-browser-smoke 预存腐化修正（End 末位应为审计，自 028 起即 stale）；聚焦 tests/browser/project-context+tests/modules/governance 16 文件 83 通过、tests/architecture 25 通过、`npx tsc --noEmit` 零错误。
- 2026-08-10 T-03 完成（项目级 review 豁免，2026-08-09）：原流程续接端到端验证落 `tests/modules/governance/approval-center-continuation.test.ts`（内存库夹具，7 用例 characterization 首轮全绿）。裁决一律走与中心 UI 完全相同的真实路由调用链（GET 取版本→POST 既有裁决路由），中心列表同步经真实 GET pending 路由+UI 同款 zod strict pageSchema 解析断言（A-155）。场景矩阵：①执行域批准→approved 出列→`consumeApprovedCommand` 消费续跑（consumed/running v3/工具调用 requested/action 落行）；②proposal accept→块落定（head v2、state accepted、inline_decisions 落行、终态二次裁决 409 ACTION_CONFLICT）→出列；③过期审批（域内既有过期缝 `expireOpenApprovalsForProjectExecution` 造数）→中心 expired+decisionHint→裁决 409 APPROVAL_STATE_CONFLICT 脱敏 envelope→执行零推进、中心持续显示失效不误消；④拒绝执行审批→paused+COMMAND_APPROVAL_REJECTED+工具调用 rejected→continue 回 running→出列；⑤拒绝 proposal→块落定 rejected→出列；⑥执行版本漂移（另一客户端先批准）→旧版本 POST 409 EXECUTION_STATE_CONFLICT 脱敏（内部文案不外泄）→中心反映 approved 真相；⑦块 stateVersion 漂移→409 VERSION_CONFLICT+currentStateVersion→中心反映 settled 真相。未发现真实断裂，零生产代码改动。聚焦套件 tests/modules/governance+execution-approvals+inline-decision-http+structured-message-http 6 文件 42 通过；`npx tsc --noEmit` 零错误。
- 2026-08-10 T-04 完成（项目级 review 豁免，2026-08-09）：真实浏览器验收落点 `tests/browser/execution-browser-smoke.mjs`（A-156/A-157）。造数：执行域复用 isolated Beta 一次性命令审批（waiting_approval）；内联决策域由 provider stub 在首个规划轮顺带 emit 真实 proposal 块（公共协作缝持久化，一次性 flag 防多轮重复 emit）；失效态=stale 触发域内 expire 写缝——mission PATCH 使 frozen input 漂移 + waiting_approval 执行 advance 失败关闭（409 STALE_EXECUTION、执行 stale、开放审批 expired），零手改 DB。验收路径全绿：中心 API 两项（execution 新者优先+proposal）→「审批」tab 键盘 ArrowRight×3 选中（roving tabindex/aria-selected/焦点实核）→呈现断言（域徽标/状态徽章/种类/标题/impactSummary/来源链接 thread+run 规范身份）→44px 全按钮扫描+链接/tab 抽查→鼠标定位来源执行（execution-{id}-title 焦点实核）→键盘 Tab 链（tab→刷新列表→批准→Enter）批准执行审批→waitForFunction 钉列表收缩消 notice/refresh 竞态→中心出列→同理拒绝 proposal→块落定 rejected（stateVersion 2）+中心空态→续跑证据（advanceUntilStatus 后 approval consumed+执行 staged+运行详情卡「变更待审阅」）；失效项呈现（已过期徽章+「无法裁决：请求已过期。」+零裁决按钮）+桌面暗色复核+窄屏抽屉键盘选中/44px/axe。矩阵 desktop-light 全路径 + desktop-dark/narrow-light 失效关键路径（A-157）；axe 5 态 blocking/contrast 全零，既有 chrome 项基线归因（landmark-unique:moderate@execution-timeline、aria-allowed-role:minor@task-context-drawer，A-125/A-135/A-148 同例不修）；秘密扫描面扩展审批中心 DOM 文本+4 张截图字节+中心 API 负载，SECURITY SCAN PASS 零泄漏。生产缺陷一处顺手修复（A-158）：执行时间线 `<ol role="log">` 显式角色覆盖 implicit list 致 li 失去列表父级（axe listitem serious，自 0b648dc 起既有；028  axe 未触发系扫描时机在运行详情 tab 外）——role=log 上移包裹 div、ol 恢复列表语义，execution-review-ui 12 用例零改动通过。造数纪律一处钉入 provider stub：lapsed 阶段 step 0 写全新 src/lapsed.txt（沙盒快照含 canonical 已合入 beta.txt，已存在文件+expectedHash=null 被 verified 写语义失败关闭，非 restart 问题）。验收统计：assertions=69、axeStates=5；证据落 `features/029-unified-approval-center/evidence/`（4 截图+acceptance results.json）。一次性全量：`npx vitest run` 全绿、`npx tsc --noEmit` 零错误、`npm run build` 通过（路由表含 /api/projects/[projectId]/approvals/pending）。
