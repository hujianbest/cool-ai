# 任务票 — 结构化消息与就地决策

- [x] T-01 收敛唯一 Thread Fact writer — Blocked by: None — 覆盖: 既有 Thread/Run/Agent fact 写入公共 Seam；交付: `appendBatchTx` 覆盖全部既有 intent，迁移 thread-service、run-service、action committer/orchestrator 等 producer 且保持行为；判据: 聚焦回归通过，仓库约束测试证明除 migration 外无 direct fact SQL
- [x] T-02 建立 raw ingress/JCS 共享 codec — Blocked by: T-01 — 覆盖: Codec Seam；交付: raw bytes/string 一次完成 duplicate-key、I-JSON/JCS、strict schema、limits/credential 校验并返回 branded value/canonical bytes，persisted read 分流 Known/UnknownSchema/Invalid；判据: 官方向量与 wire/domain ±1 测试证明 size/hash 只消费同一 canonical bytes
- [x] T-03 建立 current schema manifest 与 exact validator — Blocked by: T-01, T-02 — 当前要求: identity 9 `CURRENT_SCHEMA` 是唯一 DDL source，覆盖 blocks/state/decisions/receipts/facts 与 operation matrix；exact object/FK/current-data validator 拒绝 drift、断链、非法 transition 与 partial schema
- [x] T-04 建立 canonical fresh/exact reopen contract — Blocked by: T-03 — 当前要求: 空库单事务 bootstrap 并最后写 identity 9；非空库只接受 exact current，v1～v8、unsupported、partial、drift、data-invalid 均不迁移、不修补并失败关闭
- [x] T-05 打通最薄 Proposal persist/read 路径 — Blocked by: T-04 — 覆盖: Structured Message Store 公共 Seam；交付: 合法非空 Message + 单 Proposal 经 codec 写入 immutable block/initial state/fact 并按原顺序读回，纯文本与 text+blocks 共存且拒绝 block-only；判据: 一个端到端领域行为测试跨提交与读取验证 identity、版本、actor/source snapshot 和零部分写入
- [x] T-06 原子提交 Agent Proposal/Checklist turn — Blocked by: T-05 — 覆盖: Agent Public Turn Committer Seam；交付: strict Agent output 在既有 lease/context 事务内提交 Message、两类 blocks、initial state 与 fact，多 block 任一失败整体拒绝且无 raw/credential 泄漏；判据: 合法 turn 恰提交一次，字段/来源/lease stale 与 fault injection 均无部分事实或降级卡
- [x] T-07 提交 verified Diff/File/Handoff 投影 — Blocked by: T-06 — 覆盖: Verified Source Projection + Agent Commit Seam；交付: Diff/File 只解析同 tuple 已落库 artifact/observation，Handoff 只投影既有 handoff fact，并提交三类只读 block；判据: 有效来源冻结读回，失效/跨 tuple/latest/path 输入失败关闭且不读宿主、不创建新 Run/handoff
- [x] T-08 暴露 strict tuple structured-message HTTP — Blocked by: T-05, T-07 — 覆盖: Domain/HTTP Adapter Seam；交付: 严格 path/query/content-type/body/DTO 与 project/thread/run/message/block/source tuple scoped read/source-navigation contracts，返回 sanitized stable envelopes；判据: 逐一替换 tuple 成员与 unknown/invalid persisted 数据时同形失败、零泄漏、零写入，Known/UnknownSchema 响应可判定
- [x] T-09 纵向交付 Proposal Inline Decision success — Blocked by: T-06, T-08 — 覆盖: Inline Decision Domain/API Seam；交付: `accept|reject` 通过 canonical intent/hash、expectedStateVersion CAS 原子形成 completed operation、新 state revision、Decision、Business Action Receipt 与 decision fact；判据: 首次成功返回唯一 Receipt，Proposal 终态禁用后续 transition，事务故障回滚全部业务行
- [x] T-10 纵向交付 Checklist 多次合法 transition — Blocked by: T-09 — 覆盖: 同一 Inline Decision Domain/API Seam；交付: `check_item|uncheck_item` 一次只更新既有 item、stateVersion 单调递增且 blockRevision 不变；判据: 多轮 check/uncheck 恰形成连续 state/Decision/Receipt/fact，批量项、文本改写、额外动作/字段均拒绝
- [x] T-11 闭合 operation replay/conflict/recovery — Blocked by: T-10 — 覆盖: Operation Outcome Domain/API Seam；交付: completed same-hash replay、异 hash conflict、terminal VERSION_CONFLICT、not-found unknown-write 查询及 strict tuple 写入保护；判据: success 只重放原 Receipt，version-conflict 四类业务行为零且重放原 envelope，hash conflict 不改原 operation，无持久 inline pending 或重复动作
- [x] T-12 构建 fact-only Transcript read model — Blocked by: T-06, T-07, T-08 — 覆盖: Transcript Model Seam；交付: 纯文本与五类 block 按 fact/message/block 顺序归并、分页去重、冻结版本/source 标签、UnknownSchema 不可执行占位、Invalid 损坏失败；判据: 公共 transcript 输入跨页重放结果稳定，unknown 不猜字段且不阻断其他合法事实，target epoch 丢弃旧响应
- [x] T-13 交付 Proposal/Checklist 就地决策 UI — Blocked by: T-09, T-10, T-11, T-12 — 覆盖: fact-only UI Seam；交付: 两类交互 block 的 loading/error/disabled/success/focus、pending 对账、stale 重读、Receipt/冲突呈现与键盘路径；判据: 组件公共表面只以 fact/operation outcome 显示成功，终态与 stale 禁用理由明确，焦点和 target switch 不串线
- [x] T-14 交付 Diff/File/Handoff 安全导航 UI — Blocked by: T-07, T-12 — 覆盖: fact-only UI/source-navigation Seam；交付: 三类只读 block、脱敏预览、受控 File/Run/execution/Approval 入口与 unavailable/unknown 状态；判据: 卡片无编辑/合入/执行能力，跨 tuple/失效来源不回显路径或 fallback latest，高风险入口只导航正式 surface
- [x] T-15 验证 reopen/pagination/stale/current history 恢复 — Blocked by: T-04, T-11, T-12 — 覆盖: canonical reopen + persistence recovery Seam；交付: current 纯文本零合成、新 blocks/decisions/receipts/facts 跨分页重启一致、stale/unknown-write durable outcome 可恢复；判据: identity 9 exact reopen 不重排不复制，completed/version-conflict/not-found 分流稳定，非法 tuple/version/JCS 数据失败关闭
- [x] T-16 完成真实 browser/axe 与全量回归 — Blocked by: T-13, T-14, T-15 — 覆盖: Browser/Axe Seam 与仓库公开验证命令；交付: 真实 Proposal/Checklist replay/stale 双页面、Diff/File/Handoff/Approval 导航、刷新、desktop/narrow、light/dark、键盘/44px/focus/contrast 证据；判据: 受影响 axe、目标 smoke、全量 tests、typecheck/build 均通过且无 accidental artifacts
