# 行走骨架 Frame

- 日期: 2026-07-29
- 意图: 为独立产品创造者搭起可一键启动和测试的 Web 应用骨架，并打通“创建示例项目 → 提交任务 → 持久化状态与结果 → UI 回显”的最薄真实端到端路径。
- 切片: S-1
- 范围外: 真实模型接入、角色与技能配置、多 Agent 自主接力、共享记忆、并行工作区执行和 owner 审批；这些由 S-2 至 S-6 交付。
- 模式: 建造
- 风险档位: 2
- 档位理由: 全新项目脚手架与首条跨 UI、业务逻辑和持久化的新功能路径，属于标准新功能；不涉及迁移、认证、支付、破坏性公共接口或不可逆操作，未命中档位 3。
- 用户可感知: 是
- 环境基线: evidence/baseline-20260729T142128Z.log (exit 0)
- 基线说明: 使用 Node.js 内置测试运行器执行 `npm test`，最小测试骨架通过；Windows 下门禁需设置 `PYTHONUTF8=1` 并通过 `cmd /c npm test` 调用 npm。此前一次无法解析 npm 和一次默认 GBK 解码失败均未作为通过证据。
- 假设: 无；产品级默认选择见 product/assumptions.md。
