# 进度

- 特性: 032-thread-tags-bulk-organize（对应切片: S-18 / CI-2.6）
- 当前阶段: done
- 执行模式: auto（用户 2026-08-09 明示：不在电脑前，问题按助手推荐处理）
- 已加载扩展: 无
- 下一步: 无（S-18 已 ship：T-01..T-05 全部勾选；全量 267 文件/2431 用例 118.2s 绿、smoke:threads 50 断言/32 axe 态 0 serious/critical、tsc/build 绿；backlog 勾选后随切片提交推送）
- 用户可感知: 是
- 评审状态: 项目级 review 豁免（AGENTS.md 2026-08-09 起生效）；不伪造评审工件；spec/architecture/tickets 由主会话按 backlog S-18 已确认条目直接产出
- 共享理解: backlog S-18 条目视为 auto-approved 2026-08-11

## 实施记录

- 2026-08-11 特性开立：S-18 准入全部成立——`CAP-COL-02` Thread Catalog 核心已交付；阻塞前置 S-17（`CAP-OPS-02` 线程查询，031 于 2026-08-11 ship）与 S-24（`CAP-GOV-02` 跨域统一审批中心，029 于 2026-08-10 ship）均已交付。spec/architecture/tickets 同批产出；规格/架构/代码评审按项目级 review 豁免跳过并在此记录。
- 2026-08-11 关键默认决策（台账 `product/assumptions.md` A-182..A-189）：schema 纯增三表 identity 17→18 零既有表改动（A-183）；创建按折叠名幂等（A-184）；删除=同事务清边+计数、确认走 UI 强确认而非 CAP-GOV-02 审批中心（A-185）；listThreads 增 tagId 过滤与 tags 恒在投影（A-186）；批量整理用项目作用域 `thread_tag_operations` receipt 承载 operation 语义（A-187）；标签写不进 audit_event_outbox、搜索索引零改动（A-188）；UI 形态=管理对话框+chips+筛选条+多选批量条（A-189）。
- 2026-08-11 T-01 完成（标签 schema 与 CRUD 命令/查询，项目级 review 豁免）：三轮 RED/GREEN——RED① schema 断言（identity 18 + 三表 + 索引缺失）→ GREEN① `current-schema.ts` userVersion 17→18 纯增 `thread_tags`/`thread_tag_edges`/`thread_tag_operations` + `thread_tag_edges_by_tag` 索引（零既有表改动），reopen 不变量两条（edges⊆threads、edges⊆tags），identity 引用同波次同步 12 文件（rejection 矩阵 legacy 1..17/unsupported 19、unsupported-schema-input 联合、current-schema 双套件、execution-audit-outbox、public-collaboration reopen/source-api/favorite/outbox 五套件、persistent-threads smoke user_version 18），write-ownership manifest owners+notes 登记三表；RED② 命令/查询缝模块缺失 → GREEN② DTO 下沉 `src/shared/collaboration-contracts.ts`（ThreadTagDto/ThreadTagListItemDto/ThreadTagListResponseDto/ThreadTagCreateResponse/ThreadTagDeleteResponse），新 `thread-tag-service.ts`（createThreadTag trim+1..40 grapheme+name_key=NFC+大小写折叠+ON CONFLICT 幂等 created:false；listProjectTags 可选 query instr 折叠 contains+threadCount+limit 50/100；deleteThreadTag 同事务清边+removedEdgeCount+跨项目 404；`insertThreadTagOperationReceiptTx` 锚点写入器供 T-03 receipt 复用并满足 writers 不变量），模块公开面 commands/queries/dto 与装配根 `threadTagService` 登记；RED③ 路由缺失（import.meta.glob 断言）→ GREEN③ `GET/POST /api/projects/[projectId]/thread-tags` 与 `DELETE …/thread-tags/[tagId]`（严格路径/查询/正文校验、脱敏 envelope、全响应 no-store、流式 64KiB 上限沿 025 favorite 先例）。验证矩阵全过：折叠唯一矩阵（NFC/大小写/trim）、grapheme 上限与空白 400、幂等创建、contains 搜索与用量计数、删除清边计数与 FK 兜底（项目级联，A-190）、跨项目隔离、reopen 幂等与不变量负例。聚焦测试 74 文件 852 通过（public-collaboration + adapters/sqlite + architecture 含 writers manifest）；`npx tsc --noEmit` 通过。新假设 A-190（线程不可删使 FK CASCADE 兜底经项目级联验证）、A-191（标签路由全响应 no-store）、A-192（receipt 锚点写入器 T-01 先行登记）。未 commit/push。
- 2026-08-11 T-02 完成（分配命令与列表筛选/投影，项目级 review 豁免）：三轮 RED/GREEN——RED① `setThreadTagAssignment` 不存在（调用即 TypeError，5 个命令缝用例失败）→ GREEN① 共享契约 `ThreadTagAssignmentResponse` + thread-tag-service 幂等 upsert/delete（ON CONFLICT DO NOTHING 冻结首 edge created_at、receipt-less 025 先例、JOIN 双表 tuple 校验跨项目 404）+ 模块公开面 commands/dto 登记；RED② `PUT …/threads/[threadId]/tags` 路由缺失（import.meta.glob 断言 2 用例失败）→ GREEN② 新路由（双路径参数严格校验、流式 64KiB、严格 JSON DTO、脱敏 envelope、全响应 no-store 沿 A-191/A-195）；RED③ listThreads 无 tagId 过滤与 tags 投影（6 用例失败：未知字段 400/投影缺失/路由 400）→ GREEN③ thread-service parseListInput +tagId（RESOURCE_ID、与 favoritesOnly 互斥 400 `tagId:"not_combinable"` A-193）、EXISTS 子查询过滤保持 last_activity_sequence DESC 排序不变故游标兼容、页面线程 id 批量第二查询投影恒在 `tags: ThreadTagRefDto[]`（name+id 码元序，无 N+1）、GET /threads 白名单 +tagId（单值/duplicate/empty/RESOURCE_ID）、共享契约 ThreadListItemDto 增 tags + ThreadTagRefDto、组件 zod（threadTagRefSchema + threadListItemSchema + finishCreatedThread 乐观项 tags:[]）与四处测试夹具（cockpit-test-fetch、thread-search-ui、persistent-thread-list-ui 含 createdThread 剥离 tags、thread-stale-response）同波次更新。验证矩阵全过：幂等切换矩阵（重复 assign 冻结 created_at/重复 unassign 零行无错/解除后再分配新时间戳）、跨 tuple 404 七组合×双向写零生效、互斥 fields 断言与游标语义（tagId+limit+nextCursor 翻页一致、普通过游标不变）、筛选确定性（他项目/不存在 tagId 空页 200 不 404，A-194）且普通列表排序不变、投影恒在含空数组与 name 码元序、reopen 后过滤/投影一致。聚焦测试：public-collaboration 54 文件 668 通过（thread-tag 套件 34）、tests/architecture 6 文件 25 通过、受影响浏览器套件 tests/browser/threads 6 文件 71 + threadListPayload 两个消费套件 8 通过；`npx tsc --noEmit` 通过。新假设 A-193（互斥错误词汇 tagId:not_combinable）、A-194（tagId 过滤=空页语义不 404）、A-195（分配路由全响应 no-store）。未 commit/push。
- 2026-08-11 T-04 完成（线程区整理 UI，项目级 review 豁免）：一轮 RED/GREEN——RED 新建 `tests/browser/threads/thread-tags-ui.test.tsx` 18 用例全失败（无管理对话框/chips/筛选条/批量条/确认对话框）→ GREEN 全过。实现落于 `components/project-thread-navigation.tsx` + token 化样式入 `app/cockpit.css`：①「管理标签」对话框（创建输入 trim+≤40 grapheme 校验「请输入标签名称。」/「标签名称不能超过 40 个字符。」、contains 搜索「无匹配标签。」、列表带「已分配 N 条线程」用量计数、删除强确认如实「删除标签“X”将解除 N 条分配。此操作不可撤销。」+ 成功「已删除标签“X”，解除 N 条分配。」）；② 列表项标签 chips（`.status-label.thread-tag-chip` 复用徽章范式、不新增颜色，经 `.thread-list-main` 纵向布局）；③ 筛选 chip 条（`role=group`「按标签筛选线程」，选中 tagId → GET `tagId=`，无标签整条隐藏、与已收藏互斥回退「全部」，A-199/A-200）；④「整理线程」多选模式（`aria-pressed` toggle）+ 批量条（`role=region`「批量整理线程」、已选计数 live、「添加标签」/「移除标签」两组互斥 picker、「应用更改」→ `role=dialog`「确认批量整理」如实「将为 N 条线程添加 X 个标签、移除 Y 个标签。移除会立即解除这些线程上的标签分配。」、operationId=crypto.randomUUID、成功 `role=status`「已为 N 条线程更新标签。」+ 静默刷新、失败行内 `role=alert`+「重试批量整理」沿用同一 operationId 幂等重放）。样式全 tokens、44px（`.status-label.thread-tag-filter-chip`/`.thread-list-select` 经 `--control-min`）、键盘/Enter/Space/Esc、`focus-visible`、loading/empty/error/disabled 全态。防陈旧：tags 列表 `useTargetRequestGuard` AbortController + projectId 切换全量重置（manage 对话框/organize 模式/选择/批量态全清，A-201/A-203）；整理模式 Esc 在原声 keydown 层 stopPropagation 不冒泡关外层抽屉；创建/批量成功共用单 `role=status` live region（A-202）。验证矩阵全过：交互矩阵、全态、键盘可达与焦点归还、互斥回退、确认文案计数如实、防陈旧（旧项目 tags 响应被丢弃）、target switch 不串、CSS tokens 契约断言（`readFileSync` 断言零硬编码 hex/px/rem/em、纯 `var(--`）。同波次 fixture 更新：`tests/cockpit-test-fetch.ts` 增 `/thread-tags?limit=100` 空响应 + 四个受影响浏览器套件（thread-search-ui 17 处、persistent-thread-list-ui 5 处、thread-stale-response 1 处）补 tags 守卫。聚焦 `npx vitest run tests/browser/threads` 7 文件 89 通过；`npx tsc --noEmit` 通过；广扫 `tests/browser` 中 review-full-chain 与 team-skill-slice 两个全链路冒烟在整批运行下偶发超时、隔离/同跑均通过，与标签无关（既有时序敏感）。新假设 A-199..A-203。未 commit/push。
- 2026-08-11 T-03 完成（批量整理命令，项目级 review 豁免）：两轮 RED/GREEN——RED① `applyThreadTagBatch` 不存在（调用即 TypeError，8 个命令缝用例失败）→ GREEN① 共享契约 `ThreadTagBatchResponse`/`ThreadTagBatchAppliedItem` + thread-tag-service 批量命令（严格 parseBatchInput：未知字段/必填/RESOURCE_ID/空 threadIds 400，threadIds≤100 且 add+remove 去重并集≤20 上限 400 `threadIds:"too_many"`/`tagIds:"too_many"`（A-196 计数语义）；单事务 ensureProject→receipt 查重（同 operationId 同 canonicalRequestHash 重放返回存储响应 replayed:true、异 hash/残缺行 409 OPERATION_CONFLICT 复用 operation-receipts 词汇）→全量 tuple 校验（thread/tag 存在计数比对，任一跨项目整体 404 回滚零生效零 receipt）→幂等边 upsert/delete（per-thread 如实 addedTagIds/removedTagIds 摘要；add/remove 同标签重叠=先加后去、摘要双记、净边不留（A-197））→复用 T-01 锚点 `insertThreadTagOperationReceiptTx` 同事务写 receipt）+ 模块公开面 commands/dto 登记；RED② `POST …/thread-tag-batch` 路由缺失（import.meta.glob 断言 2 用例失败）→ GREEN② 新路由（projectId 严格校验、拒绝 query/fragment、流式 64KiB、严格 JSON DTO、脱敏 envelope、全响应 no-store 沿 A-191/A-195 扩展 A-198）。验证矩阵全过：重放不重复生效（边幂等+receipt 短路+响应体逐字节一致含 operationId）、409 冲突（零边变化零新 receipt）、原子性负例四组合（他项目线程居末/他项目标签/缺失标签/缺失线程均整体 404 零生效零 receipt）、上限/去重 400（101 线程、11+10 标签、空 threadIds、非法 operationId/tagId 形态）、receipt 行持久可查（requestHash 64-hex/status/httpStatus/responseJson 重放跨 reopen 一致）。聚焦测试：public-collaboration 55 文件 678 通过（thread-tag-batch 套件 10）；`npx tsc --noEmit` 通过。新假设 A-196（上限按去重后并集计数）、A-197（add/remove 重叠=先加后去摘要双记）、A-198（批量路由全响应 no-store）。未 commit/push。
- 2026-08-11 T-05 完成（真实浏览器验收标签整理，项目级 review 豁免）：smoke:threads 新增标签验收段内聚单块，置于 031 搜索段之后、终扫 git/秘密扫描之前（031 A-179 零位移先例，既有断言逐字节未动），证据四截图落 `features/032-thread-tags-bulk-organize/evidence/`、断言与 axe 态入 014 results.json（A-204 造数/置位默认）。验收链路全绿：desktop light 管理对话框（创建 trim/折叠重复「已存在」/40 grapheme 上限校验、contains 搜索命中与空态、初始焦点落新标签输入、删除/关闭按钮 44px、axe）→ API 真实分配（幂等切换矩阵）→ reload 后 chips 投影（name 码元序）→ 筛选条（chip 点击实核 GET `tagId=` 响应、UI 顺序与服务端页逐条一致、aria-pressed、空标签「下暂无线程」+ 清除筛选、与已收藏视图双向互斥回退、mutex 组合 400 `tagId:"not_combinable"`、chip 44px、axe、截图）→ desktop dark 筛选+axe+截图→回 light →「整理线程」多选（复选框 click+Shift+Tab/Space 键盘选、focus-visible 环、44px、筛选条整理模式隐藏）→ 批量条加/去 picker →「确认批量整理」对话框（如实计数文案、初始焦点落「确认应用」、Enter 确认）→ POST 200 receipt（operationId/threadIds/add/remove 逐字段断言、per-thread 摘要）→ 成功 notice「已为 2 条线程更新标签。」→ chips 静默对齐 → 同 operationId API 重放 replayed:true 响应逐字节一致且边不重复 → 异 hash 409 OPERATION_CONFLICT → 管理对话框删除（用量计数如实、确认文案「将解除 N 条分配」、初始焦点落「取消」、Esc 分层只关确认层焦点还搜索框、删除后 notice/列表/筛选条即时一致）→ reload 后各视图一致（筛选条仅余「全部/缺陷跟踪」、chips 3×缺陷跟踪、API 计数一致、已删 tagId 过滤空页 200）→ restartApp 重启保持（API 计数/筛选/chips 全一致）→ 跨项目隔离（外部项目真实造标签「外部标签」：legacy 列表/搜索零命中、筛选条不可见；跨 tuple 分配/删除/批量全 404 RESOURCE_NOT_FOUND 且 envelope 不回显他项目 id；跨 tuple 拒绝后边零变化；标签 API 响应逐体显式断言无 apiKey/masterKey）→ narrow light（抽屉内管理对话框初始焦点/搜索/44px/axe、Esc 焦点归还抽屉内开启器、chip Tab 焦点环+Enter 筛选、截图）→ narrow dark（筛选+axe+截图）。矩阵覆盖：desktop light（管理+chips+筛选+批量+删除全路径）、desktop dark（筛选）、narrow light（管理+筛选）、narrow dark（筛选）四态截图齐备。axe 6 态（desktop light 管理对话框/desktop light chips+筛选/desktop dark 筛选/narrow light 管理对话框/narrow light 筛选抽屉/narrow dark 筛选抽屉）blocking 与 color-contrast 全零，唯二残留为既有 aria-allowed-role:minor@窄屏抽屉（A-125/A-135/A-148/A-180 同例归因，低于门禁不修）。秘密扫描：标签 API 响应全入 productApiBodies 终扫 + 逐体显式断言；tagFacingText/narrowTagFacingText DOM 标签态文本与四截图字节入 publicSurfaces 终扫（apiKey/masterKey/宿主附件根零泄漏）。**生产缺陷修复（A-205）**：验收窄屏段首轮暴露 useModalSurface 抽屉暂停/恢复双缺陷——①恢复时初始焦点微任务无条件聚焦抽屉关闭钮，抢走刚关闭对话框归还给开启器的焦点；②恢复时重新捕获 body overflow 导致暂停/恢复循环后捕获值陈旧为 "hidden"，关抽屉后页面滚动永久锁定。修复=初始焦点微任务跳过「焦点已在 surface 内」情形 + body overflow 改模块级引用计数（仅最后解锁者恢复首次加锁前的值）；jsdom 回归钉于 cockpit-mobile-integration（抽屉内开管理标签→Esc→焦点归还开启器→overflow 保持→再 Esc 关抽屉→overflow 归零），RED 先证（无守卫时焦点落关闭钮）、受影响 tests/browser 20 文件 154 全过。**smoke 基建记录（A-206）**：第二轮运行在既有 014 输入历史段「没有匹配的输入历史。」waitFor 遇一次性超时（该段与本片改动零机械关联——桌面宽、无活动 modal surface、输入历史面板不用 useModalSurface），零改动重跑即过，归因 A-112 已记录的轮询/焦点竞争类既有不稳定苗头，未改既有段落。一次性全量验证：`npx vitest run` 267 文件 2431 通过（118.22s，基线 ~110s 内）；`npx tsc --noEmit` 通过；`npm run build` 通过（四条标签路由全部入列）。新假设 A-204（验收段造数/置位/扫描面默认）、A-205（抽屉暂停/恢复焦点与 overflow 双缺陷修复）、A-206（输入历史段一次性 flake 归因）。未 commit/push。
