# 规格

见 `architecture.md`（阶段 1 规格要点与测试缝同页展开）。演示判据：

1. 点击「打开文件夹」弹出系统文件夹选择器（测试中为脚本化路径），页面无「文件夹路径」输入。
2. 已有已验证 Provider 时出现规划/实施/复核三名 Agent；新文件夹项目自动包含他们。
3. 驾驶舱看不见使命看板、共享记忆、HelpTip；中间是群聊 composer。
4. 无 Provider 时不能假装发消息，引导配置模型服务。
